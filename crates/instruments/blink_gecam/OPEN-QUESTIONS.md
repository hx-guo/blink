# GECAM —— 已核实的事实与未决的问题

本文件记录 GECAM 三颗星接入 TGF 搜索时实地核对过的结论，以及还没做、
还没定标的部分。凡是"实测"字样，都指在 IHEP 集群上对归档文件直接统计的结果，
日期见各条。

## 一、三颗星与数据量（2026-09-10 勘测）

| | GECAM-A | GECAM-B | GECAM-C（HEBS） |
|---|---|---|---|
| 探头 | 25 GRD + 8 CPD | 25 GRD + 8 CPD | 12 GRD + 2 CPD |
| MET 历元 | 2019-01-01（MJDREFI 58484） | 同 A | 2021-01-01（59215） |
| 事例产品起止 | 2022-10-21 .. 2026 | 2020-12 .. 2026-09 | 2022-07 .. 2025-02 |
| 有数据的天 | 554 | 约 2100 | 818 |
| GRD 小时文件 | 4,609 | 约 52,700（含版本） | 19,624 |
| 单小时 | 974 MB / 5410 万事例 | 2.7 GB / 1.49 亿事例 | 297 MB / 1650 万事例 |
| 合计 | 约 4.5 TB | 约 110 TB | 约 5.8 TB |

GECAM-A 逐年的 GRD 小时文件数是 3 / 287 / 481 / 2693 / 1145（2022..2026），
**绝大多数在 2025 年以后**，落在 WWLLN 库（止于 2024-12-31）之外；2024 年底
之前只有 771 小时。GECAM-C 逐年 3228 / 8198 / 7419 / 779 小时（2022..2025），
2022–2024 基本满 24 h/天，整段在 WWLLN 覆盖内。

归档路径：A/B 在 `/gecamfs/Archived-DATA/GSDC/LEVEL1/daily/{yyyy}/{mm}/{dd}/GECAM_{A,B}/`，
子目录小写（`GRD_evt` `CPD_evt` `posatt`）；C 在 `/gecamfs/hebs/Archived-DATA/GSDC/LEVEL1/daily/{yyyy}/{mm}/{dd}/`，
日期目录下没有卫星层、子目录大写（`GRD_EVT` `CPD_EVT` `POSATT`）。文件名
`{星代号}{类型}_{yymmdd}_{HH}_v{NN}.fits`，同一小时多版本取版本号最大的。

**不是可用卫星的**：`/gecamfs/GTM-DATA` 只有 2024 年的 LEVEL0 码流，没有 LEVEL1；
`/gecamfs/DRO` 只有 2024-04 的遥测，而且 DRO 是地月远距离逆行轨道，看不到地球 TGF。

## 二、文件格式（三颗星一致）

逐小时 FITS：`PRIMARY` + `EBOUNDS` + `GTI` + 每探头一个 `EVENTS%02d`。
GRD 事例列 `TIME, PI, GAIN_TYPE, DEAD_TIME, EVT_TYPE, FLAG, EVT_PAIR`，
CPD 少 `GAIN_TYPE` 和 `EVT_PAIR`。整套结构与 SVOM/GRM 同源，`blink_svom_grm`
就是本 crate 的模板。

**能量梯。** EBOUNDS 的道 0..447 是一条连续单调的梯，2.00 keV 到 10053.5 keV，
相邻道 `E_MIN[k+1] == E_MAX[k]`，每道宽约 1.34%（低端疏、高端密，不是均匀对数：
ch0 = 2.0–2.2、ch50 = 35.0–36.0、ch54 = 39.1–40.2、ch100 = 100.1–101.8、
ch200 = 373.9–378.9 keV）。**B 与 C 逐道数值完全一致**，所以道号阈值三星通用；
但这是实测结论不是设计保证，`EvtFile` 载入时逐条核对（头尾、梯内连续性、
40 keV 是否仍落在 ch54），对不上就整小时报错。

> **⚠️ 2026-09-11 更正：上一段的"三星通用"只对 448 道那把梯子成立，不是通则。**
> **GECAM-C 2022-08-03 .. 2022-10-15 的 1,121 个小时用的是另一把 896 行的
> EBOUNDS**（正好细一倍，覆盖同一能量范围），在那把梯子上 **40 keV 落在
> ch109 而不是 ch54**、梯顶在 ch895 而不是 ch447。**写死的道号在两把梯上是
> 两个能量**，照搬会把能窗整个搬错位（14 keV 以上全收、514 keV 封顶）。
> 载入时的核对**动作是对的**，这 1,121 小时现在被整小时判 `corrupt_data`；
> 要修的是把梯长与能阈道都从 EBOUNDS 自己推出来，见第 21 条。
> **B 星无细梯纪元（646 天逐日抽点全部 498 行），A 星待查。**

**溢出道。** 道 448 及以上 `E_MAX` 一律 20000 keV、`E_MIN` 不单调（每探头每
增益档各占一行，B 有 50 行、C 有 22 行），能量不可信。实测 `EVT_TYPE == 2`
的事例 PI 正好全落在这一段（B 某小时单路 6.00%、C 15.11%）。
**同样只对 448 道那把梯子成立**：896 道那把的溢出哨兵不在表里（表止 ch895，
`EVT_TYPE == 2` 的 PI 落在 896..917）。**"溢出段从 448 起"是这把梯子的长度，
不是仪器常数。**

**增益。** `GAIN_TYPE` 0 高 / 1 低，两档共用同一套 PI 刻度（C 实测高增益
PI 11..448、低增益 179..460），所以道号可以直接比，不必分档处理。

**位姿。** `Orbit_Attitude` 表 1 Hz、一小时 3700 点（越过整点约 100 s）。位置
只给地固系直角坐标 `X/Y/Z_WGS84`，**单位是米**（|r| 实测 B 6.963–6.974e6、
C 6.855–6.870e6），要自己折成经纬高，见 `io::geodetic`。四元数模为 1。

**GTI 占空比低。** 抽查 GECAM-C 某小时 GTI 只有 1763 s / 3700 s（切成 4 段）、
GECAM-A 某小时 1303 s / 3600 s（1 段）。曝光按 GTI 算，事例流也按同一份 GTI
过滤——SVOM/GRM 上只在分母上用 GTI 而事例照单全收，缺口两端半秒数据的本底窗
伸进空区，造出过整整两类假信号。

## 三、当前判据

* 事例准入（`Event::keep`）：`EVT_TYPE == 1` 且 `54 <= PI < 448`。
  **两个道号都是编译期常量，因此只对 448 道那把能量梯成立**——GECAM-C 早期
  1,121 小时的 896 道梯上它们对应的是另外两个能量，那批小时现在被整小时拒收，
  见第 21 条。**在改成逐文件推出来之前，这一行准入不是三星通用的。**
* `min_duration = 0`（搜索参数，**不要动**）；窗长判别在候选表上按搜索报出来的
  `bin_size_best` 事后做，**不是**靠抬 `min_duration`。两者的区别与代价见第 14
  条的对照表、阈值见第 30 条。
* GTI 同时用于分子（事例过滤）与分母（曝光）。
* 单路探测器占比 > 0.8 的候选否决，按整候选窗算，四台仪器共用
  `blink_algorithms::detector_share`。
* 搜索窗 0 .. 1 ms，`false_positive_per_year = 20`，`min_number = 8`，
  `coincidence = 1`（各路 GRD 朝向不同，合成一路）。
* CPD 计数随每个候选记录在 `Signal::acd` 里（`AcdCounts` 的字段在 GECAM 上的
  含义见 `chunk::search::cpd_counts` 的文档注释）——**只统计，不否决**。
* 逐路 GRD 计数（候选窗与基线窗各一份）记录在 `Signal::detectors` 里，见
  `blink_core::DetectorCounts` 与下面第 13 条——同样是**只统计，不判**。

## 四、未决的问题

### 1. 能阈 40 keV 没有用 GECAM 自己的 TGF 定标

`MIN_CHANNEL = 54`（39.1–40.2 keV）是照 SVOM/GRM 的量级取的起点。SVOM 那个
42 keV 是拿 72 个闪电证实的 TGF 扫出来的：证实样本的窗内谱在更低的道上一律
低于本底、从 42 keV 起才反超，切在那里少 15% 信号、少 43% 本底，信噪反升。
GECAM 是 LaBr₃、能量梯一路到 10 MeV，谱学响应不同，这条结论不能直接搬。
**要做的**：等 GECAM 攒出闪电证实样本后按同样办法重扫，脚本可照
`scripts/plot_svom_threshold.py` 改。

### 2. CPD 判据未定标

CPD 是 GECAM 相对天格/SVOM 最大的优势——一路独立的带电粒子探测器，可以直接
回答"这个候选是不是粒子"。但"符合到什么程度算粒子"没有数据支撑，所以现在
只把计数存下来（`n_acd`、`n_acd_multi`、`n_acd_bg`），不做否决。
**要做的**：全量跑完后拿真候选看 `n_acd/n` 的分布，看它与闪电关联率的关系，
再定阈值。存原始计数而不是比例就是为了这一步能复议而不必重跑。

顺带：天格那 51 个"毫秒级软暴"至今说不清是什么（谱与本底重合、高磁纬、
无闪电、不成串）。如果 GECAM 上能找到同类事件，CPD 可以一次性回答它们
是不是带电粒子。这是做 GECAM 的一个主要动机。

### 3. `EVT_TYPE == 3` 稀有到无关紧要（已答，2026-09-10）

抽 GECAM-C 三个不同月份的整点小时：

```
2022-08-15 03h  事例 227,843,552  type3 =  2  (0.0000%)
2023-01-30 07h  事例  23,239,879  type3 = 24  (0.0001%)
2023-06-15 12h  事例  19,966,568  type3 = 13  (0.0001%)
```

性质三个小时高度一致：**PI 全部落在 `keep` 窗 [54, 448) 内**（中位 170–415，
`>= 448` 占 0%）、`GAIN_TYPE` 全是 0（高增益）、`FLAG` 全是 0、散在 3–4 路
探头上、时间不成簇（相邻间隔中位 27–590 s，`< 1 ms` 占 0%）、GTI 内占 50–85%。

**每小时整仪器只有个位到二十几个，占事例的 1e-6 量级**，一天全仪器约 300 个
摊在 12 路 × 86400 s 上。能量、增益、标志、成簇性都看不出是什么特殊物态，
但量级上不值得再追。**`keep` 只认 1 不改。**

顺带一条已经实测钉死的：**`PI >= 448` 的占比与 `EVT_TYPE == 2` 的占比逐日
逐位完全相同**（GECAM-A 7 个 epoch 全部），所以两者不是包含关系，**是同一个
集合**——第二节里原来写的"`EVT_TYPE == 2` 的事例 PI 正好全落在这一段"可以
加强成这一条。GECAM-C 上同样自洽：那三个小时 `EVT_TYPE == 2` 占 11.9–17.3%，
而 `type3` 的 PI 全 < 448、`type1` 的分布也没有溢出，448 以上全归 `type2`。
`Event::keep` 里那两条冗余判据照旧都留着——**但它们否的都是同一批事例，
真正缺的上界见第 19 条。**

### 4. `FLAG` 标的是双增益对里的角色（已答，2026-09-10）

实测 GECAM-B 某小时单路 `FLAG` 只有 0（89.98%）和 10（10.02%）两个取值，
FLAG=10 的 PI 中位是 448、与溢出道重合，先前记的是「看着像溢出标志」。
拆双增益重复记录时拿到了答案：**同一路探头同一时戳的事例对，`FLAG` 组合
100% 是 (0,10) 或 (10,0)**——它标的是这条记录在双增益对里是哪一支，
「FLAG=10 的 PI 中位是 448」只是「高增益那支常常饱和」的表象。

判据没有用 `FLAG`，也不打算用：去重走的是「同探头同时戳」这个实测事实，
见第 17 条。

### 4b. 审计的基线窗不是搜索的本底窗（已改注释，窗不动）

`cpd_counts` 与 `detector_counts` 取的基线是候选两侧各
`CPD_BASELINE_SECONDS = 1 s`，而 `search_new` 用的是两侧各 `neighbor / 2`
= **0.5 s**——**审计窗正好是搜索窗的两倍宽**。原注释写的是"与搜索的
`neighbor` 同一量级"，是错的。除此之外还差两件事：**审计不挖空窗**
（搜索从本底里挖掉候选两侧各 `hollow / 2` = 5 ms），**审计不夹 GTI**
（搜索把本底窗夹到候选所在的那段活时间里再算时长，审计的
`baseline_seconds` 是标称的 2 s、不是活时间）。

**后果**：`n_acd_bg / baseline_seconds` 只有在本底率**平稳且窗内没有 GTI
缺口**时才是"该候选处的 CPD 率"。GECAM 的候选恰恰常落在率剧变的地方
（GTI 边界、高磁纬进出、粒子增强期），而 GECAM 的 GTI 一小时常被切成
一到四段——**窗跨了缺口就低估率，跟着高估任何「观测/期望」的超出**。
第 14 条里那些 obs/exp 的数如果是用这个标称分母折的率算的，方向就是
偏大的一侧；要准得回 CPD 事例流按活时间自己数。**另外不能拿这些计数去
反推搜索的 `mean`，会差两倍。**

**实测偏差在 GECAM-C 上可忽略，但要按仪器分别量。** 3385 个候选逐条比
「基线窗与 GTI 的交集时长」和标称 1.98 s：`t_bg` 中位 1.9800、1–95 分位
全是 1.98，**只有 0.38%（13 个）被 GTI 切到**（那 13 个 `t_bg` 中位 1.137 s、
朴素法把率低估中位 1.74 倍）。全样本 obs/exp 正确 3.533 对朴素 3.535，
**差 0.1%**；判据链那条 `CPD 本底率 ≤ 500 c/s` 两种算法各留 3246 个、
**逐条不一致的 0 个**。所以 C 星现有的 obs/exp 不用改。**但 C 星占空比
62.7%、缺口中位 393 s（长而稀），贴边的候选才少；GECAM-B 的 GTI 切得更碎，
那边必须重新量。**

**正确的做法是分子分母都夹 GTI**（`evidence/gecam-c/gc_cpd_norm.py` 就是
这么做的）：本底事例按 GTI 过滤，分母取「基线窗与 GTI 的交集时长」而不是
标称窗宽；两个零假设对照（平移窗 + GTI 内随机时刻，各 13540 次）在四个
符合半宽上都回到 obs/exp = 1.00 ± 0.4%，**随机对照必然抽到贴边位置，
回得到 1.00 说明分母没有在 GTI 边界上系统性虚低**。

**决定：不改窗，只把话说清楚。** 两倍窗给的统计量更多、更稳，而改窗会让
已经落盘的候选表前后不可比。关系钉在
`the_audit_baseline_window_is_twice_the_search_one` 这条测试里。

### 4c. 探头级停机：GTI 不表达，只有逐路基线看得见

GTI 是**全仪器**概念。25 路里掉一路，仪器照样"活着"，只是有效面积在变，
而 `mean` 的分母不会跟着改、`fa` 就偏低。HXMT 那边查明的机制是**整个机箱
静默好几秒**（不是 30 ms 级短缺口），停摆前每 100 ms 的计数被顶到上限
约 972 的 FIFO 饱和平台，三个箱先后停摆，逐路基线向量给出的
`111111000000000000` 一字不差对上时序。**他们的全量还没跑完，87 个样本里
83 个是列车成员、可能早被摘掉，所以这在 HXMT 上还不是已确认的缺陷。**

GECAM 25 路 / 12 路同理，而我们已经把逐路基线向量存进 `Signal::detectors`
了，**查这个几乎零成本**。**GECAM-C 未观察到**（2023-06-15 全部 3385 个候选、12 路基线向量）：有零格
的候选 **0 / 3385**，逐路零格次数全 0，基线计数分位 1/5/50/95 =
64 / 507 / 1069（最低的 1% 也有 64 个计数 / 2 s，离零很远）。
**记成「未观察到」而不是「不存在」——只有一天。**

**还要做的**：GECAM-A（25 路）与 GECAM-B（25 路）上同样统计一遍，路数多一倍，掉一路的概率更高。

### 5. 姿态四元数的分量顺序未核实

表里是 `Q1..Q4`，`blink_core::Attitude` 只收三个分量，现在原样取前三个。
标量分量到底在 Q1 还是 Q4 没有核实。姿态目前只作元数据随候选记录、不参与
任何判据，所以不影响搜索结果；但**做流强/光子谱就会卡在这里**——SVOM 上
就是卡在同一个问题上（响应矩阵要靠姿态定入射方向）。

### 6. `min_number = 8` 比已发表样本低了整整一档

GECAM-B 单路 GRD 实测约 894 c/s，25 路合计约 22 kc/s（能阈过滤前）；
SVOM/GRM 三路合计不到 1 kc/s。`min_number` 是计数下限，本底高时基本不起
作用（泊松判据先绑定）。

**已发表目录给了一把尺子**（见第 10 条）：147 个真 TGF 的 `NetCounts`
中位 **66.9**、5–95% 21.2–185、最小 8.7，**只有 3.4% 少于 20 计数**。
而我们 GECAM-C 一天的候选计数中位 **10**、只有 10.4% ≥ 21。**我们的阈值比
已发表样本低了整整一档，也就是说我们和他们不是在找同一批东西。** 这解释了
候选量超出的一部分，但不是全部——见第 14 条的验收线。

**要做的**：先做第 14 条里那个零成本实验（把已落盘的候选按等效显著性阈切
一刀），再决定 `min_number` 要不要动。

### 7. 窗长上限 1 ms：两个已发表目录给出相反的证据，**不要关掉**

沿用 SVOM/GRM 的 1 ms。SVOM 上验过 5 ms：候选多 47% 而闪电关联数为纯本底，
82 个证实 TGF 一个不多一个不少。

**支持 1 ms 够用的**：已发表的 GECAM TGF 目录（第 10 条）147 个事件的
`Duration_us` 中位 179.5 µs、95% 分位 372、**最大 670.9，100% 短于 1 ms**。

**反对的**：Fermi/GBM 那一路拿已发表 GBM 目录做完备性检验，**时长 > 1 ms
的那一档找到率只有 82.9%、达显著 43.9%**，而 < 0.5 ms 那档是 98.0% / 79.7%——
直接对应 `max_duration: 1 ms` 的损失。

**两者不矛盾，但要先对齐口径才能判**，至少三种解释：

* **时长定义不同。** GECAM 目录的 `Duration_us` 是 **Bayesian Block 块长**，
  不是 T90，也不是半高全宽；GBM 目录那一列是什么口径还没核。BB 与 T90 在
  低计数下的偏向方向不同，不能直接比。
* **选择效应不同。** GECAM 那 147 个是触发式样本、判据严到 P < 2e-21，
  **亮而短的容易入选，长事件通常暗、会被系统性丢掉**——所以"100% 短于
  1 ms"不能读成"TGF 都短于 1 ms"，它跟 GBM 找到更长的一批是自洽的。
* **仪器真看到不同的东西。** GBM 有效面积大得多，探得到更长更暗的一批。

**要做的**：核实两个目录各自的时长定义，口径对齐后再判。**如果长事件是
真的，1 ms 上限就是四个仪器共同的已知损失**，该写进每一路的记录，而不是
某一路单独关掉它。GECAM 自己的复核要等闪电证实样本攒出来后量 T90，同
SVOM 的办法。

### 8. GECAM-B 的内存与总量（已实测，2026-09-10）

**总量是 35.8 TB，不是 110 TB。** 逐文件 `find -printf "%s %p"` 实测：
GRD `.fits` 共 52,766 个（含全部版本 65.6 TB）；**每小时取最大版本后
26,761 小时 / 35.80 TB**，版本冗余占 43%、搜索一个都不读；再要求
GRD+CPD+posatt 三件齐全（`from_epoch` 的硬条件），**可搜 26,751 小时 /
35.78 TB**，只有 10 个小时缺件。原来那个 110 TB 是拿「52,700 × 2.7 GB」
外推的，而 **2.7 GB / 1.49 亿事例那个小时是 p93 不是典型小时**：去重后单
小时中位 1.23 GB（约 6,800 万事例）、均值 1.34 GB、p95 2.91 GB、最大
11.9 GB（约 6.6 亿事例）。事例行 `D,I,B,E,B,B,B` = 18 B，与 2.7 GB / 1.49 亿
= 18.1 B/事例 相符，所以字节数可以直接折事例数。

**逐日覆盖 1,813 天、平均 14.76 小时/天，只有 262 天是满 24 小时。**
26,751 可搜小时折合 1,115 个满天（GTI 还没扣）。逐年（去重）：2021 5,183 /
2022 6,901 / 2023 3,872 / 2024 3,928 / 2025 5,852 / 2026 1,024 小时。
**WWLLN 覆盖内（2021–2024）19,884 小时 / 27.6 TB，占 74%**——CPD 判据定标
（第 2 条）能用的就是这一段。

**峰值内存实测**（作业 81204578，1 s 采样 `VmHWM` 并记录当时打开的 FITS
路径，所以每个采样点能归到具体小时）：典型小时 1.1 GB 文件 → 峰值 2.15 GB，
1.5 GB 文件 → 3.18 GB，**全归档第三大的 11.1 GB 小时 → 峰值 20.37 GB，
没被 OOM**。峰值/文件字节约 **1.84–2.1**。载入 1.1 GB 约 5 s（约 220 MB/s），
整小时墙钟约 100 s——**时间花在搜索不在 I/O**。

一个作业跑一整天时内存由那天最大的小时定：day-max 中位 1.84 GB（→ 约
3.8 GB RSS）、p90 3.52 GB（→ 约 7 GB）、p99 8.21 GB（→ 约 16 GB）、最大
11.90 GB（→ 约 22 GB）。所以 **`-mem 25000` 一档覆盖全部 1,813 天**；想省
资源可分两档：`-mem 12000` 覆盖 day-max ≤ 5.7 GB 的 1,734 天（95.6%），
余下 79 天单独用 `-mem 25000`。

**`RAYON_NUM_THREADS` 是空设置。** `rayon` 在本仓库零命中（连 `Cargo.lock`
里都没有这个包）。串行链：`blink/src/main.rs` → `blink_search::process` →
`blink_workflow` 的 `for day_offset in 0..total_days` 裸循环 →
`blink_search` 的 `for hour in 0..24` 裸循环。**一个进程同一时刻只驻留一
小时**，所以上面那些峰值就是单进程峰值，作业脚本里那行 `export
RAYON_NUM_THREADS=2` 什么也没做。

**HHMMSS 命名的文件（已修）。** 2021 年有 2,384 个 GRD 文件把小时字段写成
HHMMSS（`gbg_evt_210228_190000_v00.fits`），而 `io/file.rs` 原来拼的是
`..._19_v`，`starts_with` 匹不上，这批文件对搜索不存在。按小时去重后这种
写法覆盖 1,559 小时，其中 **102 小时是独苗**（分布在 32 天、全在 2021），
占 0.4%——**原本判断为"只记不改"**。

**改口的理由**：**已发表的 147 个 TGF 里有 2 个（1.4%）正落在这种小时上**
（2021-03-25 09、2021-05-02 16）。匹不上就等于连数据都读不到，完备性检验
量的就成了文件名匹配而不是搜索算法。0.4% 的曝光不值得改，1.4% 的外部真值
值得。

现在两种写法都认，三类产品一起放宽。**版本号要单独解析出来比大小，不能
靠整串文件名排序**：两种写法混在一起时 `_190000_v01` 会排在 `_07_v00`
前面（`'0' < '_'`），照字典序取最后一个就会拿到低版本。放宽也不能顺带
放宽小时——`_19` 之后只许接 `_v` 或四位数字 MMSS 再接 `_v`，三条都有测试。

### 9. GTI 缺口不在 SAA，在极区（已答，2026-09-10）

SVOM 上验过"缺口全部落在南大西洋异常区、因而不必另立 SAA 掩模"。**这条在
GECAM-C 上不成立。** 取 6 天（2022-09-15 / 2023-01-30 / 2023-06-15 /
2023-11-02 / 2024-03-10 / 2024-11-20）全部 139 个小时文件的 GTI，缺口取中点
用 POSATT 定星下点，370/371 个定到位：

```
GTI 合计 319428 s / 文件跨度 509399 s → 占空比 62.7%
缺口 371 个，总时长 189968 s（占跨度 37.3%），时长分位 5/25/50/75/95 =
  107.5 / 333 / 393 / 486.5 / 1298 s
缺口中点 |偶极磁纬| 中位 61.4°（5–95% 39.2–72.0）
  |磁纬| > 55°（极区）: 82.4%      |磁纬| < 30°: 3.0%
  落在经典 SAA 框内   :  3.8%
  纬度分位 5/25/50/75/95 = −74.7 / −55.7 / 42.3 / 65.7 / 69.5（双峰，两个极冠）
```

长缺口（> 60 s，365 个）同样：SAA 框内 4%、|磁纬| 中位 61.4°、> 55° 占 83%。
C 星在太阳同步轨道，每圈两次穿极冠，缺口中位 393 s ≈ 6.5 分钟，与一次极区
穿越的时长量级相符。

**两个后果，要记进曝光口径：**

1. **曝光不是纬度均匀的**——我们系统性地丢掉极冠，而极冠正是粒子最多的
   地方。所以"候选里 47% 在 |磁纬| > 40°"是**已经切掉最脏的一段之后**还剩
   下的，污染比表面看到的更重。
2. **要不要另立 SAA 掩模仍未答**：缺口不覆盖 SAA，那 SAA 穿越期间到底有没有
   数据、数据脏不脏，还没查。查法很便宜：拿候选的位置对 SAA 框做直方图，
   不用重读事例流。

### 10. 已发表的 GECAM TGF 目录（到手，2026-09-10）

Zenodo `10.5281/zenodo.8028217` 的 `GECAM_TGF_Catalog.xls`，**147 行**，
列有 `UT`（微秒精度）、卫星经纬高、`Duration_us`、`NetCounts`、
`HardnessRatio_200keV`、`CPDtoGRDcountsRatio`。转成的 CSV 在
`scratch_gbm/gecam/gecam_tgf_catalog.csv`。

**样本身份**：Zhao et al. 2023 GRL / arXiv 2306.10255。**只用 GECAM-B**
（原文：A 星当时还没开机），**2020-12-10 至 2022-08-31**、有效观测
274.5 天，判据是"≥2 个 GRD 组各 ≥8 计数、100 µs 分箱"、P < 2e-21，
`Duration_us` 是 **Bayesian Block 块长**。**是亮事件搜索**——这不影响它当
真值用，只影响怎么解读：找不到亮 TGF 是硬伤，找到一堆它没有的东西不一定
是错，而"它没有长事件"也可能只是选择效应（见第 7 条）。

**与 GECAM-C 只重叠 4 个**（C 星 2022-07-27 才入轨），**与 GECAM-A 零重叠**
（A 星 2022-10-21 才有第一批事例）。所以完备性检验只能在 **GECAM-B** 上做，
性质与 GBM 那边的 88.9% 完备性 / 70.7% 达显著同一类。

**分布**：

```
Duration_us            中位 179.5   5–95% 35.7–372   最大 670.9  → 100% < 1 ms
NetCounts              中位  66.9   5–95% 21.2–185   最小   8.7  → 只有 3.4% < 20
HardnessRatio_200keV   中位 0.679   5–95% 0.444–0.865
CPDtoGRDcountsRatio    中位 0.040   5–95% −0.010–0.178   34.7% ≤ 0
|纬度|                 中位 11.5°   >30° 占 0%（B 星轨道倾角 29°，是轨道限制）
```

> **⚠️ 2026-09-11：下面那个 142/147 是 `e34f560` 口径的数，已作废，重跑中。**
> 三条独立证据定位到当时跑的是 `gecambrun/blink_gecam`（md5
> `aa12ec0afcb6f7a179c1a0bd2dcf5c58` = commit `e34f560`），**而 `e34f560` 的
> 双增益去重判据是「同探头时戳完全相等」**；`3c017a2` 换成「同探头、死时间内、
> 一高一低」之后，**B 星实测时戳相等只覆盖真实重复的 88.44%，漏掉的 11.56%
> 全落在 89 / 104 ns 上**——正是候选窗的尺度。去重更狠 → 候选计数下降 →
> 暗端 TGF 可能掉到 `min_number = 8` 之下（147 个里 `NetCounts` 最小 8.7）。
> **所以"代码 diff 是空的、结论可以沿用"这个推论不成立**：空的是
> `3c017a2..main`，不是 `e34f560..main`。
>
> 第二个失效的口径：当时 `GECAM_ARCHIVE` 指向一棵影子树，里面把 2 个 HHMMSS
> 命名的小时**改名**成标准写法。`io/file.rs` 现在已经认 HHMMSS，重跑只按原
> 文件名软链、跑真归档。
>
> **教训：二进制文件名一律带 commit 或变体后缀。** 不带后缀的 `blink_gecam`
> 被覆盖过三次（`e34f560` → `de4badf` → `3c017a2`），是这次追溯困难的根源。

**完备性检验做完了（2026-09-10）：142/147 = 96.6% 找回，找回的
142/142 = 100% 达 `fa ≤ 1e-5`。** GBM 那边同类检验是 88.9% / 70.7%，
GECAM-B 两项都更高。147 个所在的 138 个小时全部 searched，曝光 346,934 s。

**时刻差 `dt`（我们的最佳格起点 − 目录 UT）中位 +0.096 ms、5–95%
−0.038 .. +1.82 ms**——这顺带把 MET↔UTC 的历元和时戳口径两边验了一遍。

> **⚠️ 2026-09-11：原文这里写的"没有系统性偏移"分辨力不够，要改口。**
> `dt` 中位 +0.096 ms，而暴发时长中位只有 179.5 µs——**偏移比暴发本身还大。**
> 逐个看：第一个 TGF `2021-01-20T08:11:28.313707`（目录 `Duration_us` 73.78、
> `NetCounts` 65.19），按「目录 UT + `Duration_us`」取窗只有 **7 个计数**、
> `mf = 0`、`f₃ = 0`；真正的暴发在 **dt = +54.5 µs** 处，`count = 66`
> （对上 `NetCounts` 65.19）、`bin = 70.97 µs`（对上 `Duration_us` 73.78）、
> `fa = 5.38e-90`。**两个独立的量同时对上，才确认那才是同一个事件。**
>
> **后果（会传染）：任何拿「目录 UT + `Duration_us`」当真 TGF 窗算出来的量
> ——同戳占比、CPD 符合、谱比——量的都是暴发前的本底，不是 TGF。** 正样本
> 一律用匹配上的候选窗（最佳格），另加一个构造无关的固定窗
> `[UT − 0.1 ms, UT + 2.0 ms]` 做交叉检验。
>
> **匹配容差的偶然期望要并排给**：B 星候选率 8.67 /s，±10 ms 内偶然撞上别的
> 候选的期望是 0.173 个/TGF，142 个匹配里理论上可有约 25 个是偶然。实测
> `|dt|` 最大只有 3.06 ms、中位 0.107 ms、> 2 ms 的只占 4.3%，**没有铺满
> ±10 ms，所以偶然匹配没有真的发生**——但别在别的星上照抄 ±10 ms：A 星候选率
> 10.5 /s，同样的窗偶然期望是 0.21，比 B 星还高。

> **⚠️ 2026-09-11 再修一次：`dt` 不是一个常数偏移，它正比于暴发自己的长度。**
> 新口径重跑（138 小时，669/669 天，完整性断言过）量到：
>
> | | |
> |---|---|
> | `dt` 中位 | **+95.8 µs**（与全队那个 +90.4 µs 一致） |
> | `dt` 5–95% | **−37.9 .. +1816.5 µs**（跨两个数量级，**不是窄分布**） |
> | 与目录 `Duration` 秩相关 | +0.304 |
> | 与 `NetCounts` 秩相关 | **+0.455** |
> | **`dt / Duration` 中位** | **0.737** |
>
> 按 `Duration` 分档，`dt` 中位从 0–100 µs 档的 +30 µs **单调涨**到 200–400 µs
> 档的 +202 µs。
>
> > **⚠️ 同日再修：上面这个"偏移正比于暴发长度"的读法是错的，`dt` 跟的是
> > 亮度。** 偏相关做完（n = 142，秩偏相关）：
> >
> > | | |
> > |---|---|
> > | `dt ~ Duration` | +0.304 |
> > | `dt ~ NetCounts` | +0.455 |
> > | `Duration ~ NetCounts` | +0.539 |
> > | **`dt ~ Duration \| NetCounts`** | **+0.078 ← 控住亮度之后几乎没了** |
> > | **`dt ~ NetCounts \| Duration`** | **+0.363 ← 控住时长之后还在** |
> >
> > **所以 `dt / Duration = 0.737` 是两个自变量彼此相关造出来的假象**，
> > 「目录 UT 给起点、最佳格落在时长的 3/4 处」站不住。**这条错误曾经被广播
> > 到全队（"按 0.7 × Duration 缩放"），已更正。教训：偏相关没做之前不要
> > 把两个相关自变量里的哪一个认定成因。**
> >
> > **匹配器的嫌疑已排除**：四种挑法（最显著 / 最近 / 计数最多 / 最早）并排，
> > 前三种中位 +95.8 .. +98.7 µs、与 `NetCounts` 相关 +0.41 .. +0.46，几乎一样；
> > ±10 ms 内窗内候选数中位只有 1（5–95% 1..2），与 `NetCounts` 相关只有 +0.036。
> >
> > **`Duration_us` 的定义也查清了：既不是 T50 也不是 T90，是 Bayesian Block
> > 的块长**（Zhao et al. 2023, arXiv 2306.10255）。这给出一个**不用改我们这边**
> > 的候选解释：**BB 的块起点本身就依赖亮度**——同样的上升沿，暴越亮越早越过
> > BB 的可探测门槛、块起点越早，于是 `dt` 越大。与"控住时长后 `dt` 仍随亮度走"
> > 一致。**但这只是候选解释，没钉死。** 要钉死只能做构造无关的复测：只锚目录
> > UT、在原始事例流上用固定窗滑动找峰，看 `峰 − 我们的 t0` 与 `峰 − 目录 UT`
> > 哪一个跟亮度相关——前者不相关、后者相关 ⇒ 是目录的 BB；反之是我们的最佳格。
> >
> > **在钉死之前，"目录 UT 偏早约 +90 µs"这个口径全队都不该当常数用**：实测它
> > 随亮度从下四分位约 **+11 µs** 涨到上四分位约 **+394 µs**，**跨 35 倍**。
> > **操作结论：不要用任何固定偏移，也不要按时长缩放——锚到实际的峰**（匹配上
> > 的候选最佳格）；要构造无关的对照就用下面那个非对称固定窗，并把偶然污染
> > 一起报。

**匹配窗应当是非对称的**（2026-09-11，把上面那条"固定窗 `[UT − 0.1, UT + 2.0] ms`"
的理由补全）：**偶然期望精确正比于匹配窗的总宽度**——候选率 642206/天 = 7.433/s，
按 `宽度 × 率 × 147` 预言 ±0.5 / ±3 / ±10 ms 三档分别是 1.09 / 6.55 / 21.9 个，
实测 1.11 / 6.52 / 20.59，**三档全对上，误差 2% 以内**。而 `dt` 是**单边分布**
（83% 为正）。**对称窗等于把一半宽度花在 dt 几乎不出现的负侧。**

**二维扫描做完了（2026-09-11），最优是 `[−1000, +3000] µs`：找回 141/147、
偶然期望 4.45（3.0%）。**

| lo ＼ hi (µs) | +300 | +1000 | +2000 | +3000 | +5000 | +10000 |
|---|---|---|---|---|---|---|
| −3000 | 99/3.7 | 127/4.5 | 136/5.6 | 141/6.7 | 142/8.9 | 142/14.5 |
| **−1000** | 97/1.4 | 126/2.2 | 136/3.3 | **141/4.45** | 142/6.7 | 142/12.2 |
| −100 | 95/0.4 | 124/1.2 | 134/2.3 | 139/3.4 | 140/5.7 | 140/11.2 |
| 0 | 75/0.3 | 104/1.1 | 114/2.2 | 120/3.3 | 121/5.6 | 121/11.1 |

（每格 = 找回数 / 偶然期望）

**同宽度的对称对照才是公平的比法**：`±2000 µs` 同样 4000 µs 宽、偶然期望
**逐位相同的 4.45**，但只找回 **136**——**非对称在同等偶然代价下多找回 5 个**。
反过来比：对称窗要拿到 141 得开到 `±3000`（偶然 6.68），**非对称只要 4.45，
偶然低 33%。** 这张表也再次验证「偶然期望精确正比于总宽度」：**宽度相同 ⇒
偶然期望逐位相同。**

**对外的完备性口径定为 `[−1000, +3000] µs`，141/147 = 95.9%，偶然期望 4.45
个（3.0%）。**（先前提的 `±3 ms` 同样是 141，但偶然是 4.6%。）

**召回率必须与偶然污染并排报，单报召回没有意义**：

| 容差 | 找回 | 偶然期望 |
|---|---|---|
| ±0.5 ms | 110/147 = 74.8% | 1.11（1.01%） |
| ±1 ms | 126/147 = 85.7% | 2.21（1.75%） |
| **±3 ms** | **141/147 = 95.9%** | **6.52（4.62%）** |
| ±10 ms | 142/147 = 96.6% | **20.59（14.5%）** |

**对外报 ±3 ms 那档，±10 ms 只作上界。** 召回的 142 个全部 `fa ≤ 1e-5`
（142/142），没有一个是靠放宽阈值捞回来的。找回者 `count` 中位 **64**
（5–95% 18..168）——**这是第 22 条门槛 (1) 比较净计数时的分母基线**。

**没找回的 5 个**（±10 ms 内一个候选都没有，不是 `fa` 不够；`NetCounts`
20–94 跨全档，所在小时候选 1267–14574，**既不是暗端也不是稀疏小时**）：
`2021-04-25T22:31:42.346340` / `2021-06-21T22:38:57.377719` /
`2021-08-09T05:40:44.260704` / `2021-10-05T10:16:04.302001` /
`2022-06-15T18:06:04.110702`。未解释。

**这条把两种可能分开了：搜索没问题，问题在判据。** 但它测的量有上限：147 个
是 P < 2e-21 的触发式亮样本（`NetCounts` 中位 66.9），而我们的候选计数中位
只有 10——**完备性高只说明我们在亮端不漏，说明不了暗端找的是什么**，而暗端
正是绝大多数候选所在的地方。

**而且它同时否掉了"靠显著性阈分开"这条路**：142 个真 TGF **全部** `fa ≤ 1e-5`，
而 GECAM-B 那 138 小时里的候选是 **300.8 万个 = 74.9 万/天**（已发表速率
0.54 个/天，高出 6 个数量级；GECAM-C 是 3385 个/天，B 星又高出 2 个数量级）。
**真样本和污染在 `fa` 上完全重叠，只能靠粒子判据分。**

**没找回的 5 个**（所在小时全部 searched，不是数据缺失）：时长偏长的居多
（5 个里 3 个 > 190 µs，而全样本中位 179.5），**与 GBM 那边"长事件找回率低"
的方向一致**，指向第 7 条那个 `max_duration = 1 ms`。逐个的归因（GTI 切掉 /
单路占比否决 / 根本没进候选）还在查。

**一个口径必须记住**：147 个里有 2 个的小时文件是 HHMMSS 命名的
（2021-03-25 09、2021-05-02 16）。**修 `io/file.rs` 之前这 2 个连数据都读不到，
真实完备性是 140/147 = 95.2%**；上面的 96.6% 是在影子树里给它们建了标准命名
软链之后量的。文件名匹配已经放宽（见第 8 条），所以现在的二进制不需要软链。

**还要做的**：拿这 142 个当**正样本**量 `bin_size_best` 分布、`multiplet_frac`
与 `f₃`、CPD 符合分布（与目录自带的 `CPDtoGRDcountsRatio` 中位 0.040 互校）、
`PI >= 368` 占比。**GECAM-C 没有正样本、只有 2 路 CPD，至今所有关于 CPD 判据
的推断都是外推；这 142 个是第一批能反过来验判据的真值。任何判据只要切掉其中
任何一个，就不能用。**

### 11. 双星符合：共视曝光只有 6 小时，不是一条产线

先前记的是"A 与 B 同期在轨、C 在 2022–2024 与它们重叠，同一个 TGF 被两颗星
同时看到既是最硬的真伪判据也能定位"。**实测下来这条要大打折扣。**

**A∩B 的 GTI 重叠是 119.2 小时**（A 与 B 的 MET 历元相同，GTI 可直接比、
不必折 UTC；跟 C 星比才要折）。A 星 2022–2024 曝光 370.4 h，其中 310 个小时
B 星也有文件（47.9%、覆盖 124 天），真重叠 **119.2 h = 4.97 天**，占 A 星总
曝光的 32.2%。

**但同时刻不等于共视。** 从 posatt 的 `X/Y/Z_WGS84` 算星间距离（抽 40 个重叠
小时、10 s 一格、8,904 个采样点）：**星下点地面距离中位 7,861 km**
（5–95% 571–18,098 km）。按距离阈算时间占比：

| 星下点距离 | 时间占比 |
|---|---|
| < 400 km | 3.7% |
| **< 800 km**（TGF 可见半径常用值） | **5.2%** |
| < 1,600 km | 5.8% |
| < 3,000 km（掠射，衰减严重） | 24.8% |

这符合 GECAM 双星星座的设计——A 与 B 分布在轨道两侧才能全天覆盖，**代价就是
它们绝大多数时间看的不是同一片地球**。

**所以 WWLLN 覆盖内能做双星符合的曝光是 119.2 h × 5.2% ≈ 6.2 小时。** 按
GECAM-A 现在 10.5 候选/s 的率，那 6.2 小时里 A 就会出 23 万个候选，而真 TGF
的期望是零点几个量级——**先淹死在假候选里，判据做好了也没有统计量。**

**结论：把"双星符合"从近期路线里拿掉**，它不该跟"跑 183 天"并列，差两个
数量级。真要保留就改成一个**便宜的定向检验**而不是产线：两边都有候选表之后，
按 UTC 对齐，只看那 6.2 小时共视窗内有没有毫秒级同时的对。**有就是极硬的真
TGF 证据（假候选是各星独立的粒子，不可能跨星同时）；没有也在预期之内、不构成
任何否定。** 成本是一次表连接。

**两条限定：**

* **2025–2026 段的账不一样。** A 星那段有 3,838 个小时文件、按 57.3% 的 GTI
  折约 2,190 h 曝光，B 星那段基本满覆盖，所以共视可能到 **114 小时**，比
  WWLLN 段多 18 倍。**但那段没有闪电真值**（库止 2024-12-31），只能走
  "两星互为真值"而不是"闪电为真值"。双星符合真要做，该放到 post-2024。
* **800 km 这个阈没有 GECAM 自己的数据支撑**，属于未决。放宽到 3,000 km 是
  24.8%，但那个角度上大气衰减很重、探测效率极低，不建议拿它算可用曝光。
* **定位精度还没到能评估的地步**：就算共视，两星的方向重建误差圆（第 13 条
  第二档，还没做）能不能小到有用，不知道。

### 12. 代码里重复的 GTI 处理

`GtiHdu`（本 crate 与 blink_svom_grm 各一份）、`union_intervals`（blink_fermi_gbm）
做的是同一件事。现在照抄一份是为了不动正在跑全量的 crate。**要做的**：等
GECAM 稳定后把 GTI 处理提到 `blink_core` 或 `blink_algorithms`，三处合一。

### 13. 用探头数做方向：原料已存，分析未做

25 路 GRD 朝向各不相同，逐路计数的相对高低就编码了入射方向——这是 GECAM 相对
天格（4 路）和 SVOM/GRM（3 路）的第二个结构性优势，也是这次把逐路计数写进
`Signal::detectors` 的原因：**候选表落盘后事例流就不在手边，事后要补方向分析
就得重跑全量**（GECAM-C 5.8 TB、GECAM-B 110 TB），所以哪怕分析还没开工，
向量也得当场存。

**原料。** CALDB 里有现成的方向相关蒙卡响应：
`/gecamfs/soft/CALDB/data/gecam-{a,b}/grd/bcf/mc_rsp/gamma_T{θ}_P{φ}/bg{01..25}_x_*.rsp`，
401 个方向 × 25 路，每个是标准 OGIP 响应（300 入射能 × 1080 道）。目录名里的
T/P 是星体坐标系下的天顶角与方位角，六位定点（`T002306` = 23.06°）。
CPD 有同样一套。GECAM-C 的 CALDB 在同一棵树下（`log_HEBS.txt`）。

**分两档做。**

*第一档，不需要姿态，也不需要响应矩阵。* 拿闪电证实的 TGF 样本，看它们点亮
的是哪几路，和本底的逐路分布比。朝地的那几路会系统性地亮——这等于用数据
自己标出"哪边是地球"，绕开了第 5 条那个没核实的四元数约定。有了这条，
"从天上来还是从地球来"就能判，而这正是 TGF 与 GRB、TGF 与 TEB 的分界，也正是
天格上卡住的那个问题（那 51 个毫秒级软暴至今说不清是什么）。

*第二档，完整定位。* 用 `mc_rsp` 对 401 个方向逐一算期望计数向量，与实测
向量做泊松似然比，得到方向和误差圆。再结合卫星位置和 TGF 产生高度（约 15 km）
投影到地面，就能给出源的地面足迹——**那样闪电关联就从"800 km 半径内"变成
"落不落在误差圆里"，是强得多的判据**。这一档需要先解决第 5 条：把星体坐标系
和四元数约定核实清楚，否则 T/P 折不到地固系。

**要做的**（按顺序）：先攒出闪电证实样本 → 第一档的逐路分布对比 → 核实姿态
约定 → 第二档似然定位。第一档在有样本之后就能做，不依赖任何未决项。
### 14. 候选数比配置的假阳性率高四个数量级：根因已定位，判据还没定标

**现象。** GECAM-C 单日（2023-06-15）全量搜索出 **3385 个候选**，而
`false_positive_per_year = 20` 一天该有 0.055 个，差四个数量级。

> **✅ 2026-09-11：这个"四个数量级"定量闭合了——候选率 ≈ 簇率 / 0.72。**
>
> GECAM-B 在**同一批小时**上并排量（簇率取该小时安静段里"1 µs 格子中 ≥ 8 个
> 计数"的格率，`min_number = 8`；候选率 = 该小时候选数 / `searched_seconds`）：
>
> | | 中位 | 5–95% |
> |---|---|---|
> | `m ≥ 8` 簇率 | 5.327 /s | |
> | 候选率 | 7.289 /s | |
> | **比值** | **0.719** | **0.654 .. 0.783** |
>
> **秩相关 +0.915（22 个小时）。要害是：簇率与事例率无关，却与候选率强相关**
> ——事例率在 7,931 到 13,685 之间上下乱走，簇率跟着候选率走。**所以这不是
> 量级巧合，是同一件事。** 剩下的 28% 是"簇 + 邻近本底光子"与跨簇叠加。
> （口径：1 µs 格子会把 6–15% 的簇劈开，**真簇率再高约 10%，比值升到约 0.79，
> 方向一致。**）
>
> **直接量尾巴给出了 `fa` 被低估多少**（不经 Fano——Fano 是二阶矩，看不见尾巴）。
> 安静段 13,190 s，`bin = 10 µs`、λ = 0.101，数"格子里 ≥ k 个计数的格数"：
>
> | k | 合并前实测 | 合并后实测 | 泊松解析期望 | **合并前 / 泊松** |
> |---|---|---|---|---|
> | 4 | 526,191 | 4,736 | 5,270 | 99.9 |
> | 6 | 175,850 | 25 | 1.78 | 9.9e4 |
> | **8** | **80,639** | **11** | **3.2e−4** | **2.5e8** |
> | 12 | 22,182 | 4 | 2.8e−12 | 7.9e15 |
>
> **在搜索自己的阈值（k = 8）上，合并前的 `fa` 被低估 2.5 亿倍。** 之所以候选率
> 只高"四个数量级"，是因为它混了各种窗宽。
>
> **这条也说明了那个"另一类杠杆"在哪**：候选本身就是簇，**所以杠杆在事例流
> 层面（把簇并掉，即第 22 条的 τ），不是再加一条候选级判据。**

**排除掉的解释**（都是实测）：本底估计不稳——局部率与搜索本底率的中位比是
1.00，没有一个候选差到 2 倍；假阳性公式算错——`sf × trials` 与报出的 `fa`
精确吻合。

**根因：带电粒子穿过整台仪器，泊松独立性被破坏。** 把最显著那个候选的原始
光子打出来：窗长 0.15 µs、12 个计数，其中 **7 路不同探头在同一个时戳
（dt = 0.9835 µs）上各响一个**；第二显著的候选是 6 路同戳。定量：
**97.3% 的候选窗内含同戳簇**（3295/3385），参与同戳簇的事例占比中位 0.625，
最长同戳串中位 3、最大 9。CPD 在 ±10 µs 内有计数的候选占 **14.1%**，
纯本底该是 3.4%。

**离线判据（`scripts/cluster/gecam_features.py` 的 `multiplet_frac`）。**
量的是参与任何同戳簇的事例占比，不是最长的那一串——GECAM 有 12 路（A/B 25 路），
一个窗里往往有几个各自独立的小簇，最长串只有 2、除以 8 就成了 0.25，看着像
不同戳。阈值有个理论锚点：时戳量化约 0.03 µs，8 个计数落在 84 µs 窗里偶然
撞上一对的概率只有 1.0%，而偶然撞一对给出的占比正好 2/8 = 0.25，所以
0.25–0.3 是"偶然"与"结构"的分界。切完剩 **528 个/天**（`≤ 0.3`，15.60%）
或 473 个（`≤ 0.25`），恰好为 0 的只有 90 个。

> **⚠️ 2026-09-11 撤回：本节下面一整段（"有效但不充分"、判据压掉粒子
> "约 4.4 倍"、"残留粒子份额 ≤ 12% / ≤ 30%"、"显著端幸存者仍有 15–20% 是粒子"）
> 的论证依据已经不成立。** 那套论证的标签是「候选窗内 CPD 紧符合超出 ⇒ 带电
> 粒子」，而 **CPD 是塑料闪烁体、对 TGF 的伽马有真实的康普顿响应**：已发表的
> GECAM TGF 目录自带 `CPDtoGRDcountsRatio` **中位 0.040**——这是目录作者自己
> 的测量，与我们的口径无关，**说明真 TGF 在 CPD 里本来就有可观计数**。
> 所以"CPD 有超出"分不开粒子与 TGF，以它为标签得出的每一个数都失去依据。
>
> **观测事实仍然成立，失效的是解释**：四个窗长箱的 obs/exp 确实一个都不反，
> 但那不再能读成"判据在挑光子"。**方法自检那两组零假设对照（平移窗 + GTI 内
> 随机时刻，obs/exp 回到 1.00 ± 0.4%）不受牵连**——它测的是期望值算法无偏，
> 与标签对错是两回事，那部分照旧有效。
>
> **一条上升为全队规矩的教训**：任何「在否决探测器里有命中 ⇒ 是粒子」的标签，
> 必须先量出**确认的真事件**在该探测器里的响应并按它归一化；未归一化的命中率
> 一律无效。这已经是同一失效模式的第三次——GECAM 上「CPD 命中率 93%」那次
> （93% 正是那个本底率下的偶然期望）、这一次、以及 HXMT 上 ACD 判据被实测
> 证伪（对 REP 与 TGF 的响应没有差别）。
>
> **配套那条 `CPD 本底率 ≤ 500 c/s` 还有第二个毛病：阈是绝对计数率，跨星
> 不可迁移。** C 星 2 路 CPD，500 c/s 等于每路 250 c/s；B 星 8 路，照搬过去
> 等于收紧 4 倍。**要用就写成"每路 250 c/s"。**

**这条判据经过一轮正规检验：有效，但不充分。**（GECAM-C 2023-06-15，
3385 个候选；判据量是「候选窗 ±half 内有没有 CPD 计数」的**观测/期望**，
期望按当地实测 CPD 率算 `μ = r·(bin + 2·half)`、逐候选求 `Σ(1−e^−μ)`。）

*方法自检先过。* 两组零假设对照，同一套机器：把每个候选窗整体平移 ±0.2/±0.5 s
（窗长不变，13540 次），obs/exp = 1.008 / 1.004 / 0.987 / 1.005（±10 µs /
±100 µs / ±1 ms / ±10 ms），|z| ≤ 1.4；GTI 内均匀抽时刻、窗长从候选分布抽
（13540 次），1.009 / 1.002 / 1.001 / 1.003。**CPD 与 GRD 没有共同读出假象，
期望值算法无偏。**

*有效。* ±10 µs 口径的 obs/exp，幸存 / 被切：

| bin_us | 幸存 | 被切 | z |
|---|---|---|---|
| < 10 | 1.37 | 16.08 | −7.18 |
| 10–50 | 2.58 | 12.08 | −4.51 |
| 50–200 | 3.13 | 8.84 | −6.29 |
| 200–1000 | 1.83 | 3.35 | −2.95 |
| 合计 | 1.90 | 8.40 | −14.89 |

四个窗长箱**没有一个是反的**，±100 µs 同样。收紧 `fa` 时两边一起动，幸存者
1.90 → 2.37（fa ≤ 1e-3），被切者 8.40 → 21.18，**分离在扩大不是缩小**。
判据把 CPD 可见的紧符合粒子签名压掉约 **4.4 倍**。

*不充分。* 扣掉高粒子环境（CPD 本底率 > 500 c/s，占幸存者 14%，全样本只有
4.1%）之后，幸存者的 ±10 µs 超出反而升到 **2.92**；再按 fa 收紧，`fa ≤ 0.01`
的 18 个低本底幸存者里 3 个在 ±10 µs 内有 CPD 计数、期望 0.23，
P(≥3|0.23) ≈ 0.2%。样本只有 3 个事例，但方向明确：**显著端的幸存者仍有约
15–20% 是粒子。** 残留份额上限 ≤ 12%（±10 µs 口径）/ ≤ 30%（±100 µs）。
**这条判据有效但不能单独用。**

**「残留 ≤ 12%」这个数带一个大前提：它假定 CPD 的粒子标记效率就是实测的那
14.1%。** GECAM-C 只有 2 路 CPD 对 12 路 GRD，也就是说**约 86% 的粒子 CPD
根本认不出来**。所以 CPD 命中率这个指标**只在相对比较里可信（幸存者 vs
被切者、这个尺度 vs 那个尺度），绝对值不可信**，任何由它推出的"污染份额是
多少"都只是下界。等 GECAM-B 的 8 路 CPD 对 25 路上来（覆盖比约翻倍）才谈得上
定量。

**另一条口径纪律**：本节里大量结论是几十到几百个样本的比例（`0/405`、
`53/53`、`3/18`），**这些数都该带区间**——`0/405` 的 95% 置信上限是 0.74%
而不是 0。天格那个"阈 0.35、真暴最大 0.333、余量 0.017"就是余量看着有、
其实落在样本涨落之内的例子。

*拆成跨探头同戳救不回来。* `xdet_frac`（只算跨探头同戳）在同样阈值下幸存者
多一倍（923 vs 528）而 obs/exp 反而更高（2.00 vs 1.90）；即使要求窗内一对
跨探头同戳都没有（293 个），残留还有 1.80。**残留不是定义粗造成的，
`multiplet_frac` 的定义不用改。**

*但它对「亮而长的粒子暴」系统性失效，这是个真的失效模式。* `multiplet_frac`
是个占比，分母是窗内计数，所以会被计数稀释：2023-06-15T14:08:35–14:08:59
那 0.63 s 里搜出 22 个重叠候选，个个 n ≈ 68、窗长约 980 µs（顶到 1 ms 上限）、
**12 路探头全亮**、CPD 命中率 **100%**、`pi_ratio` **1.03**（谱与本底完全重合），
`mf ≤ 0.3` 全部放行；同日 22:51:16 还有一个同型的。粒子暴越亮越长，越容易
从这条判据下漏过去，而那正是显著端。

*配套的第二道判据（新）*：**CPD 本底环境率 ≤ 500 c/s**。±10 ms 口径的那个
反常（幸存者 1.45）**100% 来自高本底环境的候选**，扣掉后降到 1.02，一点不剩。

*谱比的问题还在，而且判据帮倒忙。* 幸存者 `pi_ratio` 中位 1.25、被切者 1.38、
全样本 1.36——**切完之后样本反而变软了**，与 TGF 该有的方向相反（天格短硬暴
是 3.0）。这条没有解释，只能等闪电关联来判。

**两条已经证伪的路子，记下来免得重走：**

1. **把同戳数按偶然撞车期望归一化**（`期望对数 = n(n−1)/2·q/W`）当判据——
   坏统计量。窗长到亚微秒时槽位数 `W/q` 少于事例数，生日问题饱和，比值把
   选择整个倒过来：它优先留下「0.1 µs 窗内 8–12 个计数、点亮 7–11 路探头」
   这种最典型的粒子穿越。亚微秒窗里撞车本来就该发生，按偶然期望归一化在
   那一档没有分辨力。
2. **最小窗长判据**——**在 GECAM-C 上**白搭。亚微秒候选（365 个，10.8%）的
   `mf` 中位是 0.875，早被 `mf ≤ 0.3` 切光了，再加 `bin ≥ 10 µs` 只从 528 减到
   520。

   > **⚠️ 适用范围（2026-09-11）：这条只对 GECAM-C 成立，不是通则，见第 30 条。**
   > **B 星上窗长是目前最强的单一判别量**，根因是两星池结构差一个数量级——
   > C 星亚微秒候选只占 **10.8%**，B 星占 **75.9%**（25 路、能阈前约 22 kc/s，
   > 亚微秒同戳堆积比 C 星多一个数量级）。A 星候选 `bin_us` 中位 0.15 µs、
   > 与 B 星池的 0.134 µs 几乎一样，所以 A 星也属于 B 那一类。
   >
   > **但"不要动 `min_duration`"这个操作结论在三颗星上都仍然成立，理由变了**：
   > A 星实测把 `min_duration` 设成 10 µs **不能减少候选数**——9 计数、30 ns 的
   > 簇放进 10 µs 窗里 `count` 仍是 9、`mean` 只有 0.19，`fa` 远小于 20，
   > **照样触发，只是换一个 `bin_size_best` 报出来**。窗长只能当**离线判据**。
   >
   > **⚠️ 这两件事此前被混为一谈，在这里写清楚：**
   >
   > | | 抬 `min_duration`（搜索参数） | 事后按 `bin_size_best` 过滤（离线判据） |
   > |---|---|---|
   > | 改的是 | 搜索能试哪些格 | 候选表的一列 |
   > | 对亚微秒簇 | **拦不住**，改报一个更长的 `bin_size_best` | 直接切掉 |
   > | 真 TGF 代价 | 未知（连它拦不拦得住都没有） | 实测留 82.1% |
   > | 定标来源 | 无 | 147 个已发表 TGF |
   > | 结论 | **不要动，维持 `min_duration = 0`** | **要用** |
   >
   > **"窗长判据在 C 星上白搭"与"`bin_size_best` 是最强的单一判别量"也不矛盾。**
   > 前者说的是**在 `mf ≤ 0.3` 之后**再叠窗长，C 星只从 528 减到 520——因为
   > C 星的亚微秒候选只占 10.8%，`mf` 已经把它们切光了，窗长没有剩下的活干。
   > 后者说的是**单独用**窗长，在亚微秒占 75.9% 的 B 星池（和同类的 A 星池）上
   > 它一个人就切掉 98.0%。**两句话量的是不同的东西：一个是增量，一个是总量。**
   > 而 `mf ≤ 0.3` 本身已经出局（第 27 条），所以现在能站住的只剩后者。
   >
   > **正确的做法（三颗星一致）**：保持 `min_duration = 0` 不动，让搜索照常报
   > `bin_size_best`，**在候选表上按报出来的值过滤**，阈值与代价见第 30 条。

**同戳类判据原理上抓不到「亮而长的粒子暴」，换分母也救不了。** 把 9 个
亮暴（去重后 `count ≥ 30`）逐条摊开：8 个的**同戳簇最大探头数是 1、跨探头
同戳占比 0.000**——窗内一对跨探头同戳都没有，它们那点 `mf`（0.03–0.15）
全部来自同探头的双增益重复（见第 17 条），不是粒子签名。物理上说得通：
单个粒子穿整台仪器才会在同一时戳点亮多路，而**簇射/粒子暴是几十个粒子在
1 ms 里陆续到达，各有各的时戳，本来就不该同戳**。`max_d ≤ 2` 对 45 个
`count ≥ 30` 的候选放行 45/45，`max_d ≤ 1` 放行 37/45。**能抓住它们的只有
CPD**：9 个全部 `cpd10 ≥ 1`，CPD 本底率中位 2557 c/s，谱比中位 1.07。

`mf ≤ 0.3` 的通过率随窗内计数单调上升，正是这个稀释：

| 窗内计数 | n | 通过率 | 窗长中位 | 点亮 | 谱比 |
|---|---|---|---|---|---|
| 8–10 | 2444 | 9.2% | 66.7 µs | 6 路 | 1.39 |
| 10–15 | 550 | 30.0% | 209.3 µs | 7 路 | 1.31 |
| 15–30 | 111 | 53.2% | 711.6 µs | 8 路 | 1.29 |
| ≥ 60 | 5 | 100% | 978.6 µs | 12 路 | 1.12 |

**一条方法上的纪律：一次持续的粒子增强会被搜索抓成几十次独立短暴，做统计
之前要按人群摘掉。** 2023-06-15T14:08:35–14:08:59 那 0.63 s 里就有 22 个候选，
同日 22:51 还有 10 个。**注意这不是管线漏并**——共用的 `snapshot_stepping`
里 `Candidate::mergeable` 早就把严格重叠的候选并掉了（HXMT v6 实测重叠对数
为 0），那 22 个是窗长短、彼此不重叠，所以是**人群问题不是归并问题**。

**不要按 episode（时间聚簇）归并。** HXMT 实测残留的 61 个多成员 episode
跨度中位 11 ms、分散在几十个日期，154 个成员里有 17 个带闪电关联——
**TGF 本身就有毫秒级多脉冲结构，按 episode 并会把物理抹掉**。该用的是
`blink_wwlln` 里那条列车密度判据 `neighbors_10min`：数每个候选 ±600 s 内
**全量初始候选池**（含亚阈）里有多少邻居，超阈就摘。HXMT 实测认证 REP
切 98.8%、认证 TGF 只误切 0.7%；`is_train=1` 的候选按 1 s 归并压缩 86.1%
而 `is_train=0` 的只压缩 0.98%，**亚秒堆积和分钟级列车是同一批物理事件**。
**但这条判据在 GECAM-C 上实测双向失效，不要照搬。** 一天 3385 个候选的
±600 s 邻居数：中位 53、四分位 45–63、5–95% 31–75、最大 97（均匀分布下
的期望是池率 × 1200 s = 47.0）。绝对阈 34 **坐在分布的第 8 百分位**，
摘掉 92.2%、把 `fa ≤ 0.01` 的干净幸存者 15 个全摘光；按池中位缩放的
`max(34, round(3.78 × 53)) = 200` **高过分布最大值 97，一个都摘不掉**，
等于关掉判据。

**更要命的是它对检验样本零分辨力**：那两串粒子增强（14:08 的 36 个散在
0.63 s、22:51 的 17 个）的 `neighbors_10min` 分别是 61 和 85，**就是池中位
的水平**——一次 0.63 秒的增强在 ±600 s 的计数窗里根本看不见。按邻居数
分五档之后，真伪相关的量全平（`mf` 通过率 15.6/13.6/15.7/13.2/21.1%、
CPD 命中率 12.6/14.0/15.4/13.0/14.7%、谱比 1.35–1.37），**唯一单调的是
地理纬度**（|lat| 中位 70.7° → 40.3° → 33.3° → 21.0° → 14.8°）。
**在 GECAM-C 上 `neighbors_10min` 携带的是轨道位置信息，不是真伪信息。**

**根因是尺度不匹配，正确的做法是按人群扫尺度。** GECAM-C 要的是**亚秒**
尺度（0.63 s 里 36 个候选），天格要的是**几十秒**——天格把窗从 ±600 s 换成
±30 s 之后，17 个高磁纬软暴的同类尖峰数中位 **182**（范围 10–508，17/17
全有）而 29 个短硬暴（含 7 个闪电证实）只有 **4**，泊松期望约 2 个/分钟，
超出 90 倍，据此定性为磁层沉降电子微暴。两台仪器独立指向同一件事。
**要做的**：±0.5 / 5 / 30 / 60 / 600 s 各扫一次，把整条分离度曲线报出来，
别只报某一个尺度下的结论——天格早先在 ±600 s 上量到「软暴不成串」，
换到 ±30 s 结论完全相反。

（先前据 22 个未摘的候选误得出「fa ≤ 0.01 的幸存者 CPD 命中 9 个、期望 0.6，
15 倍超出」；摘掉之后是 5 个、正规期望 2.47，2.0 倍。**期望值差 5 倍是因为
用全局 330 c/s 代替了当地实测率**——那 21 个里有几个在 2500–3400 c/s 的
环境里。全局率不能代替当地率。）

**另一条已经证伪的机制假说**：「擦边入射的粒子只穿一两路探头，表现为单路
大沉积 + 余辉/堆积，跨探头同戳为零因而漏过判据」。否证：幸存者的
`det_frac_max` 中位在**每一个 fa 档上都是 0.250**，p90 ≤ 0.404，`fa ≤ 0.01`
的幸存者里没有一个 ≥ 0.6；被切侧中位同样是 0.250。单路集中这个形态在
幸存者里根本不存在。**顺带**：这也说明 `MAX_DETECTOR_FRACTION = 0.8` 那条
单路毛刺判据在 GECAM-C 上等于空转——全样本 `det_frac_max` 的 p90 才 0.375，
根本没有东西给它拦。别以为它在干活。

**要做的**：

* **闪电反标定**：拿 WWLLN 证实的候选看它们在 `multiplet_frac` 上怎么分布。
  这是外部真值，比任何内部一致性都硬。正确问法是「证实样本的分布长什么样」，
  不是「阈值切完还剩几个关联」——若证实样本的分布与全样本无差别，说明同戳量
  根本不携带真伪信息，那也是结论。
* **验收线（`main` 定的）**：GECAM-B 在 274.5 天里 147 个已发表 TGF =
  **0.54 个/天**，而且是在轨道倾角 29°、只覆盖 TGF 主产区的条件下。
  C 星 12 路 GRD（B 的一半面积）、太阳同步轨道，量级只会更低。
  **在候选率降到每天几个之前，任何判据组合都不算收敛。** 现在切完剩
  377 个/天，高出 2–3 个数量级。
* **零成本的第一个实验**：已发表判据是 P < 2e-21，折成我们的口径约是
  `fa ≤ 7e-7 /年`（我们的 `fa ≤ 20` 对应 sf ≈ 5.5e-14，两者差约 3e7 倍）。
  拿已落盘的候选表切一刀就行，不用重跑。**两种结局都有信息**：剩下个位数
  且 CPD 干净，说明超出主要是阈值口径差异；剩下个位数但全带 CPD 标记，
  说明粒子污染一路顶到最显著端、判据链仍然没有分辨力。所以**不要只报剩
  几个，同时报剩下那几个的 CPD 标记率和同戳占比**。
* **分组符合（`coincidence = 2`）已评估，暂不做**：带电粒子穿仪器恰恰是
  同时点亮多路（C 星最显著候选 7 路同戳、A 星 `n_det_hit` 中位 7），
  任何分组下都会同时越两组的线，**对我们的主要污染源结构性无效**；它拦的
  是「计数挤在一路」的毛刺，而那种形态在 A 和 C 上都不存在。另外已发表
  判据里的「GRD 组」是按指向的物理分组，我们不知道 25 路的真实分组。
* **CPD 判据的定标主战场移到 GECAM-B**：8 路 CPD 对 25 路 GRD，而 C 星只有
  2 路对 12 路，14.1% 的命中率天生偏低、分辨力弱，在 C 星上调阈值是在跟统计
  噪声较劲。B 星定出来的判据再回头套 C 星。

**在闪电反标定给出答案之前，同戳量只是离线分析量，Rust 搜索侧一个字不动。**

**一个还没评估的替代方案**（比候选级否决干净，但改的是搜索语义，要先获批）：
不在候选级否决，而在**事例流层面把同戳的跨探头计数并成一个事例**再交给搜索。
一次粒子穿越在泊松意义上本来就是一个事件，并了之后独立性从源头恢复，本底
估计也跟着一致地修正；而 TGF 的光子彼此独立，落进同一个 0.03 µs 格子的很少，
代价很小。这条没有实测支撑，只是设计上的判断。

### 15. GECAM-A 的标定库停在 2022-01-14 之前的生成器（加载器已放行）

标定库 readme 原话：`v0.9 pi：最后50道超高不再从read_highlow_threshold_kev
读取并计算，而是直接填0`；`2022-01-14 软件重大更新。之前50道超高都赋值0，
现在下阈改为4096道对应的能量；上阈改为20MeV`。GECAM-B/C 跟进了新版，
**GECAM-A 的 `pi/` 只有 2023-01-10 那一版，一直在用旧生成器**，所以 A 星
全部 4,609 小时的 ch448 `E_MAX` 都是 0.0 而不是 20000——实测 9 个 epoch
横跨 2022-10 到 2026-06、24 个文件无一例外。

原来那条"`E_MAX` 必须是 20000"的溢出道校验因此把 A 星整颗星拒之门外
（试跑 2024-01-11：3 小时 `corrupt_data` + 21 小时 `missing_data`，
搜索小时数为零）。现在两个哨兵值都放行；道 0..447 的连续性、头尾、
40 keV 落在 ch54 这几条一条没松，A 星与 B 星那把梯子逐道完全一致、全程都过。

**还没做的**：A 星的 `c2e`（能量刻度）只有 9 个 epoch——1971/01/01 那份默认，
加 2025/01..04 和 2026/03..06。**2022-10 .. 2024-12 完全没有在轨 EC**，
而这段正是 A 星唯一落在 WWLLN 覆盖内的部分；2025-05 .. 2026-02 也是空的。
对照 GECAM-B 的 `c2e` 有 67 个 epoch、2020-12 发射后立刻起一路密集到 2026-06。
判据只用道号（`54 <= PI < 448`），所以这是能阈口径的系统误差而不是正确性
问题，但幅度要量：拿 LaBr₃ 的 ¹³⁸La 内禀本底线（约 1470 keV，落在 ch300
附近）分增益档、逐 25 路量峰位，看 2022-2024 段相对 2025-06 有 EC 的段移了
多少、往哪边移，折成 keV 报 ch54 实际落在哪；同时比两段的 `PI >= 448` 占比。
`mc_rsp`（401 方向 × 25 路）倒是齐全的，所以第 13 条的定位那一档 A 星不缺原料。

**能标偏差已经实测（¹³⁸La 1470 keV 内禀本底线，2026-09-10）。** 分增益档、
逐 25 路、7 个 epoch。1470 keV 在梯子上落 **ch303**（1467.7–1487.3 keV）；
高增益档 7 个 epoch 全部量不到峰（1470 keV 已出量程），以下全是低增益档。

| 日期 | 在轨 EC | 峰位中位 (ch) | 名义能量 | 逐路极差 | MAD |
|---|---|---|---|---|---|
| 2022-10-21 | 无 | 286.24 | 1175.0 keV | 28.2 ch | 2.23 |
| 2023-06-16 | 无 | 291.47 | 1259.6 | 53.1 | 3.74 |
| 2024-01-11 | 无 | 289.49 | 1226.9 | 57.8 | 4.40 |
| 2024-09-06 | 无 | 290.94 | 1250.7 | 54.7 | 3.74 |
| 2024-12-31 | 无 | 289.39 | 1225.2 | 39.4 | 3.10 |
| 2025-06-15 | 有 | 296.72 | 1350.3 | 30.9 | 3.31 |
| 2026-06-01 | 有 | **298.00** | 1373.4 | **14.4** | 2.74 |

**方向是安全的那一侧。** 峰位偏低 = PI 被系统性低估 = 真实 1470 keV 的东西
被算到名义 1225 keV 的道上，所以 `PI >= 54` 实际 admit 的是**比 40 keV 更高**
的事例，**少收信号而不是多收本底**。绝对值不能信（即使有 EC 的 2026 那天
也比 ch303 低 5 道，那是重心法在陡康普顿台阶上的固有偏置），所以只用相对
有 EC 段的差，每道 1.34%：2024-12-31 **+12.1%**、2024-01-11 **+12.0%**、
2022-10-21 **+16.9%**、2023-06-16 / 2024-09-06 **+9.1% / +9.9%**。
**2022–2024 段 PI 低估 9–17%、中位约 12%，`ch54` 名义 39.1–40.2 keV，
实际能阈落在 44–47 keV。**

**溢出段占比与标定无关**，排除了「增益偏高把 MeV 事例提前推进溢出段」：
`PI >= 448` 逐 epoch 是 0.56 / 14.12 / 13.80 / 11.06 / 7.07 / 4.78 / 19.38%，
没有随标定的单调趋势，**有 EC 的 2026-06-01 反而最高**，所以它是轨道/本底
环境驱动的（C 星实测 15.11% 在同一量级）。

**在轨 EC 修的主要是离群探头，不是整体。** MAD 各 epoch 都在 2.2–4.4 ch，
核心那批探头本来就挤得很紧；真正改善的是极差，无 EC 段 28–58 ch（能量比
1.5–2.2 倍），2026 有 EC 后收到 14.4 ch（1.21 倍）。具体是 **d15 和 d25 在
所有无 EC 的 epoch 里都系统性偏高**（322 / 322 / 319 / 314，大队在 285–295），
2026 有 EC 后被拉回队里（293 / 297）；另有 d07 / d08 / d11 低增益档事例太少
量不出峰。

**后果**：2022–2024 段不只是整体能阈偏到 44–47 keV，**逐路的有效能阈还差到
2 倍**。做第 13 条第一档（逐路计数定方向）时这会直接伪装成方向信号——
**d15/d25 会系统性少收事例，看起来就像「那两个方向一直是暗的」**。第一档
若要在 2022–2024 段上做，必须先按这张表把逐路能阈拉平，否则出来的
「哪边是地球」是标定伪影。

**GECAM-A 的候选形态（2024-01-11，新二进制新口径，对账 74,012/74,012 =
100.00%）**：`mf` 中位 **0.800**，`mf ≤ 0.3` 只剩 **3.92%**（C 星旧口径
是 15.60%，两者口径不同、不能直接比）；`mf == 0` 占 0.69%。候选率
**10.498 /s**（曝光 7,050 s、候选 74,012），**是 GECAM-C 的 143 倍**。
`bin_us` 中位 **0.15 µs**（约 5 个量化步长），而幸存者中位 37.10 µs——
**窗长混杂在 A 星是 247 倍**（C 星只有 2 倍），任何按 `mf` 分组的比较
不做窗长分层就是废的。`det_frac_max` 中位 0.133、**`> 0.8` 的一个都没有**。

**收紧 `fa` 救不了 A 星，反而更糟**：阈从 `fa ≤ 1` 收到 `1e-8`（8 个数量级）
候选只掉 16%（67,506 → 56,448），而幸存者里 `mf ≤ 0.3` 的比例从 2.07% 掉到
**0.24%**——泊松显著性最高的候选恰恰是粒子簇（一堆事例挤在 0.12 µs 里，
显著性拉满）。**`fa` 与 `multiplet_frac` 反相关，`fa` 不是 A 星可用的把手。**

**双增益重复率必须带分母（2026-09-11）**：A 星逐小时 8.42–11.76% 是占
**原始**事例，占**准入后**计数是 **25.1–29.0%**（实测三小时 25.12 / 29.02 /
26.02%），两个数差 2.3 倍，别混。B 星同口径实测：死时间内一高一低的对占原始
事例 **26.590%**，两条都过准入、被数两遍的计数占准入后事例 **24.75%**——
**是 C 星（3.25%）的 7.6 倍**，这解释了 B 星候选率为什么比 C 星高两个数量级。

**双增益修正在 A 星砍掉 42.7% 的候选**（2026-06-01 同一天同一份数据：
112,415 → 64,380，`signals.json` 172 MB → 98 MB），`merged_gain_duplicates`
逐小时 2.71–5.94%（2026 段）/ 8.42–11.76%（2024 段）——**无在轨 EC 的段更
严重**，原因未知。修完 A/C 仍是 143 倍，**双增益不是主因**。

**183 天的体量**：外推 **1,400 万候选 / 19.5 GB JSON**（`mf ≤ 0.3` 否决后
约 55 万）。**在候选率降下来之前不铺。**

**一条修正**：2025-05 .. 2026-02 **不是真空缺**。2025-06-15 实测峰位 296.72，
与 2026-06-01 的 298.00 是一档、远离无 EC 的 289 那档，说明 CALDB 的 EC 是
**向后顺延**的（2025/04 那份一直用到 2026/03）。**真正没有在轨 EC 可用的只有
2022-10 .. 2024-12。**

### 16. GECAM-A 在 2025 年之前没有 CPD 事例产品

**加载器已经改成 CPD 缺失非致命（2026-09-10）**：原来 `from_epoch` 把
「找不到 CPD 文件」当致命错误，等于把 A 星唯一落在 WWLLN 覆盖内的那段
曝光整个扔掉——实测 2024-01-11 三个有 GRD 的小时全报 `os error 2`、
搜索小时数为零，而 2026-06-01（有 CPD）6 小时全部成功。现在降级为
「这一小时没有 CPD 数据」，GRD 照常搜，**`Signal::acd` 留 `None` 而不是
一组零**，标记是诊断量 `n_cpd_detectors = 0`。只放过找不到文件；文件在
却读坏了仍旧报错。

不是缺几天，是 `CPD_evt` 目录根本不存在：2023-06-16 / 2024-01-11 / 2024-09-06 /
2024-12-31 / **2025-01-17** 的 `GECAM_A/` 下都只有
`GRD_CPD_simevt GRD_bspec GRD_btime GRD_evt aux posatt`；2026-06-01 才多出
`CPD_btime CPD_evt`。**起点实测在 2025-03-12**（此前一小时都没有）。这与第 15 条自洽——A 星 CPD 的 `c2e`
只有 1971/01/01 一个 epoch，因为 2026 年之前没有 CPD 数据可标。

**后果**：A 星在 WWLLN 覆盖内的那 647 小时，`Signal::acd` 全零，**CPD 粒子
判别这条腿是空的**。第 2 条的 CPD 判据定标只能靠 C 星和 B 星。A 星 25 路
GRD 的同戳簇集不受影响，那条腿是好的。

**A 星 2022-2024 的实测清单**（去重版本、只认 `_v\d+\.fits`，`*_quality.json`
不计）：**183 天 / 647 小时**，不是先前二手的 198 天 / 771 小时。逐月见
`evidence/a/`。全是零散小时，没有一天满 24 小时，每天 1–11 小时、中位 3；
2024-02..06 长期每天固定 3 小时，像排定的观测计划而不是连续巡天。647 是
**文件小时**不是曝光。**实测（2026-09-11）647 个文件小时的 GTI 之和是
1,333,596 s = 370.4 小时**，GTI/文件跨度 96.05%、名义小时→实际曝光 57.3%——
先前写的「可能只有 230 小时量级」是下界，偏低。posatt 全覆盖，183 天没有一天缺。

### 17. 双增益重复计数（已修，2026-09-10）

**同一个物理光子会被 ADC 的两个增益支路各写一行。** 实测 GECAM-C
2023-06-15 12h 全 12 路 GRD：

* 同探头同时戳的事例对占原始事例 **6.635%**（EVENTS01 单路 7.855%）
* 这些对 **100%** 是 `GAIN_TYPE` 一高一低，PI 相同的占 **0%**
  （高增益那条中位 448 = 溢出道，低增益那条中位 377）
* `FLAG` 组合 **100%** 是 (0,10) 或 (10,0)
* `EVT_PAIR` 是 (N,2) 的布尔列，同戳对两端 **100%** 为真，
  非同戳事例只有 5.0% 为真

多数时候高增益那条已经饱和进溢出道、被 `Event::keep` 的 `PI < 448` 挡掉，
一个物理事例只剩一条（占 98.8%）。**但剩下的 1.2% 两条都过了准入**，于是
约 **3.25%** 的准入后计数是重复计数——而候选窗内这个比例是 **6.82%**，
富集 2.1 倍，**50.4% 的候选窗里至少含一对重复**。两条记录时戳完全相同，
泊松独立性直接破掉，跟带电粒子跨探头同时点亮是同一类机制，只是发生在
一路探头内部。

> **⚠️ 2026-09-11：下面那个"约 28%"是反标定的粗估，低估了 2.5 倍。真跑出来是
> 砍掉 69.4%。** GECAM-C 12 天逐天新旧对比（旧 = 无去重的二进制，新 =
> `blink_main` 87c191c 的死时间去重）：**43,529 → 13,332，剩 30.6%，逐天散布
> 只有 29.5–32.4%。** 30 天外推 105,978 → 约 32,400，每天约 1,160 个（旧 3,530）。
> **这直接改写"要多少天才够 3σ"的算式**：偶然本底正比于池子，按 17 天那条标定
> （61,558 个候选 → 偶然 12.76）折，32,400 个的偶然本底约 6.7。
>
> **教训**：拿 (count, mean, fa) 反标定试验数 T 再重算 fa 的粗估，在这里错了
> 2.5 倍。**有条件真跑就别估。**

粗估影响（把每个候选的 `count` 扣掉同探头同戳的多余事例、本底率同步降
3.25%，用 (count, mean, fa) 反标定试验数 T ≈ 9.6e9 后重算 `fa`；
**这是近似，T 的四分位有 ±20% 散布**）：

| `fa` 阈 | 修前 | 修后 |
|---|---|---|
| ≤ 20 | 3385 | 2435（剩 72%） |
| ≤ 1 | 1707 | 1337（剩 78%） |
| ≤ 0.01 | 896 | 732（剩 82%） |

**约 28% 的候选是双增益重复计数造出来的。**

> **⚠️ 2026-09-11：下面这个「同探头同时戳」的口径漏并了一成多，见第 32 条。**
> 两条支路之间有**一个约 100 ns 的固定延迟**，所以同一个光子的两条记录时戳
> **并不相同**。`e34f560` 的 `dedupe_gain_pairs` 按同戳并，实测只抓到全部对的
> **85.8–88.4%**（B 星 5 个小时、跨三年）；漏掉的 11.6–14.2% 全落在这约 100 ns
> 上——**正好是候选窗的尺度，而且因为时戳不同，同戳类判据完全看不见**。
> 折算到准入后计数是漏并约 2.9%。
>
> **后果：用 `e34f560` 产物算出来的任何同戳量（`multiplet_frac`、`f₂`、`f₃`）
> 都系统性虚高，不能与新口径（`3c017a2` 的「同探头、死时间内、一高一低」）
> 混用。** 下面那段「判据用同探头同时戳这个实测事实」按新口径已作废。

**修法**：`search` 里在 `events.sort()` 之后加一道 `dedupe_gain_pairs`——
同一路探头、同一时戳的两条并成一条，**保留低增益那条**（高增益支路会饱和，
低增益那条永远是有效测量）。判据用「同探头同时戳」这个实测事实，不用
`EVT_PAIR` 那一列：实测 `EVT_PAIR` 为真的事例占 19.9% 而同戳对只占 7.9%，
两者对不上（伴侣可能被上游过滤掉、或差一个时戳格），语义没核实清楚之前
不拿它当判据。并掉的条数记在 `merged_gain_duplicates` 诊断量里，不静默。

**还没做的**：

* **两档的 PI 不是同一把尺子（已答，2026-09-11）。** 只取高增益条没饱和的
  那些对：PI 完全相同只占 **4.49%**（B 星 1152 万对）/ **3.06%**（A 星），
  高−低中位 **−8 道**、5–95% −31 .. +3，**两星独立测量、结论一致**。
  两档 PI 差还随能量发散（ch100–200 −13 道、ch200–300 −64 道）。
  **后果：`MIN_CHANNEL = 54` 在两档里是两个能量，去重保留低增益那条改变的
  不只是计数、还有能阈。** 顺带一条 B 星实测：高增益支路 `PI >= 448` 占
  **58.09%**（A 星是 0.00%），所以「保留低增益」在 B 星更是必须的。
* **这个修正之前跑出来的所有 GECAM 结果都要重跑**，包括 GECAM-C 的 30 天
  和已经落盘的特征表。
* GECAM-A / GECAM-B 上的重复率没有量过，只在 C 星实测。A/B 是 25 路 GRD，
  比例未必一样。

### 18. 时戳量化不是仪器参数，是 float64 的 ulp（已答，2026-09-10）

先前记的「时戳量化约 0.03 µs」错了，而正确答案不是换一个常数就完事。

实测 GECAM-C 2023-06-15：最小正间隔 **1.49012e−8 s，正好 = 2⁻²⁶ s**，把
相对时刻除以 2⁻²⁶ 余数中位 0、p99 0、最大 0（完全整除）。而 **2022-08-15
的最小正间隔是 7.4505806e−9 s = 2⁻²⁷ s，正好是一半**。TIME 列是 `D`
（float64）秒、TSTART = 7.737e7 s，**`np.spacing(TSTART)` 与实测值比值
1.0000**——所谓「量化」是归档把时刻存成 float64 秒造成的，**跨过 2ⁿ 秒就
粗一倍**：

| | q 变粗的日期 | 之前 | 之后 |
|---|---|---|---|
| GECAM-C（历元 2021-01-01） | 2023-02-16T17:21:04（MET = 2²⁶ s） | 7.45 ns | 14.9 ns |
| GECAM-A / B（历元 2019-01-01） | 2023-04-03T10:42:08（MET = 2²⁷ s） | 14.9 ns | 29.8 ns |

**这张表原来漏了 B 星最早的一段（2026-09-11 补）。** 先前只写两段，是因为
A 星的数据几乎都在 2025 年以后；但 **B 星 2020-12-10 就开始出事例产品，而
MET 跨过 2²⁶ 是在 2021-02-15T17:21:04**，所以 B 星有三段：

| GECAM-B（历元 2019-01-01） | q |
|---|---|
| 2020-12-10 .. 2021-02-15T17:21:04 | **7.45 ns**（2⁻²⁷） |
| .. 2023-04-03T10:42:08 | 14.9 ns（2⁻²⁶） |
| 之后 | 29.8 ns（2⁻²⁵） |

分界按 `MET = 2ⁿ` 精确算出来，不是逐日抽样估的——原表里的 2023-04-01（A/B）
与 2023-02-19（C）都比精确值早两三天，那是抽样分辨率造成的。**147 个已发表
TGF 里有 4 个（3 个小时）落在 B 星 q = 7.45 ns 这一段**，占比小但不为零，
所以任何按 q 归一化的量都要**逐候选取 `np.spacing(t0)`，不能按星按年查一个
常数**。

**这条分段带来一个能当尺子用的副产品，见第 32 条**：同一颗星、同一套电子学
换三个 q，双增益支路延迟量出三对不同的"表观值"（96.9/104.3、104.3/89.4、
89.4/119.2 ns），而底下只有**一个约 100 ns 的固定延迟**。**所以先前把 A 星的
89 ns 与 119 ns 读成"两条线"是错的**，那是同一个数在 29.8 ns 格子上的投影。

**A 星的数据绝大多数在 2025 年以后，q = 29.8 ns，是 C 星的两倍粗**；
任何按 q 归一化的判据都必须**按卫星、按日期取 q**，而且在任务中途会跳一次。
仪器真实时间分辨率 ≤ 7.45 ns，被归档格式抹掉了、拿不回来——所以
「GECAM 的时间分辨率是 15 ns」这种说法在对外材料里是错的，要改口径。

**天格不是这么回事，别照搬。** 天格历元 2018-01-01、数据在 2022–2024，
MET ≈ 1.3–2.1e8 s，float64 在这个量级的 ulp 是 2⁻²⁵–2⁻²⁶ s（15–30 ns），
而天格实测的量化步是 **2⁻²² s = 238 ns，粗 8–16 倍**，所以那个多半是真的
仪器/编码量化。

顺带修正第 14 条那个锚点：8 个计数、84 µs 窗、q = 14.9 ns 时偶然撞一对的
期望是 **0.50%**（先前写的 1.0% 用的是 0.03 µs）；三路同戳的期望是 1.8e−6，
n = 70 / 1 ms 时也只有 1.2e−5。**但「窗内出现三路同戳就是粒子、零参数零阈值」
这句话已被证伪，见第 14 条末尾新增的那一段**——它只说明本底不会误触发，没说
真 TGF 不会。「撞一对给出的占比正好 2/8 = 0.25」这一条不受 q 影响。

### 19. 满量程道与堆积包：三颗星三种形态（准入口径，未修）

> **2026-09-11 汇总：下面这一整节是 GECAM-A 的实测，不是通则。** 三颗星实测下来
> 形态完全不同，A 星那套宽包口径不能搬：
>
> | | 满量程道 `edge` | `edge` 处的形态 |
> |---|---|---|
> | **A 星 2022–2024**（无在轨 EC） | 低增益 379（375–381） | **ch368–383 宽堆积包，抬升 12.8–22 倍** |
> | **A 星 2025+**（有 EC） | 低增益 447（梯顶） | 包移到 ch420–447 |
> | **B 星 2021 段** | 低增益 382（375–392） | **没有包**，包/平台 = 0.39×，谱单调降到悬崖 |
> | **B 星 2022 起** | 低增益 447（梯顶） | **ch447 单道尖峰**，2024-01-11 实测比邻道高 **117 倍** |
> | **C 星全程** | 高增益 189–196、低增益 406–410 | 高增益**无包**；低增益包抬升仅 1.2–2.0 倍；**逐过境不漂**（同日三个过境差 ≤ 2 道） |
>
> **所以 `PI >= 368` 这个量跨星量的不是同一个东西**，B 星要同时记 `n_pi368` 与
> `n_pi447`。C 星与 A 星在一点上一致：`EVT_TYPE == 1` 里 `PI >= 448` 逐路逐档
> **0.000%**，**448 这个上界三颗星都是空转的**。
>
> **`edge` 有第二条独立路子：EBOUNDS 溢出段（448 以上）每一行的 `E_MIN` 就是
> 该 (探头, 增益) 支路的满量程能量。** C 星 22 行 = 12 路高增益 + 10 路低增益
> （det06/det12 没有低增益支路），与从直方图求的 `edge` 逐路吻合。B 星 50 行
> （25 路 × 2 档）同样对上（查表值系统性比直方图高十几到三十道，方向是对的——
> 查表给满量程本身，直方图的 `edge` 是"最后一个计数 ≥ 10 的道"，天然低几道）。
> **但这条捷径有两个已知的空洞**：(a) **GECAM-A 整颗星用不了**，ch448–497 这
> 50 行的 `E_MIN`/`E_MAX` 全是 0.00（第 15 条那个旧生成器）；(b) **B 星 138 个
> TGF 小时里有 25 个（18%）同样全是 0.0**。所以做准入上界必须有回退路径
> （回退到逐过境求直方图）。另外 **896 道纪元里哨兵行根本不在表里**（第 21 条），
> 那一段没有输入。
>
> **在 A 星上这个准入上界是率的把手，不只是谱学修正**：2024-01-11 实测
> 62,830 → 22,420（掉 35.7%），窗内事例存活 71.9%、本底存活 95.5%，
> **候选窗对堆积包的富集是 6.18 倍**（28.06% vs 4.54%）。而且**超量程沉积与
> 同戳簇不是同一批事例**：事例级 2×2 比值只有 1.176，候选级按 `bump_frac` 分
> 六组 `f₃ > 0` 平在 83–91% 无趋势——**所以两条判据在链上是相乘的**。

### 19a. GECAM-A 的原始记录：ch≈379 与那个宽堆积包

天格上实测过一类缺陷：每个探头有自己的量程上限，上限之下一个超量程沉积的
堆积包、之上基本为零，而准入只把最高道当溢出道，于是这些沉积以"1.2–1.9 MeV
硬光子"的身份进了搜索和谱硬度——扣掉之后他们 TGF 群的硬度中位从 **4.24 掉到
2.06**。

**GECAM-A 上确实有（2024-01-11，3 个过境、逐 25 路逐增益档实测）：**

| | 低增益档（去重后保留的那一支） | 高增益档 |
|---|---|---|
| 陡降位置（最后一个 ≥10 计数的道） | 中位 **379**，逐路 375–381 | 中位 **157**，逐路 149–160 |
| 对应能量 | ch376 = 3867、ch379 = 4024、ch383 = 4243 keV | ch157 ≈ 215 keV |
| 陡降之下的堆积包抬升 | 中位 **12.8–14.4 倍**，最大 21.8 倍 | 0.34–0.36 倍（**没有包**） |

谱形（h02，`d01`，原始计数）：平台约 250 计数/道，`ch368` 起暴涨到 9750
（39 倍），撑到 `ch378 = 3827`，`ch379` 掉到 3——**两个数量级的悬崖，上面
全是零**。`d07` 峰在 ch370 = 14563，`d25` 撑到 ch378 = 3930 后归零。
**这就是超量程沉积堆在上限附近的形状，与天格一模一样。**

**先前记的"位置不漂、是器件固定值、可以做静态逐探头上限表"是错的**——那是
只看了 2024-01-11（恰好是稳定的一天）得出的，抽样不足。跨 5 个 epoch 重测之后
结论变了两次：

**这个"上限"根本不是硬件量程，是 ADC 满量程经过能量刻度映射到的道号。**
ADC 在固定的原始码值上饱和，它落在哪一道取决于用哪份 `c2e`：

| epoch | 在轨 EC | 高增益 `edge` | 低增益 `edge` |
|---|---|---|---|
| 2022-10-21 | 无 | 156 | 379 |
| 2023-06-16 | 无 | 156 / 156 / **165** | 379 / 379 / **383** |
| 2024-01-11 | 无 | 157 / 157 / 156 | 379 / 379 / 379 |
| 2024-09-06 | 无 | 157 / 157 / 156 | 379 / 379 / 379 |
| **2025-06-15** | **有** | **227 / 230 / 233** | **447 / 447 / 447** |
| **2026-06-01** | **有** | **212 / 210 / 211** | **447 / 447 / 447** |

**台阶正好落在 CALDB 的 `c2e` 从 2025/01 起有 A 星的那条线上**：低增益从
ch379（4024 keV）跳到 **ch447（9921 keV，梯子顶）**——**梯子本来就是按
"满量程 = 顶道"设计的，是 2022–2024 段的标定把它压下来了。** 这与第 15 条
的能标偏差互为印证：默认 EC 在 1470 keV 处低估 12%，而 `c2e` 是分段二次的、
非线性使偏差随能量放大，到满量程处 447 → 379 相当于**低估 40%**；双增益两档
PI 差随能量发散（ch100–200 −13 道、ch200–300 −64 道）说的是同一件事。
**三条独立的测量指向同一个结论。**

**逐过境漂移确实存在，只是不是每天都发生：** 2023-06-16 那天三个过境里 h16
整体偏高 **9 道**（**25/25 路无一例外**，是整仪器的增益偏移不是个别探头），
2025-06-15 有 11/25 路极差 > 5 道；而 2024-01-11 / 2024-09-06 极差只有 0–2 道。
机制说得通——增益随温度走（CALDB readme 自己就在用「1470 keV 峰位 vs 温度」
修正），温度随轨道位置变，**而 2022–2024 段没有在轨 EC，这个漂完全没被修**。

**所以静态上限表不够**：要按 epoch 分段（2025 前后两个值），漂移活跃的日子还
要逐过境定。稳妥做法是**从数据本身逐过境求 `edge`，不查表**——那是一次直方图，
成本相对读 5400 万事例可忽略。

**污染是分段的，而且集中在最要紧的那一段：**

| | 2022–2024 段 | 2025+ 段 |
|---|---|---|
| 满量程道 | ch≈379 | ch≈447 |
| 堆积包位置 | **ch368–383** | ch420–447 |
| 现有 `PI < 448` 的准入 | **整个包都放进来了** | 挡住大半 |

**谱污染集中在没有在轨 EC 的 2022–2024 段——也就是 GECAM-A 唯一能做闪电关联的
那 370 小时。** 2025 年之后现有的 448 上界基本是对的（堆积包顶端 ch420–447
那截仍会漏进来）。逐路差异在有 EC 的段反而更大：2024-01-11 低增益逐路 375–381
（6 道），2026-06-01 是 **420–447（27 道）**。

**`PI < 448` 这半个准入条件是空转的**：`EVT_TYPE == 1` 的事例里
`PI >= 448` 占 **0.00%**（25 路 × 3 过境无一例外），溢出段与 `EVT_TYPE == 2`
是同一个集合。**`Event::keep` 里真正缺的不是 448 这个上界，是 ch≈379。**
现在 ch368–383 那个堆积包整个在准入范围内。

**还没做的**：

* GECAM-B / GECAM-C 上有没有同样的上限与堆积包，位置在哪、漂不漂。**B/C 的
  `c2e` 从发射后就有在轨版本（B 星 67 个 epoch），所以按上面的机制推，它们的
  满量程道本来就该在 ch447 附近、堆积包被现有上界挡住大半**——但这是推断，
  没测过。
* **准入上界怎么定还没定案。** 一个候选做法是 `EvtFile` 载入一小时时逐探头
  逐增益从 PI 直方图求满量程道、上界取 `edge − margin`，好处是自适应、
  CALDB 再更新一次也不失效，坏处是**引入"从数据自己定准入边界"的依赖**：
  统计量太少的过境（A 星有 GTI 只有 19 s 的文件）会把 `edge` 拖低而误切，
  所以要有下限保护。**这是搜索语义的修改，定案前先量"按正确口径切之后还剩
  什么"。**
* **超量程沉积与同戳簇是不是同一批事例。** 粒子沉积大、最容易超量程，
  若强相关，那么一个逐探头量程上限的准入上界可能**同时砍掉谱污染和一大块
  假候选**——而且这是**纯准入口径修正，不是新判据**。验法：把候选按"窗内
  事例落在 ch368 以上的占比"分组，看它与 `multiplet_frac` 是否正相关。
* 在这条查清之前，**所有谱学结论（谱比、硬度比、`HardnessRatio_200keV`）
  都带着这批 3.9–4.2 MeV 的假硬光子**，不能当定论。

### 20. `DEAD_TIME`：A/B 是 4 µs 常数（已答），C 星不是常数（未解释）

`DEAD_TIME` 列**每路只有一个取值 = 4**（GECAM-A 25 路 × 两档全部），**单位
没标**。同探头事例间隔：高增益档 p0.1% = 5.72 µs、p1% = 23.48 µs、中位
1439 µs；低增益档 p0.1% = 6.71 µs、p1% = 47.39 µs、中位 3779 µs。

若 `DEAD_TIME = 4` 的单位是 µs，它与实测间隔下沿（5.7–6.7 µs）差 40–70%。
**但这里用的是 p0.1% 分位而不是硬边沿，所以这条列为「没验成」**——死时间是
**分布的硬边沿**（直方图从零跳到峰的那个格），不是极小值，也不能拿最小正
间隔去推（天格实测最小正间隔比死时间小几十倍，会被偶发毛刺带偏）。
**要做的**：画事例间隔直方图找硬边沿，再与 `DEAD_TIME` 对。

**已答（2026-09-11）：单位是 µs，硬边沿 3993.5 ns。** 正确的问法不是"同探头
事例间隔的下沿在哪"（那被双增益重复对污染了——同一个物理光子的两条记录时戳差
只有 0–119 ns，远小于死时间），而是**"同探头、增益档相同的相邻事例对"**，那才
是两个真独立事例。GECAM-A 实测三个小时各 406 / 690 / 104 对落在 4 µs 内，
**时戳差全部恰好 3993.5 ns，p0 = p50 = p100 一个值、没有分布**。3993.5 ns 与
4 µs 差 0.16%，就是 float64 量化（= 134 × 29.8 ns）。

**一条顺带的推论，直接废掉一个看着很像判据的量**：单路探头在 3.99 µs 内不可能
有第二个计数，**所以一个 0.15 µs 的候选窗里 9 个计数必然落在 ≥ 9 路探头上**。
`n_det_hit == count` 在短窗里是**硬件强制的恒等式，不携带任何信息**——实测
`bin < 0.05 µs` 的候选 **99.44%** 满足它。**不能当判据。**

**但 `DEAD_TIME` 在 GECAM-C 上不是常数（2026-09-11 实测，仍未解释）。**
A 星 25 路 × 两档全部是 4；C 星 2023-06-15 12h 实测**有上百个取值、4.0–204.0 µs、
量化步 0.8 µs，分成不相连的两族**：短族 4.0–6.4，长族 69.6–204.0，中间是空的。
`dedupe_gain_pairs` 拿 `dead_time` 当合并窗，所以**在 C 星上合并窗最宽能到
204 µs**。这不是零代价：把低增益支路时戳整体平移 0.37 s 后重配做偶然对照，
**72,237 / 1,735,222 = 4.16% 的"对"是偶然的**；按旧的"时戳严格相等"口径只配到
约 132.5 万对，**死时间口径多配 31%**。**要做的**：查清那两族是什么（长族像是
某种触发/读出模式），以及候选窗内（事例密度高几个数量级）的偶然配对率——
那个数才是"去重误并了多少真事例"。

### 21. GECAM-C 早期纪元用的是另一把 896 道能量梯（2026-09-11 实测，待修）

**C 星 2022-08-03 .. 2022-10-15 的 1,121 个小时用的是 896 行的 EBOUNDS**，
而 2022-09-21 起 470 行那把开始出现，**中间 5 天两把并存、逐小时切换**
（09-21 896:5/470:19、09-22 19/5、09-30 23/1、10-14 9/15、10-15 14/10），
10-16 之后全是 470 行。2023 / 2024 / 2025 逐小时扫描至今零例外全是 470 行。

两把梯覆盖同一能量范围，**896 行那把正好细一倍**。后果是道号阈值在两把梯上
是两个能量：

| | 470 道 | 896 道 |
|---|---|---|
| `ch54` | 39.12–40.18 keV | **14.16–14.54 keV** |
| 40 keV 落在 | ch54 | **ch109** |
| `ch447` | 9921–10053.5 keV | **511.0–514.4 keV** |
| 溢出哨兵 | 在表里（448..469） | **不在表里**（表止 ch895，`EVT_TYPE==2` 的 PI 落在 896..917） |

`EboundsHdu::verify` 的尾部闸（ch447 必须止于 10053.5 keV）把这些小时**整小时
判 `corrupt_data`**。**闸门动作正确——照搬道号阈值会把能窗整个搬错位，14 keV
以上全收、514 keV 封顶。** 被拒的只有这一类，`head`（ch0 = 2.00）与 `gap`
（梯内连续）两道闸一条都没失败：**两把都是干净连续的梯子，只是长度不同。**

**代价：1,121 小时 ≈ 703 小时活时间 ≈ 29 天曝光，全部落在 WWLLN 覆盖内。**

> **⚠️ 703 这个数待核（2026-09-11）：它是按占空比折出来的，不是逐小时 GTI
> 实算的。** 第 25 条刚在同一个毛病上栽过一次——那里先前报"拿回 20 小时"，
> 实算之后是 **34,373 s ≈ 9.5 小时活时间**，因为 **"整小时能载入"与"这一小时
> 有曝光"是两件事**（引发那条的小时修复后 `searched_seconds = 0.0`、
> `masked = 3600.0`，GTI 与本小时一秒都不重叠，**候选一个都没多**），而且
> 那个 20 还没扣掉与本条重叠的 8 个小时。
> **"+6% 可搜曝光"是要写进论文的数，必须是逐小时 GTI 的实算值**，896 梯验收
> 时一并替换。
相对全任务可搜曝光是 +6%，但对 2022-08..10 那段是从零到全部。

**建议的修法（未获批，搜索语义修改）**：两个常数都从 EBOUNDS 自己推，不写死、
不认版本号——**梯长 = 从 ch0 起 `E_MAX[k-1] == E_MIN[k]` 连续到断开为止的长度**
（470 版在 ch448 断，896 版不断到底），**能阈道 = `channel_above(40 keV)`**
（`EboundsHdu` 本来就在算这个值，现在只是拿它去跟写死的 54 比对然后报错）。
保留不动的闸：`head`、`gap`、以及**梯顶必须是 10053.5 keV**（两个版本都过，
只是索引跟着梯长走）——那才是真正护住能量刻度的那道闸。

**对照：GECAM-B 没有细梯纪元。** 2020-12-10 .. 2022-09 逐日抽点 646 天全部
498 行（= 448 条能量梯 + 50 行溢出段），首见即 B 星第一天有数据的那天。
**"只有 C 星有"比"C 星有"信息量大得多。** A 星待查。

**同一个写死的道号散在五处，而只有两处过校验**（2026-09-11 清点）：

| 位置 | 写法 | 过 `EboundsHdu::verify`？ | 在 896 道小时上的行为 |
|---|---|---|---|
| `types/event.rs` | `MIN_CHANNEL: i16 = 54` | ✅ | **整小时报错** |
| `io/evt/ebounds_hdu.rs` | `OVERFLOW_CHANNEL: i16 = 448` | ✅ | **整小时报错** |
| `scripts/cluster/gecam_features.py` | `MIN_CHANNEL, OVERFLOW_CHANNEL = 54, 448` | ❌ | **静默给出错的特征** |
| `evidence/gecam-c/gc_feat.py` | 同上 | ❌ | 同上 |
| `evidence/gecam-c/gc_f3.py` | 同上 | ❌ | 同上 |

Python 那三份的注释写着"与 Rust 侧 `Event::keep` 一致"——**一致是对的，但两边
一起错**。

> **⚠️ 这个形态本身比"写死常数"更值得当成一条规矩记下来（统筹要求，2026-09-11）：
> 同一个常数有几份副本，而只有一部分过校验时，有校验的那份会拦、没校验的那份
> 照跑，于是两份产物看起来都正常。** 在 896 道的小时上，Rust 侧**响亮地失败**
> （整小时 `corrupt_data`），而离线特征脚本会把 14 keV 以上的事例全收进来、
> **不报任何错**，给出错的 `multiplet_frac`、错的谱、错的一切。**"有一半会报错"
> 比"全都不报错"更危险**——它让人以为校验覆盖到了。
>
> **收敛方向**：常数收敛到一处（Rust 侧导出、脚本读），或至少让脚本走同一个
> 校验；在那之前，**新增副本必须登记进上面这张表并注明有没有校验**。
> 五处已在 2026-09-11 全部改成从 EBOUNDS 自己推。

**另外 `MIN_CHANNEL` 的文档注释里那句"三颗星的能量梯逐道一致……所以这一个道号
常量对 A/B/C 通用"已被本条证伪，要一并改掉**，不要留着。

### 22. 事例流层面的跨探头合并（τ ≈ 150 ns）：已报批，门槛已定，未实现

第 14 条末尾那个"还没评估的替代方案"现在有了带实测常数的方案：**在 `search`
里排序、双增益去重之后，加一道跨探头单链接合并——相邻事例时戳差 ≤ τ 且探头号
不同，并成一个事例。** 一次带电粒子穿越在泊松意义上本来就是一个事件，并了
之后独立性从源头恢复，本底估计同步一致地修正。

**τ 是量出来的，标尺与候选/搜索/阈值全部无关**：双增益对（同一路、死时间内、
一高一低）是物理上确定的同一个光子，它们的时戳差就是电子学给同一件事打不同
时戳的幅度。三个量化纪元一致指向**一个约 100 ns 的固定延迟**（见第 32 条；
量化纪元本身见第 18 条）。
平台检验：被并掉的计数占比 100→150 ns 还涨 0.35–0.51 个百分点，150→200 ns
只涨 0.03–0.05；簇内跨度 p50 = 60 ns / p99 = 149 ns，**簇不是被 τ 截断的**；
偶然期望富集 58–104 倍；多成员簇 100% 跨探头（同探头一对都没有，死时间保证）。
**τ 应当是常数、不随 q**：底下的量与量化格无关，q 只决定能不能分辨开；若改成
q 的倍数，C 星（q = 7.45/14.9 ns）的 τ 会缩到 30–60 ns，连那条 ~100 ns 的线
都盖不住。

**效果（A 星 2024-01-11，7050 s）**：`fa ≤ 20` 的候选 62,830 → 4，
8.9 /s → 5.7e-4 /s；τ 在 150–300 ns 之间结果不动。

> **门槛 (3) 已过（2026-09-11，GECAM-B）。合并后的事例流在 1 µs 到 10 ms 的
> 全部五个尺度上与泊松一致。** 72 段 × 10 s，6 个小时，跨 2021-01 .. 2022-08。
>
> **对照是这条结论的要害，先说它**：合并本身会吃掉 τ 内的事例，**那是一种
> 死时间效应，单独就会把方差压到均值以下**——所以"合并后 F 更接近 1"这句话
> 本身没有分母。做法是每段另跑 20 次**同段、同总数、同探头份额的齐次泊松流
> 走同一条合并管线**，拿 `F_sim_post` 当尺子。合并前的对照也用**新口径**
> （`3c017a2` 死时间 / 一高一低）去重后的流，**旧口径漏并的那 11.6–14.2%
> 不会算进 τ 的功劳**（见第 17、32 条）。
>
> | bin | λ | `F_pre` | `F_post` | `F_sim_post` | `(F_post−F_sim)/sd` |
> |---|---|---|---|---|---|
> | 1 µs | 0.012 | **1.1493** | 0.9968 | 0.9967 | **+0.3** |
> | 10 µs | 0.120 | 1.1526 | 0.9945 | 0.9965 | −1.4 |
> | 100 µs | 1.198 | 1.1557 | 0.9914 | 0.9963 | −1.2 |
> | 1 ms | 11.98 | 1.1570 | 0.9929 | 0.9961 | −0.2 |
> | 10 ms | 119.8 | 1.1632 | 0.9934 | 0.9943 | +0.0 |
>
> **合并前在 1 µs 档偏离泊松 +353σ**（10 µs +113σ、100 µs +37σ、1 ms +11σ、
> 10 ms +4σ）。KS 对指数分布：**合并前 0.0440（1% 临界 0.0047，超 9.4 倍），
> 合并后 0.0040，落到临界值以下**——F 与 KS 是两个独立的检验，都过。
>
> **三条辅助证据说明这不是"合并把一切抹平"**：① 合并掉的比例实测中位 4.58%，
> 同速率泊松模拟只有 0.18%，**25.8 倍**——吃掉的是真实成簇不是偶然相邻；
> ② `F_pre` 与 `merged_frac` 秩相关 **+0.891**（过离散的量与被合并掉的量是
> 同一件事的两面）；③ `F_pre` 与 |磁纬| +0.478、与速率 −0.767，指向带电粒子
> 跨探头同时点亮。
>
> **`F_pre` 在四个数量级上基本不变（1.149 → 1.163）本身就是零对照**：速率
> 起伏造成的过离散会随 bin 变宽而变强，实测是平的，说明**簇的时间尺度远小于
> 1 µs**——所以它能被 τ 打掉是自洽的，**堵死了"任何过离散都会被合并抹平"
> 这个反驳**。
>
> **三条限制，都要跟着结论走：**
> 1. **⚠️ 适用范围：`|磁纬| > 40°` 上这条结论不成立，而且在 B 星上补不了——
>    倾角 29° 决定了它原理上去不了。** 四天实测 `|地理纬|` 最大 29.13°、
>    `|磁纬|` 最大 38.4–38.5°、`> 40°` 占比 **0.0000%**；那 72 段覆盖
>    −37.6 .. +37.0°，**已经是这颗星的全部可达范围**。所以**不能写成"只在中低
>    磁纬验过"**（那暗示再采样就能补），要写成 **"B 星去不了高磁纬"**。
>    **推论**：REP 人群（HXMT 实测 84% 在高磁纬，HXMT 倾角 43°）在 GECAM-B 上
>    不可及 ⇒ **B 星池里原理上不该有 REP 污染**（独立论据），但**这条泊松结论
>    也不能外推到倾角更高的仪器**。
> 1b. **⚠️ 残差不是随机分布的，它随磁纬系统地变——这一条不能被"中位 ≈ 0σ"
>    盖过去。** τ 把磁纬依赖削掉了大约一半，但没有削到 0：
>
>    | bin | `F_pre~\|mlat\|` | `F_post~\|mlat\|` | `(F_post−F_sim)~\|mlat\|` |
>    |---|---|---|---|
>    | 1 µs | +0.413 | +0.382 | **+0.323** |
>    | 100 µs | +0.478 | +0.250 | +0.242 |
>    | 10 ms | +0.298 | +0.095 | +0.113 |
>
>    **所以准确的说法是：合并后的中位残差 ≈ 0σ，但高磁纬那一端 `F_post` 高于
>    模拟、低磁纬那一端低于模拟，而 `fa` 看不见这个结构。** 待办是按磁纬分档
>    定量，**并把它折算成"最坏那一档的 `fa` 被低估几倍"**（`F` 偏离 1 多少不
>    直观，簇大小分布已存，复合泊松的尾巴只由它决定）。**B 星的"高磁纬端"只到
>    38.5°，所以那个数是下界。**
> 2. **长档的约束是弱的**：10 ms 档模拟散布 sd = 0.044，比 1 µs 档（0.00037）
>    差两个数量级，那里的"≤ 0.3σ"只等于"最多还能藏 4.4% 的过离散"。正在补跑
>    24 小时 × 60 段收紧，并存簇大小分布（**复合泊松的尾巴、也就是 `fa` 被抬
>    多少，只由这张分布决定**）。
> 3. **"不是几个亮事件带的"那条对照分辨力不够**：72 段里 68 段含 `fa ≤ 1e-5`
>    的显著候选，只有 4 段不含。方向对（4 段 `F_pre` = 1.156 与 68 段的 1.156
>    一样）但 4 段太少，**不当证据**。顺带：**"几乎每 10 s 就有一个显著候选"
>    本身就是第 14 条那个候选率高四个数量级的直接体现。**

> **⚠️ 门槛 (2) 的代价已经有第一个实测下界了，而且比全小时的数大七八倍
> （GECAM-C，2026-09-11）：在真候选窗里，τ = 150 ns 合并掉的对当中
> **至少 22% 是偶然的**（2023-04-12 22.07%、2023-01-06 21.83%），
> 而全小时口径只有 4.16%。双增益那一档窗内偶然占比更高（31.3% / 32.5%），
> 折合每窗误并 0.143 条、**窗内计数被压低 1.6%**。
>
> **"至少"是硬的**：这个数用**窗内均匀重抽**量（事例数、窗长、探头与增益的
> 边缘分布原样保留，只打掉时间上的符合结构），**均匀是下界——真 TGF 的光子比
> 均匀更挤，偶然率只会更高**。所以 **22% 是代价的下限不是上限**，
> **不能当"最多损失 22%"来读**。这与门槛 (3) 禁止用解析式
> `1 − exp(−nτ/W)` 是同一件事。
>
> **方法上有一条要记的**：平移对照在窗内**系统性地量不对**——平移之后窗里剩的
> 是暴发的高增益那一半加上从窗外飘进来的本底，**窗内密度被"平移"这个动作本身
> 破坏了**，量到的是本底密度下的偶然率。窗内的零对照只能用**窗内重抽**。
>
> **还有一条口径**：**窗比 τ 还短的时候"偶然"这个词没有意义**——整窗都在 τ 之内，
> 撒哪儿都成对，真与偶然必然相等。C 星亚微秒候选占一成上下，**混在一起报会把
> 两件事平均成一个没法解释的数**，要按窗长 ≤ 1 µs / > 1 µs 分两档。

**放行门槛（统筹定，四条，缺一不可）**：

0. **（2026-09-11 追加，排在 (1) 之前）先算这道门的功效 `Π(1−qᵢ)`，
   否则 (1) 的结论不可读。** 逐个算那 147 个各自被合并杀掉的概率 `qᵢ`
   （按它自己的 n、W、探头分布、τ，**用 (2) 那套注入量，不用解析式**）：
   * **`Π(1−qᵢ)` 接近 1** ⇒ **这道门什么也没测**，"147 个全活着"不构成证据，
     要换一个有功效的门（放到暗端子样本上，或改用净计数的连续量而不是
     通过/不通过）；
   * **`Π(1−qᵢ) < 0.05`** ⇒ 全过才是强证据，(1) 这时才值得跑。

   **这条是天格实测栽出来的**：它的 v12 判据第 1 步看起来完美通过（7 个闪电
   证实的 `k₃` 全为 0），**但那 7 个的 Σλ₃ 只有 0.012——门根本没触发过，
   不是余量大**；注入实测期望误杀 1.54 个、实测 0 个，**「7 个全过」的概率
   是 0.174**。而那条判据实测比旧判据更差（回收率 0.821 vs 0.934）。
   **一个已被证明更差的判据，仍有六分之一的机会"全部通过"。**
1. **先做泊松性检验，它不过后面三条都不用做。** 取安静区间，合并后的到达间隔
   是不是指数分布、方差是不是等于均值。**若仍然过离散，`fa` 就仍然是错的，
   只是错得少一点**，整套显著性还是不能用。
2. **逐个已发表 TGF 报合并前后的净计数、显著性、`bin_size_best`，不止报
   "在不在"。** 147 个全都活着但净计数系统性掉了 15%，说明合并在吃真光子。
3. **损失率不许用解析式 `1 − exp(−n·τ/W)`，要往真实本底里注入测**，扫 (n, W)
   平面、特别是短而亮那一角，**而且注入模板要用真 TGF 的实测形态**（前沿陡、
   有拖尾；同样的 n 和 W 下光子挤在一起的程度完全不同，而挤在一起正是被 τ
   吃掉的条件——均匀撒点会把损失率系统性低估，正好偏在让判据显得安全的方向）。
4. **幸存者要逐个完整取证**，别因为它们是幸存者就默认是真的。

**这是搜索语义修改，所有既有 GECAM 目录与它不可比**，版本边界要在产物目录里
写死。代码在获批前只在分支上、标"未验证"。

**对外口径必须报两层，不能只报加了模型的那个数**（统筹定，2026-09-11）：

| 层 | 含义 | 依赖 |
|---|---|---|
| **无模型** | 只经既有判据、不经跨探头合并的候选数 | 不依赖 τ 是否成立 |
| **加模型** | 经 τ = 150 ns 合并之后的候选数（A 星 2024-01-11 那一天是 **4**） | τ 与四条门槛全部成立 |

理由：合并是一个**关于仪器的物理模型**（一次穿越 = 一个事件），它可能是对的，
但读者必须能看见去掉这个模型之后还剩什么。**只报加了模型的那个数，等于把模型
的正确性偷偷算进结论里。**

> **⚠️ 两层的具体数字要从数据现算，不许手打。** 统筹口头给的是"无模型 336 /
> 加模型 4"，但 **336 这个数本文件里没有出处**——它对应哪一天、哪一颗星、经过
> 了哪几条判据（`bin_size_best ≥ 50 µs`？双增益去重？）都还没写下来。
> 落进任何对外材料之前，**这两个数必须由跑数的人从产物里现算并在这里写明口径**，
> 包括星、日期、曝光、以及按顺序列出用到的每一条判据。
> （这条是有先例的：曾把模板的 N 写成字面量供给别人，供出 4677 而真值 3467，
> 别人据此算的误差棒小了 16%。）

### 23. `f₃ > 0` 不是与亮度无关的一刀（2026-09-11，修正第 14/18 条）

"窗 ≥ 10 µs 时 `f₃ > 0` 即粒子，三重偶然期望 ≤ 1e-5，零参数零阈值"——
**这句话只说明它不误杀本底，没说不误杀信号。** 单链接分簇的解析式
（`p = 1 − e^{−nτ/W}`，`P(至少一个三重) ≈ 1 − exp(−(n−2)p²)`，均匀撒点是下界）：

| 样本 | n | W (µs) | τ = 0 | τ = 150 ns |
|---|---|---|---|---|
| 典型 TGF | 30 | 111.4 | 0.18% | 4.29% |
| 亮 TGF | 100 | 111.4 | 6.60% | **78.89%** |
| **亮而短** | 100 | 30.0 | **58.37%** | 100% |

**连 τ = 0（时戳精确相等）的版本对亮而短的真 TGF 都要误杀 58%**，而那正是最
在意的一群。**正确用法是逐候选按它自己的 (n, W, τ) 算偶然期望，不是一刀切。**

反过来用它验最显著的一批（GECAM-A 2024-01-11，过了准入上界与窗长 ≥ 10 µs 之后
`fa ≤ 7e-7`）。

> **⚠️ 2026-09-11 作废：原文写的是「28 个、`f₃ > 0` 是 28/28、这 28 个最显著的
> 候选是粒子」。用新去重口径按同样的选法重做，给出的是 130 个，`f₃ > 0` 是
> 105/130 = 80.8%。「28/28」这个数作废，别再引用。**
>
> **但粒子的结论反而更硬了**：690 个实测三重计数对偶然期望——
> **τ = q（29.8 ns）时 0.2355，2,930 倍；τ = 150 ns 时 5.563，124 倍。**
> 窗长 p50 = 20.3 µs（10–50 µs 有 108 个、≥ 50 µs 有 22 个），**全在 `f₃` 有
> 分辨力的那一档**（见第 34 条）。
>
> **说不清的是剩下那 25 个 `f₃ == 0` 的候选，不是"全都是粒子"。**
>
> **两条口径教训**：① **报 `f₃` 必须同时报 τ**，这里两套 τ 差 24 倍；
> ② 原来那个"28 个"是在老去重口径下选出来的，而**老口径产物的对账只有
> 63.36%**——它与产出候选的二进制不是一套，**用它算的同戳量都要标口径**。

> **⚠️ 口径依赖（2026-09-11 合并时发现，方向已定、量级待逐候选数）：上面这个
> 28/28 是用哪一版去重的产物算的，当初没有记下来。** 第 32 条量到旧口径
> （`e34f560` 的「同探头同时戳」）漏并了全部双增益对的 **11.6–14.2%**，
> 折算到准入后的事例是**旧口径多留 3.6–4.1%**（B 星整小时实测；先前估的 2.9%
> 偏低）。漏掉的那批**同探头、相距约 100 ns**。
>
> **⚠️ 2026-09-11：这里先后写过三个机制，前两个都被实测否掉了，留下过程是因为
> 第三个是靠一个判别性检验选出来的。**
>
> | 机制 | 预言 | 实测 |
> |---|---|---|
> | ① 分母涨、分子不涨 ⇒ `mf` **被压低** | `mf` 旧 < 新 | ❌ 实测旧 > 新 |
> | ② 多留的事例让**偶然**撞同戳变多 ⇒ 抬高 | 抬升倍数随窗宽变（`(n−1)q/W`） | ❌ 方向对、机制错 |
> | ③ **影子副本**（见下） | 抬升只随"多留了多少事例"走，**与窗宽无关** | ✅ |
>
> **判别性检验是按 `bin_size_best` 分档**：偶然撞一对的概率 `(n−1)q/W` 从
> **1.000 掉到 0.001（三个数量级）**，而抬升倍数**恒定在 `mf` 1.02 / `f₃` 1.04**
> ——**偶然解释要求倍数跟着变，实测不变，所以②出局。**
>
> **③ 真机制：那约 100 ns 是个固定延迟。** 一次跨探头符合里，各探头的高增益
> 孪生条**都比自己的低增益条晚同一个 100 ns，于是它们彼此落在同一个时戳上**
> ——在旧口径下形成真簇的一个**影子副本**，自己就是一个合格的跨探头同戳多重。
> **所以 `mf` 与 `f₃` 同向抬高，倍数只随多留的事例比例走（3.7–4.3%，恒定）。**
>
> **实测（89/138 小时，168.8 万候选，同戳口径 `tol = 0`）**：整小时准入后旧口径
> 多留 **4.269%**；`mf` 0.7238 → 0.7378（**1.019**）、`f₃` 0.4431 → 0.4597
> （**1.038**）、`f₃ == 0` 21.11% → 18.74%。
>
> **顺带独立复核了第 22 条那句死时间保证**：`mf` 与 `f₂` **逐位相等**
> ⇒ **所有同戳簇都跨探头，一个同探头对都没有。**
>
> **⚠️ 报 `f₃` 必须写明用的是哪个簇口径——这份文件里 `f₃` 出现过两次而口径不同。**
> A 星那个"旧口径 `f₃ == 0` 只留 2.4%、新口径 39%（16 倍）"用的是 **τ = 150 ns
> 单链接**，而上面这批是 **`tol = 0` 同戳**。**约 100 ns 恰好落在两者之间**：
> 同戳口径下孪生条**另起一个影子簇**（`f₃` 只抬 3.8%），容差口径下孪生条
> **链进原簇**、把真二重凑成三重（大幅抬高）。**同一批数据两个口径差一个数量级
> 是预期之内，不是矛盾。** 四套并排（{新,旧} × {tol=0, tol=150 ns}）在跑。
>
> **三条受同一个口径影响、都要复核的结论**：本条的 28/28、第 14 条的 `mf`
> 全套数字（528/天、0.875 中位等）、第 27 条把 `multiplet_frac ≤ 0.3` 判出局
> 的那组通过率。**在复核之前，这三处的数字都只在「与它们各自的去重口径一致」
> 的意义上成立**，不同口径之间不可直接比较。
>
> **另有一条对 28/28 的独立质疑，答案是「打不到它们」**：A 星量到 `f₃ > 0` 在
> 亚微秒窗上**是零信息**（原始池 obs/exp = **1.2**，准入后 1.1——窗只有几个
> ulp 宽时偶然撞出三重本来就是常态），只有窗拉长才有判别力（`bin ≥ 50 µs` 时
> 偶然期望塌到 0.47 个而实测 14,755 个，**31,435 倍**）。**但这 28 个本来就
> 被限制在 `bin ≥ 10 µs`**，不在零信息那一档，所以这条质疑打不到它们，反而
> **支持**它们所在的那一档。**注意两套数的 τ 不同**（本条用 τ = 150 ns，
> A 星那张表用 q = ulp），偶然期望大致正比于 τ²，**不能直接比，报的时候要把
> τ 标出来。**

### 24. 成串判据在 A 星与 C 星都是空的，而且原因不同

* **GECAM-C**：`neighbors_10min` 双向失效是**尺度不匹配**——粒子增强是亚秒
  尺度（0.63 s 里 36 个候选），而 ±600 s 的计数窗看不见它。换到 ±30/±60 s
  分离度就回来了（AUC 1.000）。
* **GECAM-A**：五个尺度（±0.5 / 5 / 30 / 60 / 600 s）幸存者对均匀期望之比
  全平在 1.0 ± 0.1。根因不是尺度，是**整个池子被均匀污染**——相邻候选间隔
  中位 0.076 s，而均匀泊松池的期望中位是 0.0778 s，**池子在时间上就是均匀的**。
  A 星的同戳簇是 25 路仪器上宇宙线穿越的**稳态本底**，不是发作式增强。

**两颗星合起来的结论：成串判据能不能用，取决于污染是不是发作式的；这件事要
逐仪器先量池子的时间均匀性，不能从别的任务搬。**

### 25. CPD 空壳文件拖死整小时好数据（2026-09-11 实测，待修）

`from_epoch` 的规则是「只放过找不到 CPD 文件；文件在却读坏了仍旧报错」。
**但归档里有一类占位空壳**——文件在、大小 28,800 字节、两个 `EVENTS` 表
**零行零列**，于是报 `corrupt_data: Cannot find column "TIME"`，**整小时被扔**，
连同那一小时完好的 GRD 数据。

实测追到的例子（GECAM-C 2023-01-30 那一小时，就是 30 天旧账里 searched 654 ≠
应有 655 差的那一个）：GRD 有 `v00` 空壳和 `v01` 的 269 MB / 1,481 万事例好文件，
**版本选择正确取了 v01**（版本号解析那条修复在起作用）；拖死它的是只有 `v00`
的 CPD 空壳。

全任务扫（按最大版本的 `EVENTS` 表有没有列判）：

```
CPD 空壳（零列） 217 小时      GRD 空壳（零列） 206 小时
CPD 空壳而同一小时 GRD 是好文件（> 1 MB）的: 20 小时  ← 白丢的
```

**20 小时 = 0.10%**，量不大但是纯损失。**建议的窄修法**：CPD 文件存在但
`EVENTS` 表零列/零行 → 与「找不到 CPD 文件」同等对待（降级成这一小时没有 CPD，
`acd = None`，GRD 照常搜）；**GRD 空壳必须照旧致命。** 空壳与真损坏形态上分得开
（零行零列，不是读到一半乱码），所以这个放宽不会顺带放过坏数据。

### 26. GECAM-C 是太阳同步轨道，LST 判别量在 C 星上不可用（已答，2026-09-11）

从 posatt 直接量，不查文献：1,610 个升交点、886 天（2022-08-03 .. 2025-02-13），
**升交点地方时（LTAN）锁在 22.5 h 附近，913 天只漂 +0.81 h**（逐 60 天中位
22.509 → 22.527 → 22.797 → 23.469 h，四分位极紧）。对照非同步轨道
（Fermi 倾角 25.6°）进动周期约 53 天、会走遍 24 h。**判定：太阳同步。**

**后果是当地太阳时（LST）成分拟合这条判别量在 C 星上前置检验不通过：**

```
磁纬带       采样点    LST 覆盖的 2h 格数   最大格/最小格   变异系数
 0–10°       17183         2/12              8595          2.24
10–20°       17218         2/12              8630          2.24
20–30°       17202         2/12              8606          2.24
30–40°       17244         6/12              8441          2.17
40–55°       26272         6/12             10444          1.74
55–90°       57447        12/12                 5.7         0.61
```

**在 TGF 主产区（|磁纬| < 30°）12 个两小时格里只有 2 个有曝光，最大/最小 8,600 倍**
（全样本 LST 逐小时曝光两个尖峰在 10 时与 22 时、各占 23.2%；2–7 时与 14–19 时
合计不到 5%）。**用两个地方时点拟合雷暴日变化曲线是不可能的。** 只有
|磁纬| > 55° 那一带铺得开（12/12、变异系数 0.61），而那是极冠、不产 TGF。

**"GECAM-C 的轨道使 LST 判别量不可用"——这句可以直接写进论文限制。**
太阳同步轨道的升交点地方时被锁死是它的**定义性质**，不是"可能有"的风险。
GECAM-A/B 倾角 29°，属于会进动的一类，前提大概率通过，但**仍要各自量一遍**
（A 星有效数据只有 554 天且零散，进动可能没走完整周）。

**一条口径**：GTI 缺口落在极冠这个形态是**高倾角**的指纹，不是**太阳同步**的
指纹——极冠穿越对任何近极轨道都成立，太阳同步是对轨道面进动率的额外约束，
靠 GTI 形态区分不了，必须看 LTAN 漂不漂。

### 27. `multiplet_frac ≤ 0.3` 作为硬判据已出局（2026-09-11，外部真值判的）

规矩是**任何判据只要切掉正样本里任何一个就不能用**。拿 147 个已发表 TGF 量：

* 按我们匹配上的最佳格量，`mf ≤ 0.3` 放行 **128/140 = 91.4%**，切掉 12 个。
* 担心"最佳格可能落在 TGF 旁边的粒子尖峰上而不是 TGF 本身"，所以另做了一次
  **构造无关**的复测：完全不用我们的最佳格，只锚目录 UT，在 ±3 ms 里用固定
  100 µs 滑窗找计数最高的一格。放行 **133/145 = 91.7% [86.1, 95.2]**，
  **与按最佳格量的 91.4% 一致——所以那 8.3% 的损失是真的，不是选窗选出来的。**

**而且损失集中在暗端，正是我们的候选所在的地方**：按目录 `NetCounts` 分档，
< 30 只放行 **47.4% [27.3, 68.3]**，30–60 是 97.3%，≥ 60 是 100% / 95.2%。
**根因是 `mf` 的分母是窗内计数**——计数少就被偶然撞的一对顶上去，而我们候选的
计数中位是 9，比这里最差那一档还低。

**这条与第 14 条里"`mf ≤ 0.3` 通过率随窗内计数单调上升"是同一件事的两面**：
在污染上它表现为"亮而长的粒子暴漏过去"，在信号上它表现为"暗而短的真 TGF 被
切掉"。**一个会被计数稀释的占比，两头都靠不住。**

**它仍可以当离线诊断量记录，不要当硬判据。**

### 28. CPD 的正确用法是亚微秒时戳差，不是"窗内有没有计数"（方向确定，阈未定）

第 14 条那个 ±10 µs 的"有没有 CPD 计数"没有分辨力（第 14 条开头的撤回说明了
为什么）。换成**最近的 CPD 计数与 GRD 计数的时戳差**之后，分离出来了：

| | 真 TGF（145 个峰窗） | 对照（40,800 个池候选最佳格） |
|---|---|---|
| 窗内 CPD 计数 | 363 | 30,342 |
| `\|Δt\|` 中位 | 0.268 µs | **0.000 µs** |
| **`\|Δt\| == 0` 的 CPD 计数占** | **5.79%** | **62.04%** |
| `\|Δt\| ≤ 1 µs` 的占 | 81.54% | 75.26% |

**分离 10.7 倍，而且全部集中在 0–0.1 µs；窗开到 1 µs 两边就几乎相等**——
这正解释了原来那个 ±10 µs 口径为什么没有分辨力。物理上说得通：**粒子穿仪器时
CPD 与多路 GRD 记的是同一次沉积、同一个时戳；而真 TGF 在 CPD 里的计数是另一个
伽马散射来的，没有理由与任何 GRD 计数同戳。**

**还不能直接当否决用**：按候选级"有 ≥1 对同戳就否决"切掉真 TGF
**11.72% [7.45, 17.97]**、切掉池 35.34%，净富集只有 3.0×。**该用占比不是有无，
阈要在新口径池上重扫。** 标"方向确定、阈值未定"。

一条口径优势：**CPD 表没有 `GAIN_TYPE`、不参与双增益去重，所以这条量基本不受
去重口径变动的影响。**

### 29. LST 判别量：A/B 可用，C 不可用；模板要借 HXMT（2026-09-11）

**0a（模板对比度）GECAM 过关**：147 个已发表 TGF（触发目录，**不是闪电认证的**
——任何经闪电关联认证的样本都带 WWLLN 夜间效率造成的约 6 小时相位偏置，
HXMT 实测关联样本峰在 LST 1.5 h、未关联样本峰在 16.5 h）的 LST 分布
**瑞利 R = 0.163、Z = 3.90、p = 0.0199**，极小值 09–12 h、最大值 15–18 h。
对照 SVOM 的 83 个闪电证实样本 p = 0.82（塌了）。

**跨仪器互校是这一条里最值钱的**：与 **HXMT 未关联那 4677 个**比
**χ² = 3.86 / 7，形状一致**。两个独立仪器、倾角 29° vs 43°、触发目录 vs
我们的搜索，峰谷全对上——**同时验了三件事**：LST 算法没错、HXMT 那批确实以
TGF 为主、我们可以借它的大模板。

**但 147 个的模板直接用会系统性压低 `f_TGF`，这是回归稀释不是统计涨落**：
观测 `Σ(p − 1/8)² = 0.01415`，纯多项式涨落就占 `7/(8·147) = 0.00595`，去偏后
真实偏离只有 0.00820，衰减因子 0.58。蒙卡（300 次）实测偏差**不随 N 缩小**：
N = 100,000、f = 0.20 → f̂ 0.123；f = 0.50 → f̂ 0.307。换成 HXMT 那 4677 个当
模板偏差基本消失、σ 还小一倍。**建议：用 HXMT 未关联当模板报相对纯度
`g = f_D/f_T`（合法性来自 χ² = 3.86/7），要绝对纯度就用 147 个那份并把 0.58
显式解卷积回去、把它自己的不确定度传上。两者分开报，别混。**

**三条限定：**
1. **陆海拆分方向对但分离弱**：陆 41 个（R = 0.273、p = 0.046、圆均值 18.79 h）
   vs 海 106 个（R = 0.130、p = 0.166、圆均值 21.02 h），**两个子模板圆均值只
   差 2.2 h，不是一个午后一个凌晨**；而且目录给的是**星下点**不是源位置，沿海
   的会分错。**这也是借 HXMT 模板的风险所在**：B 星倾角 29°、样本 72% 在海上，
   HXMT 倾角 43° 能覆盖中纬大陆雷暴区，**借模板要带纬度带失配的限定。**
2. **模板的对比度全部来自暗的那一半**：亮半（n = 74）R = 0.093、p = 0.43
   **是平的**；暗半（n = 73）R = 0.321、p = 0.006；两半形状不一致（χ² = 17.90/7，
   p ≈ 0.012）。而我们要拟的候选（计数中位 9）比模板（`NetCounts` 中位 66.9）
   暗得多。
3. **0b（曝光平坦性）三星分别判**（见第 26 条）：

| | 抽样 | 跨度 | 升交点 LST 范围 | 圆集中度 R | 判 |
|---|---|---|---|---|---|
| GECAM-A | 79 天 | 1411 天 | 0.35 .. 23.72 h | 0.156 | 通过（χ² = 5.8/7） |
| GECAM-B | 204 天 | 2073 天 | 0.09 .. 23.86 h | **0.018** | 通过 |
| GECAM-C | 176 天 | 913 天 | **22.49 .. 23.52 h** | **0.998** | **不通过，太阳同步** |

C 星那条还有一条**用完全不同输入的独立验证**：本底候选（`fa > 1`，30 天
52,823 个）逐磁纬带的 LST 分布，**|磁纬| < 40° 的每条带子里候选只落在两个
3 小时格、其余六格恰好是零**（χ² 两万量级），非空的两格是 09–12 h 与 21–24 h，
与升交点地方时 22.5–23.5 h 差约 12 h，自洽。

**A 星不能靠轨道判型免掉曝光平坦性检验**：轨道管"能看到哪些 LST"，观测计划管
"实际记了哪些"，而 A 星 2024-02..06 长期每天固定 3 小时。

**一个共用的坑**：POSATT 里有 `X = Y = Z = 0` 的坏行，两条独立做这件事的脚本
都被它坑过（一条把某天算成上百个假升交点、首轮得出 B 星 R = 0.645 的错数）。
**按 `|r|` 过滤。**

**0b-ii 的正确问法（2026-09-11 补，这条会咬别的仪器）**：不是"实测的候选 LST
分布平不平"，而是**"实测结构比只由轨道相位造成的结构多多少"**。做法是拿同期
posatt 按曝光加权算一份"纯轨道预期"的 LST 直方当分母，再跟实测比。

理由是量出来的。把升交点 LST 减去一个线性漂移再扫漂移率、取圆集中度最大的那个：

| | 漂移率 | 折合进动周期 | 对齐后 R |
|---|---|---|---|
| GECAM-A | **−0.4953 h/天** | **48.5 天** | 0.696 |
| GECAM-B | **−0.4946 h/天** | **48.5 天** | 0.973 |
| GECAM-C | **+0.0008 h/天** | 约 3 万天 | 0.999 |

A 与 B 的漂移率到小数点后三位一致（同一星座同一条轨道，本该如此，也是这个扫描
没算错的旁证）；C 星慢 600 倍，与"913 天漂 +0.81 h"对得上。

**所以 A 星现有那 3 天的候选表跑出来的"本底强结构化"（`fa > 1` 的 21,399 个、
全样本 R = 0.368、`|磁纬| 0–10°` 有 70.12% 挤在 15–18 h、三个格恰好为零）
不能当 A 星的 0b-ii 结论**：3 天只走完进动周期的 6.2%，**一段短于进动周期的
曝光 LST 覆盖必然结构化，这与观测计划无关，是轨道本身。** 3 天的样本没有能力
区分"轨道相位没走完"和"观测计划挑了地方时"，报"A 星本底不平"会把轨道的账算到
计划头上。

B 星虽然跨 2073 天、走了 42 个进动周期，**但逐磁纬带分完之后每格的有效天数会
掉下来，这个分母仍然该算。**

### 30. `bin_size_best` 是目前最强的单一判别量（外部真值定标，2026-09-11）

同一台仪器、同一套搜索，两个人群：

| | 中位 | < 1 µs | < 10 µs |
|---|---|---|---|
| 已发表真 TGF（我们自己搜出来的最佳格） | **111.4 µs** | 2.1% | 6.4% |
| 同批小时的候选池 | **0.134 µs** | **75.9%** | 84.1% |

**差三个数量级。** 切一刀的代价/收益（真 TGF 存活带 Wilson 95% 区间）：

| 阈 | 真 TGF 存活 | 池存活 | 净富集 |
|---|---|---|---|
| `bin ≥ 1 µs` | 97.9% [93.9, 99.3] | 24.1% | 4.1× |
| `bin ≥ 10 µs` | 93.6% [88.2, 96.6] | 15.9% | 5.9× |
| `bin ≥ 50 µs` | 82.1% [75.0, 87.6] | **2.0%** | **41×** |

**这条对 GECAM-A 那 1400 万候选直接有效**：A 星候选 `bin_us` 中位 0.15 µs，与
B 星池的 0.134 µs 几乎一样，**整个池在这个量上与真 TGF 是两个人群**。

**但它不是独立于同戳判据的第二条腿**——两星的亚微秒候选都是同戳簇造出来的，
**窗长阈与同戳判据很可能是同一件事的两个代理，组合起来不会有乘法收益**。
用之前要先把池里 `bin < 1 µs` 那一批的同戳占比 / `f₃` 打出来，别高估判据链的
压缩比。

**并且它只能当离线判据，不能改 `min_duration`**，理由见第 14 条那条适用范围
的补注。

### 31. 十三条跨仪器可迁移的规矩（2026-09-11 立，由本轮的翻车总结）

1. **任何"在否决探测器里有命中 ⇒ 是粒子"的标签，必须先量出确认的真事件在该
   探测器里的响应并按它归一化。未归一化的命中率一律无效。** 同一失效模式已经
   出现三次：GECAM「CPD 命中率 93%」（93% 正是那个本底率下的偶然期望）、
   GECAM「CPD ±10 µs 紧符合」（真 TGF 自己 obs/exp = 7.50）、HXMT ACD 判据
   （对 REP 与 TGF 的响应没有差别）。**符合窗的宽度决定了它量的是什么**：
   ±10 µs 量的是康普顿响应（真 TGF 自己也有），同戳量的是"同一次穿越同时打到
   两种探头"——**同戳这条是物理的，符合窗那条是调出来的。**
2. **任何以绝对计数或计数率表达的阈值，跨星必须换算成"每路"或"相对本底中位"
   的形式。** 代价是量出来的：C 星那条 `CPD 本底率 ≤ 500 c/s`（2 路 = 每路
   250 c/s）直接搬到 B 星（8 路）**会否决 69.3% 的真 TGF**；按每路折算成
   2000 c/s 只否决 0.7%。**三星共用的判据要按这条扫一遍。**
3. **阈值的余量必须逐候选算，而且要在目标人群的亮度档上算，不是在定标样本上
   算。** `mf ≤ 0.3` 在全样本上放行 91.7%，看着安全；按 `NetCounts` 分档，
   **最暗那档只放行 47.4%**，而我们候选的计数中位是 9、比那一档还暗。
4. **拿已发表目录做完备性时，匹配容差要跟着被匹配对象的时长走，而且窗要按
   `dt` 的实际形状取——单报召回没有意义，必须与偶然污染并排。**（2026-09-11 加，
   由第 10 条量出来）三件事：
   * **偏移不是常数。** `dt` 中位 +95.8 µs 但 5–95% 跨 −37.9 .. +1816.5 µs，
     `dt / Duration` 中位 0.737——**目录 UT 是起点，偏移正比于暴发长度**。
     **拿一个全任务通用的毫秒数去平移窗口是错的。**
   * **偶然期望精确正比于匹配窗的总宽度**（三档预言与实测误差 2% 以内）。
     所以召回率单独拿出去一定会被高估：±10 ms 的 96.6% 里有 14.5% 是偶然。
   * **`dt` 单边（83% 为正），对称窗浪费一半宽度。** 换成非对称窗在几乎不掉
     召回的前提下把偶然污染降约 3 倍。**凡是拿目录做完备性的，先检查自己的
     匹配窗是不是对称的。**
5. **时戳量化 `q` 在一颗星内部是分段的，不是常数。** GECAM-B 实测**三段**
   （7.45 / 14.9 / 29.8 ns），根因是 MET 跨 2²⁶ 时 float64 的 ulp 整体跳一档
   （B 星在 2021-02-15T17:21:04，而它 2020-12-10 就出数据，所以跨了三段；
   147 个 TGF 里 4 个落在最细那段）。**任何按 `q` 归一化的量必须逐候选取
   `np.spacing(t0)`**，用全局常数会在跨段的样本上系统性错。**这条对所有拿
   float64 存 MET 的任务都成立**，各任务要自己查任务期跨没跨 2²⁶。
   推论：GECAM-A 那个"89 ns / 119 ns 两条线"**不是两条线**，是同一个约 100 ns
   的固定延迟在 29.8 ns 格子上的投影（最细 q 上落在 96.9 / 104.3 ns 相邻两格）。

6. **位姿坏行要按物理半径带滤，不是按"非零"滤——"非零"不等于"合理"。**
   （2026-09-11 改，原来那条判据实测漏得很厉害）
   GECAM-B 2022-06-15 一天 75,971 行里 **1,565 行（2.06%）是坏的，而精确等于
   零的有 0 行**：1,529 行是反常规格化的浮点垃圾（`X ≈ 4.28e−38`，
   `|r| ≈ 7.5e−38` m，`|r| > 1e6` 挡得住），**但另外 36 行的 `|r|` 落在
   1362 / 1371 / 1533 / 3150 / 3362 / 4290 / 5434 / 8937 / 9060 km，外加一行
   `|r| = 2.49e22` m——这批全部通过了 `|r| > 1e6`。**
   **后果可测**：用 `|r| > 1e6` 过滤，这一天算出 `|地理纬|` 最大 **90.00°**、
   `|磁纬|` 最大 **87.13°**，而 B 星倾角只有 29°。换成物理半径带之后是
   29.13° / 38.44°，与另外三天逐位一致。
   **做法**：好行的 `|r|` 全部落在 6960–6980 km（中位 6969.9、p99 6976.7），
   用 **[6600, 7200] km** 一次挡掉全部 1,565 行。各星换自己的轨道高度。
   > **⚠️ "物理带"本身也会错，如果带的锚点选的是标称值而不是物理极限
   > （2026-09-11，已经踩过一脚）。** 按"这颗星标称轨道高度 500 km，给个 ±"
   > 定带，是**按数据该长什么样**定的；正确的锚点是**"低于多少高度不可能在轨"**。
   > **前者会把真实的演化（衰减、机动、轨道调整）当成坏行。**
   > GECAM-C 按标称高度取 300–900 km 的带，结果
   > **2025-02 整月 100% 被判坏行、2025-01 有 10.18% 被判坏行，而它们全是真的**
   > ——**C 星的轨道在任务期内一路衰减，归档结束是因为卫星在再入**：地心距中位
   > 从 2022Q3 的 6882.2 km（高度 504 km）一路掉到 2025Q1 的 6703.0 km
   > （325 km），最后一天只剩 **266 km**。
   > **好在真假分得很开**：坏行全部落在 `r < 1000 km`，**1000–6000 km 一行都没有**，
   > `> 7278 km` 也一行都没有。所以下沿取 **100 km 高度**（低于此不可能在轨）
   > 就够，不必贴着标称值。
   > **用对的带（100–900 km）重跑之后，C 星那 0.476% 的坏行对结论没有任何影响**：
   > 升交点 1610 → 1609、LTAN 中位 22.547 h、913 天漂 +0.809 h、R = 0.9981，
   > **逐磁纬带的曝光表一格没变**。**"过滤让 LST 腿更不可用"那个说法是用错的带
   > 造出来的假象，已撤回**（砍掉的是 2025 年的真数据）。
   > **但这不证明"过滤没必要"**——不过一遍就不知道那 0.476% 会不会正好落在
   > 要害上，B 星那边 **36 行就把 `|磁纬|` 撑到 87°**。区别只是 C 星的坏行形态
   > 更极端（全在 `r < 1000 km`），更容易被任何合理的带挡住。
   > **配套一条**："地心距应当是一条窄带"这句话**要限定在同一段时间内**——
   > C 星同一季度内 p1–p99 只有 ±20 km（扁率 + 小偏心率，物理的），但**跨两年半
   > 整体下移 180 km**。**拿全任务的散布去定带宽会把真数据切掉。**
   **两种坏行的杀伤方式不同，要分开记**：全零行造的是**假升交点**（首轮因此
   得出 GECAM-B 圆集中度 R = 0.645，真值 0.018）；这 36 行造的是**假高纬点**
   ——**不改升交点，改的是纬度分档的归属**。凡是按磁纬分档的判据（REP 判别、
   高磁纬本底扣除、成串判据）都会被它污染，**而且不报错、看着也不异常**。
7. **"没测到高磁纬"和"去不了高磁纬"是两回事，先查倾角再写限制。**
   （2026-09-11 立）GECAM-B 四天实测 `|地理纬|` 最大 **29.13°**、`|磁纬|` 最大
   **38.4–38.5°**、**`> 40°` 占比 0.0000%**——**倾角 29° 决定了它原理上不可及**，
   不是抽样不够。所以第 22 条那条泊松性结论的限制要写成"B 星去不了高磁纬"，
   写成"只在中低磁纬验过"会误导成"再采样就能补上"。
   **两个推论**：① **GECAM-B 的候选池里原理上不该有 REP 污染**（HXMT 实测 REP
   84% 在高磁纬，而 HXMT 倾角 43°、B 星 29°），这是一条独立论据；② **反过来，
   REP 判据不能拿 B 星定标，B 星的结论也不能外推到倾角更高的仪器**
   （HXMT 43°、SVOM 30°、天格 SSO）。
8. **`met()` 的两步写法是对的，真凶是 `round(t·1e9)·1e-9`。**（2026-09-11 更正）
   `total_seconds() + float("0." + frac)` **一格都不差**（GECAM-A 五天实测
   100.00%）；而 `round(t·1e9)·1e-9` 只有 **51.0 / 63.6 / 65.7 / 66.0 / 89.0%**
   落在正确的 ulp 上，差的**全是 +1 格**（29.8 ns）。**先前把对账缺口归到两步
   写法上是错的**，要改的是后者。
9. **降率缺口用期望个数报，不要用率。**（2026-09-11 立）GECAM-A 这批曝光
   39,061 s × 目标率 6.20e−6 /s = **0.24 个**——**验收线不是"降到几十个"，
   是"降到 0 个量级"**。用率报（"2.74e−2 /s，差 4414 倍"）很容易被读成
   "已经很接近了"。
8b. **判一个修正会不会搅动排序，看的是「修正因子的散布 ÷ 被改量自身的散布」，
   不是修正因子自己跨几个数量级。**（2026-09-11 立，**这条是就地推翻一个
   错推论立的**）
   先前的推论是："SVOM 的 `fa` 改正因子近似常数（中位 6.54）所以形状不动；
   GECAM-A 的跨两个数量级（p10 = 45、p90 = 2.45e4）**所以排序一定会被搅动**。"
   **前半对，后半错。** A 星实测：改正因子跨 **1.6 个数量级**，而 `fa` 自己跨
   **108.4 个数量级**（log₁₀ 的 p1/p50/p99 = −70.9 / −21.0 / +1.2）
   ⇒ **两套 `fa` 的秩相关 0.9997**，前 10 / 100 / 1,000 / 10,000 / 100,000 名
   只换掉 **0.0 / 1.0 / 1.4 / 0.8 / 0.8%**。
   **A 星与 SVOM 的结论碰巧一样（形状基本不动），但理由完全不同，所以两边
   都得各自验，不能互相搬、也不能从"因子是不是常数"直接推。**
   **配套一条口径**：`P(X ≥ k) > P(X > k)` 恒成立 ⇒ 改对后 `fa` **逐点只增**
   （480,241 条实测 100.00%，最大反向偏离恰好 0）⇒ **按固定阈选时各阈集合
   严格套嵌，"进"的那一栏结构上恒为 0**；**按固定名额选才会换人（约 1%）**。
   两句话不矛盾，但报的时候必须说清是哪一种选法。
9b. **"测了是零"与"没测"不是一回事，而很多脚本会把空串当零读。**
   （2026-09-11 立）GECAM-A 的特征表里四列 CPD 计数在 74,012 行上**全是空串
   不是零**——因为 A 星在那段时间**根本没有 CPD 事例产品**（`CPD_evt` 目录
   2025-03 才出现，而 WWLLN 覆盖止于 2024，**两段不相交**）。把空串读成零，
   "没有测量"就被悄悄变成"测到了零"，于是 CPD 判据在 A 星上会给出一个看着
   正常、实际无中生有的结果。
   **同一条原则的另一面**（`gecamC` 的 CPD 空壳判据）：**判据要钉在"零行**且**
   零列"，明确拒绝"零行就算空壳"**——CPD 率只有 GRD 的百分之几，一小时真的
   可以一个事例都没有，那是**测到了零**。
10. **同一个常数有多份副本、而只有一部分过校验，比"到处写死常数"更危险。**
   （2026-09-11 立，见第 21 条那张表）有校验的那份**响亮地失败**、没校验的那份
   **静默给出错结果**，而**两份产物看起来都正常**。"有一半会报错"让人误以为
   校验覆盖到了。**要列表登记：哪个常数有几份副本、各自有没有校验。**
11. **`-mem` 在 `-g hxmt` 上是「及时强制」——越界一两秒内就被 hold，不是轮询
   漏网。**（2026-09-11，**"轮询式软强制"那个机制已撤回**）GECAM-B 三路只差
   `-mem`：16000 / 20000 两路**被 held**（`MemoryUsage` 15965 / 20053）、
   22000 跑完（`VmHWM` 21267 MiB）。1 s 采样的轨迹显示 **m16000 末采 15864 MiB
   而 hold 于 15965**，m20000 末采 19909 而 hold 于 20053——**553 秒的缓慢爬升，
   越界即刻被抓。**
   **`gbm` 那个「`-mem` 1 到 4000 九档全跑完」的反例已经解释掉了：`-mem` 有
   约 2.5 GB 的下限（floor），低于它的申请一律按 floor 走。** 四个判别作业
   （各瞬间分配、稳定持有固定内存 400 s）：

   | `-mem` | 实际占用 | 结果 | `MemoryUsage` |
   |---|---|---|---|
   | 1 | 3000 MiB | **held** | 2517.1 |
   | 500 | 3000 MiB | **held** | 2517.1 |
   | 2000 | 3000 MiB | **held** | 2566.6 |
   | 1 | 600 MiB | 跑完 | — |

   **申请差 2000 倍却都在 2517–2567 被 hold ⇒ 有效上限 = `max(申请, ≈2500 MiB)`。**
   GBM 的用量是 490 MiB、扫的九档是 1..4000，**全部低于 floor，所以一档都
   触发不了——那九个"全跑完"什么也没证明。**
   **所以：「小 `-mem` 没被 hold」不是「不强制」的证据，除非用量本身越过了
   floor。定标 `-mem` 的实验，用量必须跨过 floor 才有分辨力。**
   * **`VmHWM` 与 condor 的 `MemoryUsage` 在平台型作业上一致到 0.7%**
     （GECAM-B 实测），**但在锯齿型作业上差 24%**（GBM 实测，逐天串行、
     每天约 70 s 爬升后释放，99 个采样里只有 17% 超过 300 MB）。
     **所以拿 HWM 算「差百分之几被扣住」这种细账之前，先报一个「峰值持续时间
     占比」**——GECAM-B 那路 43.7% 的采样在峰值 95% 以上，是平台型，细账可以
     按 HWM 算；锯齿型不行。
   * **两边一致且可直接用：峰值不随 `-mem` 变**（GECAM-B 三路 `-mem` 大的那路
     峰值并不更高；GBM 同一天 `-mem 1` 与 `-mem 4000` 峰值逐字节相同）。
   * **操作结论不变：不能靠"没被 hold"推断没超，也不能因为别人没被 hold 就
     放心超额申请。**


### 32. 双增益两支路之间是一个约 100 ns 的固定延迟（已答，2026-09-11）

> **编号说明**：这一条在 GECAM-B 的分支上原编第 21 条，与本文件既有的
> 第 21 条（GECAM-C 的 896 道能量梯）撞号，合并时改编为 32。内容未改。

**这是 τ 合并方案唯一的物理依据，而且是构造无关的测量**：同一个物理光子被
ADC 两个增益支路各写一行（第 17 条），两行的时戳差就是两条支路的相对延迟，
与候选、阈值、任何选样标准都无关。

**关键在于 q 必须按本小时的 MET 量级取，写死会把结论搞反。** 同一颗星、同一套
电子学，三个归档量化步长（第 18 条）给出三对完全不同的"延迟值"：

| GECAM-B 小时 | MET 量级 | q | 时戳差的两条线（占全部对的比例） |
|---|---|---|---|
| 2021-01-20 08 | 6.48e7（< 2²⁶） | **7.45 ns** | **96.9 ns (6.01%)**、**104.3 ns (4.38%)**；89 ns 只剩 0.01% |
| 2021-07-13 05 | 7.98e7 | 14.9 ns | 104.3 (8.44%)、89.4 (3.44%) |
| 2022-06-15 18 | 1.09e8 | 14.9 ns | 104.3 (9.66%)、89.4 (3.93%) |
| 2022-08-30 11 | 1.16e8 | 14.9 ns | 104.3 (9.52%)、89.4 (3.88%) |
| 2024-01-11 03 | 1.59e8（> 2²⁷） | 29.8 ns | 89.4 (8.85%)、**119.2 (4.87%)** |

在最细的格子（q = 7.45 ns）上两条线落在 **96.9 和 104.3 ns，相邻两格**。最简
解释是**底下只有一个约 100 ns 的固定延迟**，float64 存储把它摊到相邻两格上。

**所以 GECAM-A 实测的「89 ns 和 119 ns，3 个和 4 个量化步」不该读成两种延迟**
——那是这同一个约 100 ns 的延迟在 29.8 ns 格子上的投影；B 星在 2024-01-11
（同样 q = 29.8 ns）逐位复现了 89/119。**同一颗星换三个量化步长、三对不同的
表观延迟，而底下只有一个数**——这比任何一颗星单独测到的都硬。

**一条外部旁证**：GECAM-C 的时序标定论文（arXiv 2308.11362）给的探头间相对
定时精度是 **0.1 µs**，与这里量到的约 100 ns 同量级。**它是旁证不是依据**
——量级相符，但那篇量的是探头间、我们量的是同一路的两个增益支路，不是同一
件事，不能拿它替代上面那张表。

**对 τ 合并方案的意义**：τ 的物理尺度是「一个约 100 ns 的支路延迟 + 归档量化」，
**不是「覆盖 89 和 119 两条线」**。按这个图像 τ = 150 ns 刚好覆盖最坏组合
（100 ns 延迟 + 29.8 ns 量化），也解释了为什么 τ 加到 300 ns 不再增加。
**但"τ 的尺度对"只是必要条件**，合并能不能用仍然要过第 22 条那四道门槛。

**顺带修正第 17 条的旧口径**：`e34f560` 的 `dedupe_gain_pairs` 按「同探头同
时戳」并，实测只抓到全部对的 **85.8–88.4%**（B 星 5 个小时、跨三年），漏掉的
11.6–14.2% 全落在这约 100 ns 上——**正好是候选窗尺度，而且因为时戳不同，
同戳类判据完全看不见**。折算到准入后计数是漏并约 2.9%。**用 `e34f560` 产物
算出来的任何同戳量（`multiplet_frac`、`f₂`、`f₃`）都系统性虚高，不能与新
口径（`3c017a2` 的「同探头、死时间内、一高一低」）混用。**


### 33. 离线重算候选窗的正确口径（GECAM-A 实测，2026-09-11）

A 星候选窗 `bin_size_best` 的中位只有 **5 个 ulp（149 ns）**，窗口口径上差一个
格子就整体错位，所以"离线重算的量对不上搜索"在 GECAM 上比别的任务敏感得多。
下面三条都是在 2024-01-11 / 2024-06-02 的 20 万个候选上扫出来的，不是推的。

**（a）窗是事例定义的、两端闭合。** `search_new` 里
`duration = data[cursor+step].time() − data[cursor].time()`、窗内事例数恒等于
`step + 1`，所以报出来的 `start + delay` 就是**首个事例**的时刻、
`+ bin_size_best` 就是**末个事例**的时刻。取半开区间 `[t0, t1)` 时 2024-01-11
的对账只有 **12.79%**（差值全为负、最大 −20）；改成闭区间 `[t0, t1]` 立刻回到
**98.43%**。

**（b）MET 还原只能舍一次，别折成整数纳秒。** 三种写法在同一批候选上的实测
（纯算术，`ga_metvariants.py`，数的是算出来的窗左端落在第几个 ulp）：

| 写法 | 与"只舍一次"落在同一格的比例 |
|---|---|
| 整秒与小数拼成十进制串一次 `float()`（基准） | — |
| `total_seconds() + float("0." + frac)` 两步相加 | **100.00%**（五天全部） |
| `round(t·1e9) · 1e-9` 折成整数纳秒 | **51.0 / 63.6 / 65.7 / 66.0 / 89.0%** |

整数纳秒那一列差的全是 **+1 格**（29.8 ns）——1.6e17 > 2⁵³，前后各舍一次。
2024-01-11 是 65.7%，与先前记的"对账只剩 67.9%"对得上。**两步相加并不出错**，
所以"对账不到 100%"不要先怪 MET 写法（见 (c)）。

**（c）真正的残差来自事例流没跟 Rust 侧对齐，不是时间算术。** `search()` 的
顺序是「`Event::keep` 准入 → **按 GTI 闭区间过滤** → `dedupe_gain_pairs`」，
而 `dedupe_gain_pairs` 保留的是 **`gain_type` 大的那条（低增益）**，不是时间
靠前那条——一对的两条时戳差 0–119 ns，留错一条窗边界就挪一个 ulp。离线脚本
补上这两条之后：

| | 2024-01-11 | 2024-06-02 |
|---|---|---|
| 闭区间 + 只舍一次，但按时间先后去重、不过滤 GTI | 98.43% | — |
| **再按 Rust 规则去重 + 过滤 GTI** | **99.99%**（62,826/62,830） | **100.00%**（139,121/139,124） |

**（d）对账指标要怎么读。** 搜索的窗是**按下标**取的（`count` 个连续事例），
端点上有同戳并列时它只收其中一条，而时间口径会把并列的全收进来，所以
`n_obs == count` 原理上到不了 100%。可解释的范围是
`n_obs − (n_lo − 1) − (n_hi − 1) ≤ count ≤ n_obs`（`n_lo`/`n_hi` 是两端点上
的同戳重数）。**要报的不是"对账 X%"，而是"不一致的那部分有多少能被端点并列
解释、剩几个说不清"**——A 星实测剩 2 / 2 个说不清，都是 ±1。

**（e）顺带修正第 18 条那张表两处。**
* A/B 的 q 变粗日期精确到 **2023-04-03T10:42:08 UTC**（MET = 2²⁷ s），不是 2023-04-01；
* **A 与 B 的分段数不同**：A 的事例产品起于 2022-10-21，已在 2²⁶（2021-02-15T17:21:04）
  之后，所以只有**两段**（14.9 ns → 29.8 ns，分界 2023-04-03）；B 起于 2020-12，
  有**三段**（7.45 / 14.9 / 29.8 ns）。2²⁸ 要到 2027-07-04 才到，本任务期内不会再跳。
* 因此**按 q 归一化的量必须逐候选取 `np.spacing(t0)`**，不能用全任务常数；
  离线脚本里应当硬断言"这一小时没跨档"，跨了就分段。

**（f）跨口径的量不可比，引用时要标口径。** 2026-06-01 那天的 `signals.json`
出自双增益去重修好之前的老二进制（`e34f560`，"同探头同时戳"口径），它的
`count` 系统性偏高，用死时间口径重算时对账只有 **49.27%、差值全为负**。
**那一天整天剔除，不做部分采信。** `multiplet_frac` / `f₂` / `f₃` 这类同戳量
同样跨去重口径不可比。

### 34. `f₃ > 0` 只在长窗上是判据，短窗上零信息（GECAM-A 实测，2026-09-11）

第 14 条与 gecam-c 的第七之六节都把 `f₃`（落在 ≥3 重严格同戳簇里的计数占比）
当作"零参数、零阈值"的粒子判定。A 星上独立复核的结果是：**这句话只在窗长足够
时成立，而"足够"是个能算出来的量，不是约定。**

做法是把判据和**它自己的偶然期望**并排放：窗内 n 个计数落在 `w+1` 个 ulp 格子
里，单格计数 ~ `Binom(n, 1/(w+1))`，于是"落在 ≥3 重格里的计数"的偶然期望是
`(w+1)·Σ_{j≥3} j·P(X=j)`。**这个期望逐候选算**，因为它只取决于该候选自己的
`(n, W, q)`。2023-06-01 + 2024-01-11 + 2024-06-02 共 311,296 个候选：

| 样本 | n | 偶然三重期望和 | 实测三重数 | **实测 / 偶然** |
|---|---|---|---|---|
| 原始池 | 311,296 | 1,506,175 | 1,864,670 | **1.2** |
| 准入上界后 | 116,668 | 876,298 | 989,029 | **1.1** |
| 准入 + `bin ≥ 50 µs` | 5,857 | **0.47** | 14,755 | **31,435** |

**原始池上 `f₃ > 0` 的占比高达 89.2%，但偶然期望本来就那么高（obs/exp = 1.2）
——这个量在那里不携带任何信息。** A 星候选窗中位只有 5 个 ulp，三个计数撞进
同一格是常态而不是异常。窗拉到 50 µs 以上，偶然期望塌到 **0.47 个**（整批合计），
而实测 14,755 个，这时 `f₃ > 0` 才是决定性的。

**推论三条：**
* 任何"`f₃ > 0` ⇒ 粒子"的结论**必须同时报该批候选的窗长分布**，否则无法判断
  它是不是在零信息区。
* 反过来 `f₃ == 0` 当判据时，**在短窗上会误杀真信号**：一个 n = 100、W = 30 µs
  的亮而短的真 TGF，`w+1 ≈ 1007` 格，偶然三重期望并不小，τ = 0 也要误杀相当
  一部分。**要按目标人群的 (n, W) 算误杀率，不能只验"不误杀本底"。**
* 这个"实测值 ÷ 它自己的偶然期望"的格式适用于所有同戳类判据，建议统一采用；
  只报实测占比（"f₃ > 0 占 89%"）是没有意义的。

**与 gecam-c 的"窗 ≥ 10 µs 才能用 f₃"是同一件事**，本条把它从一个经验阈值
变成了可以逐候选算的量。

### 35. A 星幸存候选是"真 TGF 的时长 + 本底的计数"（2026-09-11）

把 `bin_size_best ≥ 50 µs` 这条外部真值定标过的判据（B 星 147 个已发表 TGF：
真 TGF 中位 111.4 µs、这一刀留 82.1%、切掉池的 98.0%）用到 A 星的候选表上，
四天 60,540 s / 480,241 个候选：

| 步骤 | 幸存 | 率 /s | `count` 中位 | `bin` 中位 µs |
|---|---|---|---|---|
| 原始池（已含 `fa ≤ 20`） | 480,241 | 7.933 | 9 | 0.149 |
| + 准入上界后仍 ≥ 8 | 180,499 | 2.981 | 12 | 0.149 |
| + `bin ≥ 50 µs` | 9,241 | 0.1526 | 12 | 97.3 |
| + `f₃ == 0` | 3,650 | 0.0603 | 11 | 99.1 |
| + `f₂ ≤ 0.3` | 1,691 | 2.79e−2 | 12 | 120 |

这一刀在 A 星上砍掉准入池的 **94.88%**（B 星池上 98.0%，同量级，**定标搬得
过来**）。**但要害在这里**：留下来的那批 `bin` 中位 **97.3 µs 已经对上真 TGF
的 111.4 µs**，而 `count` 中位只有 **10，差真 TGF 的 64 六倍多**。被切掉的那批
`count` 最大 34、留下的最大 257——**这一刀分离出了高计数尾巴，没有换掉人群
主体。**

**这把第 6 条（`min_number = 8` 比已发表样本低整整一档）从一句担心变成了一个
量**：A 星幸存候选的形态是「真 TGF 的时长 + 本底的计数」。

**不要因此去抬 `min_number`。** 那和抬 `min_duration` 是同一类错误——改的是
搜索参数，只会让搜索改报一组不同的候选，判别力不会凭空出现（`min_duration`
那条已经实测证伪过）。**正确做法是搜索参数不动、在候选表上按 `count` 事后
过滤**：`count ≥ 30` 留 38 个、`count ≥ 64`（真 TGF 中位）留 11 个。

**另一条次序上的硬约束：`fa` 要放在最后。** 2024-01-11 上先过准入 +
`bin ≥ 10 µs` 之后收紧 `fa`，`f₃ > 0` 的占比从 63.9%（`fa ≤ 20`）单调升到
**80.8%**（`fa ≤ 7e−7`），`bin` 中位从 56.8 µs 掉到 20.3 µs——**`fa` 越严，
粒子占比越高、窗越短**，因为短窗里一簇同戳计数给出的显著度最高。
**先用 `fa` 定阈会选出一个粒子主导的样本**；必须先用 `bin` / `f₃` / `f₂`
清掉粒子，再用 `fa`。清过之后 `fa` 恢复成正常的泊松尾巴（链末端从 20 收到
1e−7 只剩 0.35%，而同一把刀在原始池上只掉 22.1%）。

**验收线建议用期望个数而不是率**：60,540 s × 6.20e−6 /s = **0.38 个**。
按 B 星已发表率这批曝光里连一个真 TGF 都不该有，所以目标是**降到 0 个量级**，
不是"降到几十个"。目前链末端 + `fa ≤ 1e−7` 是 6 个、**离目标 16 倍**。

### 36. 搜索的 p 值差一个：`sf` 报的是 `P(X > count)` 而不是 `P(X ≥ count)`（2026-09-11，**跨仪器**）

`blink_algorithms::snapshot_stepping` 里判触发和记录显著性用的是

```rust
Poisson::new(lambda).unwrap().sf(numbers[group] as u64)
```

statrs 的 `DiscreteCDF::sf(x)` 是 `1 − cdf(x) = P(X > x)`。**观测到 `count` 个
计数时的 p 值应当是 `P(X ≥ count)`**，用 `P(X > count)` 等于把"恰好观测到这么
多"那一项整个扔掉——而在这种极端尾巴上**恰好那一项就是尾巴本身**：
`P(X > k) / P(X ≥ k) ≈ λ/(k+1)`。

**实测（GECAM-A 四天 480,241 个候选，`ga_offbyone.py`）：**
* `signals.json` 里的 `sf` 与 `poisson.sf(count, mean)`（= `P(X > count)`）
  **逐条相等，100.00%**；与 `P(X ≥ count)` 差一个因子。
* 该因子逐候选是 `(count+1)/λ`，实测 **p10 = 45、p50 = 1.16e4、p90 = 2.45e4**，
  与解析式逐位对上（`(count+1)/mean` 的中位同样是 1.16e4）。
* 同时验明 **`fa = sf × 一年秒数 / bin_size_best` 精确成立**（最大偏离 2.2e−16）
  ——所以不需要"反标定一个试验数 T"，先前 an5/an7 里那个 `T ≈ 1.9e10` 是拿错
  了尾巴反解出来的，作废。

**后果分两面，不要只说一面：**
* **对池子大小影响不大**：把 `fa` 改成 `P(X ≥ count)` 之后重新卡 `fa ≤ 20`，
  四天的池 480,241 → **433,644，只掉 9.7%**。因为池子是被污染撑起来的，不是
  被阈值撑起来的——同戳簇给出的显著度本来就低到 1e−26，乘 1e4 也翻不过 20。
* **对逐候选报出来的显著性影响很大**：所有 `fa` 系统性**低了约 4 个数量级**。
  任何"这个候选的 `fa` 是 1e−7"的说法，按正确口径是 1e−3 量级。**判选阈
  （`fa ≤ 1e−5` 之类）跨口径不可比**，引用时要说明用的是哪一个。

**跨阈只有出、没有进，这是结构上必然的**：`P(X ≥ k) > P(X > k)` 恒成立，所以
改对之后 `fa` 逐点只会变大（480,241 个候选实测 100.00%，最大反向偏离恰好 0），
各阈上的集合**严格套嵌**。GECAM-A 四天：`fa ≤ 20` 出 46,597 进 0；`≤ 1e−5`
出 10,890 进 0。**所以要问的不是"进出各多少"，是"有没有已发表 TGF 掉出阈"。**

**但"改正因子跨两个数量级 ⇒ 排序被搅动"这个推论不成立，要比的是改正因子的散布
对被改量自身散布的比例。** A 星的改正因子跨 **1.6 个数量级**，而 `fa` 自己跨
**108.4 个数量级**（log₁₀ 的 p1/p50/p99 = −70.9/−21.0/+1.2）。实测秩相关
**0.9997**，取最显著的前 10/100/1,000/10,000/100,000 个只换掉
**0.0/1.0/1.4/0.8/0.8%**。**按固定阈选是套嵌的，按固定名额选换掉约 1%**
——两句话不矛盾，报的时候要说清楚用哪一种选法。（SVOM 那边改正因子中位只有
6.54、近似常数平移，所以那边"形状不动"；**A 星不是常数，但因为分母散布更大，
结论碰巧相同——这两件事要分别验，不能互相搬。**）

**外部真值这一项在 A 星上判不了，而且是结构性的**：已发表目录只用 GECAM-B、
UT 区间 2021-01-20 .. 2022-08-30，而 A 星第一批事例是 2022-10-21，**晚 52 天，
两个区间不相交**。第 2 项只能在 GECAM-B 上答。

**这是 `blink_algorithms` 的共享代码，四个仪器组都用**，所以改不改要统筹定：
改了会让所有已发表的判选阈失去可比性。**在定案之前，报 `fa` 一律标明口径。**
GECAM-A 这边的做法是两套都报（见 `evidence/gecam-a/candidate-rate.md` 第九节）。
