# 重复计数三查：双增益、跨探头同时戳、重叠候选（2026-09-10）

结论见上两级 `OPEN-QUESTIONS.md`「已核实」的双增益一条，以及第 24、26 条。

- `gain_dup.txt`：6 个整点小时、跨 2024-07 到 2026-02、9700 万个原始事例。
  同探头内 `Δt == 0` 的相邻对为 **0**，一个量化步以内也是 0——SVOM 没有 GECAM 那种
  「一个光子被两个增益支路各写一行」的重复。同时给出两档增益的交接道随时间的漂移
  （ch107 → ch112 → ch134–136）。生成：`scripts/cluster/svom_gain_dup.py`。
- `same_timestamp.txt`：901 个显著候选窗内「属于跨探头同时戳对的事例」占比与本底对比，
  以及用 83 个闪电证实当否决线的阈值扫描。富集 14–16 倍但真 TGF 与其余候选完全重合，
  且被偶然同格 `(n−1)·q/T` 解释干净，**不加判据**。
  生成：`scripts/cluster/svom_sameTS_audit.py`。
- `overlapping_candidates.txt`：同一物理事件被报成多个重叠候选的自查。
  83 个闪电证实零重复；显著 901 → 888（1.4%）；全池 25756 → 25566（0.7%）。
