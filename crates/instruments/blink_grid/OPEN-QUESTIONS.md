# 天格（GRID）搜索：已核实的事实与未决项

搜索管线与仪器无关（`blink_search::search_day` 对 `Instrument` 泛型），天格只提供
自己的 `Chunk`/`Event`。四颗有事例产品的星共用一份实现，用类型参数 `Satellite`
区分。本文记录接入时在归档里**实测核实**过的事实，以及尚未定标、需要在跑数据中
迭代的判据。样本：GRID-04 2022-03-11、GRID-03B 2023-08-12、GRID-07 2024-03-15，
及各星各年的抽查。归档自带的两份《GRID科学数据说明》docx 都是 0 字节，格式全靠
读文件本身。

## 已核实（2026-09-05）

**归档布局。** `/gecamfs/Exchange/GSDC/missions/GRID/{GRID-02,GRID-03B,GRID-04,GRID-07}/`，
866 GB、16 万个文件。每星四个产品目录：

| 目录 | 内容 | 用途 |
|---|---|---|
| `fits7/YYYY/MM/DD/evt_vXX_YY/` | 逐过境事例文件 `Gxx_evt_YYMMDDhhmm_YYMMDDhhmm_vXX_YY.fits` | **搜索输入** |
| `fits8/YYYY/MM/DD/posatt_vXX_YY_ZZ/` | 同一批过境的 1 Hz 位姿，文件名时间段与事例文件一一对应（三天抽查 52 次过境全部配上且盖住 GTI） | 定位、姿态 |
| `fits5/NNNNN_YYMMDD_vXX_YY/` | 逐过境遥测监视（SiPM 电压、温度） | 不用 |
| `fits3/` | 观测计划 | 不用 |
| `source_orbit/` | STK 导出的 10 s 星下点表 | 只到 2023 年，不用 |

小写的 `grid-03b/ grid-04/ grid-07/`（2025-06）各只有 2 个子目录，是零星补交，未接入。

**观测日与曝光。** 事例产品的天数：GRID-02 75（2020-11-14..2021-03-08）、GRID-03B 789
（2022-03-11..2024-08-01）、GRID-04 527（2022-03-11..2024-08-22，2023-03..12 缺）、
GRID-07 195（2024-01-01..07-22）。**天格是逐过境记录的**：一天 1–21 次过境、每次
10–25 分钟，一天合计 0.5–10 小时（抽查 2023-01-01 0.49 h、2023-08-12 6.96 h、
2024-03-15 10.64 h）。多数小时没有文件，账本里记 `missing_data`、曝光为零；有文件
的小时曝光按 GTI 算。

**版本目录。** 一天常有多个 `evt_v*`，高版本过境更全（2023-08-12 的 `v03_00` 只有
1 次过境，`v03_01` 有 21 次；2023-06-06 是 6 对 16）。逐日最高版本全是 `v03_xx`
（没有一天以 v02 为最高）。归档自带的 `latest_evt.csv` 过时：GRID-03B 列 763 天而
实际 789 天，GRID-04 列 313 天而实际 527 天，GRID-07 没有这个文件。所以取字典序
最大的目录，不用 csv。

**事例文件结构。** `EBOUNDS`（97 道，GRID-02 是 98 道）、`GTI`（一行：本次过境起止）、
`EVENTS0..3`（4 个探测器）：`TIME`(D, s)、`PI`(I)、`DEAD_TIME`(B, µs，恒为 15)、
`EVT_TYPE`(B)。三天 52 个文件：探测器内时间零回跳、过境之间零重叠、事例全在 GTI 内。

**时间基准。** 头里 `DATE_REF = '2018-01-01T00:00:00.000'`，`TIME` 即自此起的秒数。
对账：`TIME = 132217726.086` ↔ `DATE_OBS = '2022-03-11T07:08:46.083'`，按 UTC 直接数
1530 天 + 25726 s = 132217726 s，完全一致；2017 年后无新闰秒。

**能道。** v03 是 97 道、5–2052 keV，对数分布（ch1 = 5–9，ch10 = 43–47，ch20 = 107–114，
ch50 = 421–436，ch97 = 1989–2052 keV）；GRID-02 是 98 道、7–2011 keV，同一道号差几个
keV；v02 产品是 200 道，但没有一天以它为最高版本。**阈值按能量定，不按道号。**
实测 v03 的谱从 ch5–7（21–29 keV）才起、ch12（56 keV）达峰，低端没有 GRM 那种噪声
堆，硬件阈值本身就切在 30 keV 附近。

**`EVT_TYPE`。** 实测只有 1 和 2：2 的 `PI` 恒等于最高道 97（中位=p10=p90=97），是
溢出/饱和一类的标记；GRID-03B 一天 6224 个（0.01%），GRID-07 一天 244 万个（4.6%），
且成簇（10 µs 内相邻的占 1.4%）。3 从未出现。`keep()` 只收 1。

**位姿。** `ORBIT_ATTITUDE` 1 Hz：`Q1..Q4`、角速度、`ATT_TYPE`、J2000/WGS84 坐标、
`Latitude/Longitude/Altitude(m)`、`POS_TYPE`。**位置并不总有**：抽查 32 天，
GRID-02 全程、GRID-03B 到 2024-01、GRID-04 到 2024-01 有位置（`POS_TYPE` 1/2）；
**GRID-03B 2024-04、GRID-04 2024-03/05、GRID-07 2024 年除 01-25 外全部 `POS_TYPE=0`，
经纬度和两套坐标全是 NaN，只剩四元数**。`source_orbit` 也不覆盖 2024 年。这些时段
的候选定不了位，只能丢并计数。

**计数率。** 单探测器约 400 c/s，四路合计约 1700 c/s，`DEAD_TIME` 15 µs/事例约合
0.65% 死时间。

**首次真实数据运行（2026-09-05，四星各一天）。** 账本曝光与归档 GTI 合计逐秒相等
（四天差值全为 0.0 s），没有过境的小时记 `missing_data`，GRID-07 的候选全部落入
`dropped_no_ephemeris`（位姿无位置解，符合预期）。但候选数荒谬：GRID-03B 一天 9213 个
（4776 个 fa≤1e-5，最亮 count=351 对 mean=37、fa=1e-199），GRID-07 28624 个，GRID-02
1641 个，GRID-04 455 个——且 99.6% 落在 |lat| ≥ 40°。

**根因：高计数率下读出成帧丢数。** 最亮候选周围 ±30 ms、1 ms 分格的四路计数长这样：

    0 0 0 0 0 0 0 0 0 27 78 52 79 64 0 0 0 17 80 69 74 60 0 0 0 0 0 0 0 0 67 73 71 88 ...

5–16 ms 的密集帧，帧间 3–12 ms 一个事例都没有，四个探测器同步。搜索窗落在帧内，
本底窗横跨帧与空洞，均值被拉低，帧内的普通计数就成了"暴发"。逐秒统计空洞占比
（四路合计连续 3 ms 无事例）对计数率：2–15 kc/s 的秒空洞占比中位 0.0–0.7%，到
辐射带（|lat| > 40°，5–75 kc/s）才成片出现；三天全部候选所在的秒率中位 5–75 kc/s、
空洞占比 40–80%，**率 < 3 kc/s 且无空洞的秒里一个候选都没有**。成帧的起始速率
各星不同：03B 到 15 kc/s 还干净，02 和 04 在 5 kc/s 附近就开始丢。（注意 3 ms
这个空洞定义在 < 1 kc/s 时没有意义——那里 Poisson 本身的间隔就常超过 3 ms，
表里低速率档的"空洞"是统计涨落，不是丢数。）

据此加了**读出空洞否决**（`search.rs` 的 `has_dead_gap`）：候选的本底窗
（±0.5 s，夹在所在 GTI 段内）里最长空段 L 在本地速率 r 下应有的计数 r·L 超过门槛
就否决，计入 `dropped_dead_gap`。

**全量首跑（1586 天）之后又收紧了两处。** 首跑用 r·L > 14（P < 1e-6）、r 取窗内
速率：03B 仍剩 19346 个候选、638 个显著，一半在 |lat| ≥ 40°，最亮的在 13–17 kc/s
的辐射带里——那里的形态是四路同时一个 1 ms 尖峰（86 个对本底 4/ms）后跟几毫秒
稀疏，四路合并后最长空段只有 0.6 ms，空洞判据抓不到；GRID-04 还有 count=8、
mean=0.00、窗内速率 2 c/s 的"候选"——成片丢数里的孤帧，窗内速率本身被压没了，
r·L 永远到不了门槛。改成：(1) r 取窗内速率与整次过境速率中的大者；(2) 门槛降到
r·L > 9.2（P < 1e-4，1 kc/s 时误否决期望约 1%）；(3) 再加一道**读出可信速率上限
5 kc/s**（`RATE_CEILING`，窗内与过境速率取大者）——各星成帧起始速率不同但都不低于
这个量级，而按首跑结果这道门对 |lat| < 40° 的显著候选一个不砍（03B 277/277），
砍掉的只有辐射带。计入 `dropped_high_rate`。

## 未决项

### 1. 低能阈 30 keV 未定标

取在硬件截止之上，等于"不额外砍"。是否需要更高的阈来压本底、或者更低的阈来保
灵敏度，要等第一批候选出来看窗内能谱再定。四颗星、两种道数方案都用能量阈，
跨星一致。

### 2. `EVT_TYPE = 2` 的语义未确认

`PI` 恒为最高道，形态像溢出/饱和标记，且成簇——GRID-07 上占 4.6%，明显多于
03B 的 0.01%，可能与该星的高压或阈值设置有关。目前当作非光子丢弃；若它其实是
"高于量程的真光子"，TGF 的硬谱正好会落进去，那就丢错了。要从天格团队拿定义。

### 3. 2024 年大半时段没有位置解

`POS_TYPE = 0` 的过境搜出的候选全部落入 `dropped_no_ephemeris`。这不是几个小时，
是 03B/04 的 2024-02-09 起、07 的 2024-02-10 起到各自数据结束（2024-07/08）——约五百个
观测日，v8 全量丢了 484/146/135 个候选。归档里没有 TLE，`source_orbit` 的 STK 表止于
2023-12，用户也拿不到别的数据。

**已做（2026-09-06）：从数据本身把轨道找回来。** 三颗星都是太阳同步圆轨道（03B：
倾角 97.36°、升交点地方时 22.48 h、周期 5688 s、高度 508.6 km，连续三天一致），轨道面
由此固定；缺的只是每天的沿轨相位。本底计数率随偶极磁纬陡变（|磁纬| 40° 以上升 4 倍，
SAA 另有特征），`scripts/cluster/grid_orbit_fit.py` 用有位置解的日子定标"率–磁纬"模板，
再对无位置解的日子只用计数率拟合相位与周期（周期以前一天为先验，随阻力慢慢变短）。
方法细节与验证（2026-09-06 定稿）：

- 轨道面：倾角与升交点赤经从位姿的 J2000 矢量（角动量）定。**赤经按时间做多项式**（J2 进动
  近似匀速：03B 实测 +0.997°/天、J2 理论 +1.005°/天，223 天残差 rms 0.003°；07 用任务组 2023
  年的 STK 星历加 2024-01/02 位姿，281 天，+0.998°/天）。早先对"升交点地方时"拟合把太阳时差
  （一年 ±16 min 的季节摆动）也拟了进去，长基线残差 7 min，外推后横轨误差以几 km/天增长。
- 相位：每天用计数率拟合。周期不逐日放开（放开 ±24 s 时相位逐日游走 300–500 s，半年下来
  1/3 的天不自洽），而由最近 20 个接受日的升交点历元对圈数做局部多项式给定；相位只在预测
  ±150 s 内找（模板南北近似对称，放宽会翻到半圈对面）；偏离预测超 120 s 或 loss 超自适应
  门槛的天不接受、不写表（候选按无星历丢，不造假位置），模型继续外推；连续被拒时窗口逐步放宽
  到 600 s 并放开周期重新捕获（2024-04/05 的磁暴让周期几天内掉好几秒，局部模型跟不上）。最后
  再用前后 15 天的局部多项式平滑一遍（全程一条二次曲线会把磁暴前后的天全当离群剔掉）。
- 验证（三周外推，逐日，含平滑）：03B 01-17→02-08 误差中位 47–139 km、最大 ≤ 260 km；07
  01-17→02-04 44–80 km、最大 ≤ 203 km；沿轨相位差 ≤ 16 s，横轨稳定在 37–47 km、不随时间增长。**07 的 02-08/09 位姿本身
  不可信**：那两天计数率 8 kc/s 的峰落在位姿给的磁纬 35–39° 处（正常日同处 500–700 c/s，
  8 kc/s 只出现在磁纬 ≥ 60° 的辐射带），位姿比真实位置错了约 380 s，正是丢位置解前的最后
  两天；拟合与计数率自洽，判定拟合对、位姿错。
- 覆盖（2026-09-06 生产）：03B 158 个无位置日里 84 天有表、04 176 里 62、07 161 里 73。2–4 月
  每月 20–30 天，5 月起稀疏（2024-05 磁暴让周期几天内掉好几秒，跟踪器失锁后重捕获不稳，
  孤立的重捕获不留）。没有表的天候选照旧按无星历丢，账在 `dropped_no_ephemeris`。
- 产物：每次过境 10 s 一行的 `orbit_fit/<星>/<YYYYMMDD>.csv`，Rust 侧位姿无位置解时退回它
  （`io::orbit_fit`，`GRID_ORBIT_FIT_DIR`），计入 `positions_from_orbit_fit`；`smooth_log.csv`
  记每天的 loss、残差、是否保留。工具：`scripts/cluster/grid_orbit_fit.py`（calib / validate /
  validate-range / fit-range / smooth）。
在那之前这些时段只能做判选 A 层、不能做闪电关联。

### 4. 跨午夜的过境

过境文件归到起始那一天。0 时的小时同时看前一天的目录，靠 GTI 判断是否有交集。
抽查的三天里没有跨午夜的过境，这条逻辑还没被真实数据踩过。

### 5. 四元数约定

`Q1..Q4` 哪个是标量部分不知道，`Attitude` 只存三个分量，这里按 SVOM 的做法存
`Q1..Q3`。搜索不用姿态，只是原样带出去；将来做视场判断时要先弄清约定。

### 6. 死时间（GRID-03B）与帧长（02/04/07）：`DEAD_TIME` 列是占位值（2026-09-10 更正）

原先照 `DEAD_TIME` 列写的"15 µs/事例、约 0.65%、量级小"三处都错：该列四颗星恒为 15，
不随计数率、年份、探头变化，是占位值；实测值大得多；而且它不是可以忽略的小修正，
**它是四颗星短硬暴分布差异的主因**（未决项 17）。

实测（逐探头相邻事例 dt 的逐 tick 直方，取"从零跳到峰"的硬边沿；**不能用 min(dt)**，
那会被偶发毛刺带偏）：

| 星 | 硬边沿 | = tick 数 | 众数 | 低于边沿的间隔占比 | 样本 |
|---|---|---|---|---|---|
| GRID-03B | **4.7684 µs** | 20（半高落在 20–21） | — | 4.3×10⁻⁶ | 1.11×10⁸ 个间隔 |
| GRID-02 | **28.3718 µs** | 119 | 120 tick | 2.5×10⁻⁵ | 2.26×10⁷ |
| GRID-07 | **28.3718 µs** | 119 | 120 tick | 1.85×10⁻⁴ | 7.65×10⁸ |
| GRID-04 | **28.6102 µs** | 120 | 120 tick | 6×10⁻⁶ | 8.1×10⁵ |

是硬截止不是软斜坡：GRID-04 det0 的 tick 115–122 计数为 [0,0,0,0,0, 5623, 3005,
5086]；03B 的 tick 19 是 676、tick 20 跳到 90091、tick 21 是 236721 然后进坪。

**02/04/07 的 28.4–28.6 µs 不是"探头死时间"，是"帧长"，两者后果差很远。** 实测机制
（判据见未决项 17）：任一路击中就开一个 28.61 µs 的帧，帧内每路最多贡献 1 个事例，
**全帧共用触发那一击的时戳**，帧结束才能开下一帧。死时间只压单路计数率；帧长同时
压**计数**（每帧每路上限 1 个）、抹掉**帧内时间结构**（全帧一个时戳）、并让**同戳
变成结构性的**。一个 TGF 在 80 µs 里给 20 个光子，03B 记 20 个，GRID-04 最多 4 路 ×
3 帧 = 12 个，而且这 12 个只有 3 个不同时戳。**用"死时间"这个词会让后面的人按逐探头
独立去建模，就漏掉后两项。** GRID-03B 才是真正的逐探头独立死时间。

两个值都精确落在整数 tick 上、比值恰好 20:120 = 1:6，说明是**以 tick 为单位写进
寄存器的配置值**。**03B 已确认 788 天没变过**（1531 次过境、6102 个"过境×探头"、
2.22×10⁹ 个事例，边沿 86.3% 落在 20–21 tick，**≥ 40 tick 的 0 行、≥ 100 tick 的
0 行**；22–32 tick 那 657 行是高计数率下估计量的伪影，不是配置变化）。GRID-04 已扫的
132 天抽样里边沿一直在 120 tick，全量在跑。

本底估计仍没扣死时间/帧长；按 678 c/s 四路合计算，对本底的影响仍然小。对**时长测量**
则有一道下限：T90 短于帧长时四路合起来最多记 4 个计数，`grid_t90.py` 量出的 30 µs
量级 T90 已经贴着 03B 的分辨极限。

### 7. 四个探测器合成一路

同型探测器，与 HXMT 的做法一致。但一颗立方星四面各一个探测器，TGF 来自天底方向
时未必四个都亮，分组是否更灵敏未评估（GBM 那边的经验是同型探测器合组更好）。

### 8. 只跑过单元测试，没跑过真实数据

本文写在第一次真实数据运行之前。三天样本、四颗星的接入全靠读文件核实，
`from_epoch`/`search` 的行为要用真实的一天来验证：小时账本里的 `missing_data`
数、曝光合计是否等于 GTI 合计、`dropped_no_ephemeris` 在 2024 年是否如预期。

### 9. 读出空洞的正解是当作 GTI 缺口

现在的否决只是把落在成帧区的候选丢掉。正解是把空洞当活时间缺口、让本底按真实
活时间归一，那样帧内的计数会对帧内的速率做检验，辐射带里的数据也能搜。但
`search_new` 的活时间原先夹到候选所在的一段 GTI（假设它覆盖整个本底窗），毫秒级
的洞会把本底窗夹到一帧里、统计反而更差。**核心已改成对多段求交集之和（2026-09-06，
`live_length`）**：窗口跨过比自己短的缺口时两侧都算活时间、缺口不算，与窗内计数同
口径；只与一段相交时和原先逐位相同（GRID 4 天、SVOM 1 天、HXMT 1 天逐字节回归全同）。
把成帧丢数的空洞探测成 GTI 缺口、替代 `has_dead_gap` 否决，现在可以做了，但辐射带
不是 TGF 的地方，收益只是账面曝光，留作以后。

### 10. 首跑显著候选的构成：四分之三是穿过整星的带电粒子（判据已加，v3 已跑）

首跑 1586 天、速率门以下的显著候选（fa ≤ 1e-5）共 348 个。逐候选特征（几路探测
器亮、时间戳簇集、能谱、位置）把它们分成截然两群，分界在"最显著一格里同一时间
戳上的事例占比"：

- 261 个占比 > 0.35（中位 0.57，75 个为 1.0）：四路各一个事例、时间戳完全相同
  （跨探测器最小间隔 0），一格里就这四个加零星本底（计数中位 4），谱比本底硬得
  多（PI 中位比 3.8），经度均匀。这是穿过整星的带电粒子，四块 GAGG 同时响应、各
  留一个大沉积。它们 89% 在 |lat| < 40°——不是粒子在低纬更多，而是低纬本底安静
  （约 400 c/s），四个同戳计数才够 fa ≤ 1e-5；高纬同样的粒子淹在本底里。
- 87 个占比 ≤ 0.35：计数在几百微秒里铺开（时长中位 564 µs，计数中位 15），最小
  间隔在 4.77 µs 的探头死时间量级（当时误记为"时间戳分辨率"，实测四星时戳栅格同为
  2⁻²² s = 0.2384 µs，见未决项 6 与 16），谱温和（PI 中位比 1.7），经度集中在
  120°E–180°（海洋大陆雷暴区 43%）。这是 TGF 该有的样子。

两群在占比 0.45–0.50 之间一个候选都没有。判据取 0.35（与 GBM 同），落在
`search.rs` 的 `MAX_SIMULTANEOUS_FRACTION`，计入 `dropped_simultaneous`。四路合成
一路搜索、没有组间符合可用，这道门是天格挡带电粒子的唯一手段。

v3 全量（2026-09-06）：候选 02/03B/04/07 = 51/2380/138/31，显著 4/36/10/3 = 53，
粒子否决 4/7797/15/51。53 个显著候选按时长又分两群：

- 29 个 < 500 µs：本底中位 331 c/s、谱硬（PI 中位比 3.0）、27 个在 |lat| < 40°，经度
  落在海洋大陆、非洲、印度、南美西北——雷暴区。最显著 6 个全在这里（03B，41–175 µs）。
- 24 个 ≥ 500 µs，大多顶在 1 ms 的搜索上限。宽窗轮廓（`grid_profile.py`，±30 ms
  按 1 ms 分格）量出真实时长 3–4 ms、超出 30–95 个计数、100 µs 格里没有尖峰（平滑），
  谱与本底一样（宽窗 PI 中位 14–32 对本底 12–29），四路均分。02/04/07 的显著候选
  几乎全是这一群，地理上全在高磁纬（|lat| 43–53° 的南北太平洋，以及 GRID-04 在
  澳大利亚以南 −28..−39°、经度 100–170°E——地磁南极偏向那边，磁纬同样高）。
  像高磁纬的相对论电子沉降（微暴），不像 TGF。TEB（TGF 的电子束）也会落在高磁纬，
  区分要看磁力线足点附近有没有闪电。**足点检验（v10 全量，2026-09-08 更新）：** 用 IGRF
  把 80 个显著候选的场线追到南北 100 km 足点（`scripts/grid_footpoints.py`），时刻前移
  电子飞行时间（0.95c），近足点用 ±10 ms、远足点用 ±25 ms 跑 `blink wwlln --window-ms`
  （`scripts/grid_footpoint_tables.py`）。80 个全在 WWLLN 覆盖期内。

  | | 近足点 | 远足点 |
  |---|---|---|
  | 短硬暴 (29) | 5 关联（偶然期望 0.05） | 0（0.04） |
  | 毫秒级软暴 (51) | 0（0.02） | 0（0.08） |

  短群近足点的 5 个是阳性对照：低磁纬场线倾斜，近足点离星下点中位 590 km、7 个超出
  800 km 半径，直接关联的 7 个里留下 5 个，说明追踪＋时移这套机制是通的；远足点 0 个
  是阴性对照，挪到共轭半球不会凭空造出关联。

  **注意几何差异，别把两群的近足点当同一个检验。** 软暴在高磁纬，场线接近垂直，近足点
  离星下点中位只有 142 km（最大 651 km，无一超 800 km），所以"软暴近足点 0/51"跟直接
  关联 0/51 几乎是同一件事；软暴真正独立的那条腿是远足点（飞行 12–211 ms、共轭半球）。
  短群近足点飞行 1.5–5.3 ms、远足点 6–96 ms；软暴近足点 1.2–2.8 ms。

  按短群近足点 17% 的关联率（该率已含 WWLLN 的探测效率）推，51 个软暴若是 TEB 应有约
  9 个关联，实测 0，P ≈ 6×10⁻⁵。但这个先验是从低纬雷暴区搬到高纬远洋的，WWLLN 在那里
  效率更低，6×10⁻⁵ 应看作乐观值。**更硬的判据是能谱**：TEB 由 MeV 量级电子加 511 keV
  湮灭线组成，很硬；软暴窗内沉积能量中位 91 keV、本底 78 keV，几乎重合，软成这样不可能
  是 TEB。

  **状态：暂不对外讲，且数字需按 v12 重算（2026-09-10 更新）。** 证据指向"长群不是
  TEB"，但两点还没想干净：近足点对两群不是同一个检验（几何差异见上），以及把低纬的
  关联率当高纬的先验并不严谨。讲图里已撤掉这一栏。**另外上表那组数字是从
  `features_sig_v10.csv` 的 `start` 列取时刻的，而那一列被截到了毫秒（见未决项 16），
  足点追踪和 ±10/±25 ms 的关联窗都带着最多 1 ms 的定位误差——在 ±10 ms 的窗上这是
  10% 的偏移，不能当已验证的结果引用。** `scripts/grid_footpoints.py` 与
  `grid_footpoint_tables.py` 要按 `features_sig_v12.csv` 重跑一遍再谈结论。长群暂按
  "不进 TGF 目录"处理，依据是谱软、时长毫秒级、零闪电关联，不依赖足点检验。
- 计数守恒检验（`grid_conserve.py`）排除了"积压事例重盖时间戳"的读出解释：若是重盖戳，
  ±100 ms 的总计数应等于本底（暴发的超出 = 周围的亏缺）；实测两群 ±100 ms 净超出
  与 ±10 ms 净超出之比中位 1.18（短）与 0.80（长），都是真多出来的计数。长群里 3 个
  例外（04 2022-04-14、07 2024-01-02、03B 2022-05-01）落在本底陡变或 8.7 kc/s 的
  高本底里，±500 ms 亏缺 300 个，不算干净。
- 短群里 2 个不是光子暴：03B 2023-08-08T19:53:50（一路探测器占 100%、PI 6）与
  2023-08-10T09:50:45（一路占 95%），后者同日 09:53:45 还有一个一路占 90% 的长群成员——
  同一天同一路探测器在闹。真暴发从下方照亮四路（03B 其余 27 个 det_frac_max 中位
  0.36）。已加单路占比上限 0.8（四个仪器共用，`blink_algorithms::detector_share`），计入
  `dropped_single_detector`；v4 之后的全量才含这条。

闪电关联（v10 定稿，2026-09-07，WWLLN ±5 ms / 800 km；四颗星全在 2024-12-31 的覆盖内）
把两群分开了：

| 候选 | 覆盖内 | 关联 | 期望偶然 |
|---|---|---|---|
| fa ≤ 1e-5，< 500 µs | 29 | 7（24%） | 0.0 |
| fa ≤ 1e-5，≥ 500 µs | 51 | 0 | 0.0 |
| 1e-5 < fa ≤ 1e-3 | 65 | 1 | 0.0 |
| 1e-3 < fa ≤ 1 | 789 | 3 | 0.2 |
| fa > 1 | 2079 | 0 | 0.4 |

7 个关联的全是 03B 的低纬短硬暴（41–173 µs，|lat| ≤ 21°，偶然概率 ≤ 0.01），包括
最显著的 2022-10-04T00:09:56.543。短群 24% 的关联率对比 SVOM 的 41%；长群 0/51，
与"不是 TGF"一致。这是天格数据里第一批有闪电认证的 TGF。

v10 全量（权威，`gridrun/data`；v3–v9 存 data_v*.tar）：候选 02/03B/04/07 =
55/2632/204/122 = 3013，显著 4/38/21/17 = 80（当时按搜索窗长分成"短硬暴 29、毫秒级
软暴 51"，**这个分法已作废，见未决项 14**）。其中轨道后备
时段（2024-02-08 起）407 个候选、显著 31（短 2、长 29）、关联 0；`positions_from_orbit_fit`
03B/04/07 = 256/61/88，仍无星历 228/88/52。02/04/07 的显著全是高纬毫秒级软暴。图：
`scripts/plot_grid_populations.py`（`scratch_gbm/grid_populations_v10.png`）。

仍未做：

- 探头死时间 4.77 µs（03B；当时误记为"时间戳分辨率"）使一个很亮很短的真 TGF
  （8 个计数里 4 个落在一个戳）也会被否决。按首跑的数字这种损失不超过占比 0.35–0.45 之间的 34 个
  候选。逐个看过（2026-09-06）：全在 03B，核心窗计数中位 7，同戳重数 3 的 20 个、
  4 的 7 个，谱硬（PI 中位比 3.8，与粒子群一样），|lat| < 40° 的 30 个，时长 36–843 µs。
  7 个计数里 3 个共戳，按均匀铺在 50 个戳上算偶然概率约 1%，更像三路同时被粒子打中
  再加零星本底，而不是暴发；其中最显著一格占比 ≤ 0.35 而留在目录里的 4 个
  （2022-11-28T23:13:22、2023-06-22T07:13:26、2023-07-04T17:16:17、2023-11-01T06:59:19）
  都没有闪电。判据不动，但这 4 个不算干净。
- 幸存者的 WWLLN 关联还没跑。闪电库止于 2024-12-31，GRID-03B/04 的 2022–2024 都
  在覆盖内，`blink wwlln --instrument grid03b` 可以直接跑；偶然关联率对照是判断这
  87 个是真是假的下一步。
- 87 个 / 130 曝光日 ≈ 0.67/天，仍比 SVOM（1.25/天，面积大一个量级）显得偏多。
  最像真 TGF 的一例：GRID-03B 2022-10-04T00:09:56.543，79 µs 内 21 个、四路都亮、
  PI 中位 42–89（0.3–1.6 MeV）、本底 412 c/s、(153°E, 12°S) 珊瑚海。

### 11. 成帧丢数的物理起源与各星阈值

是 FPGA 缓冲溢出、还是传输链路的帧丢失，归档里看不出来；`DEAD_TIME` 恒为 15 µs
不反映它。各星起始速率不同（03B 15 kc/s，02/04 约 5 kc/s）可能对应固件版本。
要从天格团队拿读出设计。

### 12. 一个 0 字节的 posatt 文件

GRID-03B 2023-11-20 的 `posatt_v03_00_00/` 里有一个 0 字节文件，首跑时整天 17 个
小时因它记成 `corrupt_data`。已改成位姿文件读不出来就跳过并计入
`posatt_unreadable`；事例文件读不出来仍按 `corrupt_data` 处理。

### 13. 姿态解整段缺失：已把姿态改成可选（2026-09-06 定）

位姿文件的四元数会整段 NaN：GRID-02 2020-11-21 一天 92% 的行没有姿态（成段
154–177 s），GRID-03B 2022-10-01 有 6 段各 74 s。v3 全量里 82 个候选（44 天，3 个
显著：02 2020-11-21T21:32:19、03B 2022-07-18T15:19:25、03B 2022-10-01T12:34:00）
的姿态插成了 NaN，写进 JSON 变成 null，`blink wwlln` 读回时整颗星报错。

现在的处理：姿态表像位置表一样跳过 NaN 行；两张表都只在相邻有效采样相隔不超过
30 s 时插值（`interpolate_sampled`；位姿采样 03B/04/07 为 1 s、GRID-02 为 10 s，3 s
的门槛曾让 GRID-02 的候选全部定不了位），缺失段和过境之间不再拿两头连线——这也堵住了
位置表的一个隐患（位置解按文件整体缺失，混合的一天里候选会被插到两次过境之间）。
这些候选的时间和位置都是好的，只缺姿态，为此丢掉不合理：`Signal`/`UnifiedSignal`
的 `attitude` 已改成 `Option`（缺失时不写进 JSON，旧文件照常读回；全仓库没有下游
读姿态），四个仪器统一为"位置必须有、姿态可缺"，缺姿态的候选照留并计入
`without_attitude`。v7 里被丢的 82 个（显著 3 个）在 v8 起回到目录。

### 14. 分群判据重做：旧的"窗长 < 500 µs"作废（2026-09-10）

**旧判据错在哪。** v3–v10 一直按 `bin_size_best`（搜索窗长）500 µs 把 80 个显著候选
分成"短硬暴"和"毫秒级软暴"。窗长是搜索参数不是时长测量：`SearchConfig.max_duration`
把它顶在 1 ms 上，所有真实时长超过 1 ms 的候选都堆在上限附近，而窗长短也不等于暴发
短。用 `scripts/cluster/grid_t90.py` 实测 T90（±15 ms 内 1 ms 格框出范围，再对扣本底
的累积计数取 5%–95%）后，两组的真实时长是：

| 旧分组 | T90 中位 | T90 全距 |
|---|---|---|
| 窗长 < 500 µs（29 个） | 595 µs | 31–2297 µs |
| 窗长 ≥ 500 µs（51 个） | 2894 µs | 501–8733 µs |

两组重叠。而且旧分组把 GRID-03B 的 9 个候选划进了"软暴"，它们在四个量上与"短硬暴"
没有差别（T90 672 对 595 µs、硬度 3.14 对 2.74、|磁纬| 17.1° 对 14.8°、本底 202 对
302 c/s）。

**新判据：T90 × 偶极磁纬的两个干净角 + 一条中间带。** 数字用 `features_sig_v12.csv`
＋ `t90_v15.csv`（已修未决项 16 的两个缺陷）：

| | 个数 | T90 中位 | 谱硬度中位 | \|偶极磁纬\| 中位 | 本底中位 | 净超出中位 | 闪电关联 |
|---|---|---|---|---|---|---|---|
| A 角 T90 < 2 ms 且 \|mlat\| < 33° | 28 | 533 µs | 4.24 | 13.2° | 236 c/s | 9.9 | **7/28** |
| B 角 T90 ≥ 2 ms 且 \|mlat\| ≥ 33° | 40 | 2963 µs | 1.00 | 45.9° | 1270 c/s | 47.1 | 0/40 |
| 中间带 | 12 | 628 µs | 7.78 | 37.8° | 407 c/s | 9.3 | 0/12 |

谱硬度＝T90 区间内沉积能量中位 ÷ 本底能量中位；能量取该道 `sqrt(E_MIN·E_MAX)`，不用
能道号——四颗星的道—能对应不同（GRID-02 第 1 道 6.66 keV、GRID-03B 5.23 keV），能道
中位数之比跨星不可比。

**这两个角同时也是两台仪器，讲的时候必须一起说。** A 角 28 个全部来自 GRID-03B；
B 角 40 个里 39 个来自另外三星（03B 只占 1 个）；列联表 Fisher 精确检验 p = 2.9×10⁻¹⁸。
中间带 12 个里 9 个是 03B（短而硬、但 |磁纬| 33–45°）、3 个是 GRID-04。也就是说：
**天格四颗星里只有 GRID-03B 探测到了 TGF。** 另外三颗给出的 40 个候选在计数上确实
显著（净超出中位 47 个计数），但谱与本底一致、时长约 3 ms、集中在高磁纬、零闪电
关联——不符合 TGF 的任何一条特征。为什么只有 03B 能探到 TGF 见未决项 17（读出差别）；
B 角那 40 个是什么，见下。

**B 角的"与本底同谱"是测出来的，不是没功效。** 逐候选对暴内事例有放回自举，硬度的
95% 上限中位是 1.25，40 个里 26 个 < 1.5、34 个 < 2.0；同一把尺子量 A 角是 4.24
（95% 上限中位 10.1）。B 角若真有 A 角那样的硬度，这些计数下测得出来。

**"若 B 角是 TGF"的期望关联数。** 按 A 角 7/28 = 25% 的关联率直接外推，40 个应有
10 个关联，实测 0，P(0) = 4.5×10⁻⁵。**这是上限**：B 角在 |磁纬| ≥ 33° 的高纬远洋，
那里闪电本身少、WWLLN 探测效率也更低，同一个 TGF 在 B 角位置上被关联到的概率必然
低于 A 角，所以真实期望小于 10 个。40 个全在 WWLLN 覆盖期内，逐候选的偶然符合概率
合计 0.00。

**B 角是什么：GECAM-C 上的同型事件出现在带电粒子沉降环境里（2026-09-10）。** GECAM-C
上按天格 B 角的形态筛（窗长顶到 1 ms 搜索上限 + |偶极磁纬| ≥ 33° + 谱比 < 1.15）得到
8 个 episode，窗内计数中位 66、12 路探头全亮、|磁纬| 中位 76.9°、谱与本底重合（硬度比
0.715 对本底 0.643）——与天格 B 角同型。GECAM 有天格没有的独立带电粒子探测器（CPD）：
**这些候选出现的时刻，CPD 的本底计数率中位 2884 c/s，是全样本中位 125 c/s 的 23 倍。**
独立的带电粒子探测器在这些时刻计数率高出二十多倍，说明它们**坐在带电粒子环境里**。

三条边界必须一起记：

- **不要引"CPD ±10 µs 符合命中率 93%"这个数**（2026-09-10 由 GECAM 侧自行撤回）。那
  8 个 episode 的窗长顶到 1 ms、所在处 CPD 率 2557–3131 c/s，偶然撞上至少一个 CPD 计数
  的期望本来就是 92.6%–95.6%——**观测/期望 ≈ 1.0，超出为零**，是"长窗 × 高本底"的必然
  结果，不携带信息。正确的论据是上面那条环境率，它不依赖符合也不依赖窗长。
- **CPD 的绝对命中率不可信。** GECAM-C 只有 2 路 CPD 对 12 路 GRD，实测粒子标记效率
  约 14%——约 86% 的粒子 CPD 认不出来。CPD 的数只在相对比较里有意义，**别把这条读成
  高确定性的判决**。
- **只对亮端成立，样本也小。** 去掉亮度条件后（只要谱比 < 1.15 且 |磁纬| ≥ 33°）得到
  271 个 episode，CPD 命中率只有 20%（低本底环境 10%）——暗端候选窗短、环境率也低，
  CPD 分不开。天格 B 角净超出计数中位 47、5% 分位约 18，整支落在 GECAM 能判的亮端里，
  所以适用；**B 角若往暗端扩，扩出来的部分不能沿用这个结论**。样本是 GECAM-C
  2023-06-15 一天的 8 个 episode，30 天跑完会重出。

**"B 角是慢读出星特有伪信号"这条解释已排除**：GECAM-C 的探测器材料（LaBr₃ 对 GAGG）、
读出、死时间与天格完全无关，同形态照样出现。

**天格自己也有一条判定性的独立证据：B 角的候选是一列，不是一个。** 在候选前后 ±30 s
里按 3 ms 分格、本底取 ±1 s 局部均值，数除自己以外逐格泊松 p < 10⁻⁴ 的尖峰：

| | 个数 | 其他尖峰／分钟（中位） | 有 ≥1 个 | 尖峰总数 |
|---|---|---|---|---|
| GRID-07 的 17 个 B 角候选 | 17 | **182.0**（10–508） | 17/17 | 3308 |
| GRID-03B 的 29 个短硬暴 | 29 | **4.0** | 25/29 | 103 |

纯泊松在该本底下每分钟只该给约 2 个假尖峰（1500 c/s、3 ms 格 µ = 4.5、60 s 里 2 万格
× 10⁻⁴），182 超出约 90 倍，是真实成团。**TGF 是孤立事件；B 角的每一个都坐在一串同类
毫秒尖峰的中间，搜索挑出来的只是那串里最显著的一格。** 加上高磁纬集中不是曝光选择效应
（GRID-07 可定位曝光里 |磁纬| > 40° 只占 23.4%，而 16/17 在 > 40°，期望 4.0、
**P = 1.1×10⁻⁹**；且高磁纬本底更高、探测更难不是更容易），定性为**磁层沉降电子微暴**。

**这条同时作废一个旧结论**：早先按"10 分钟内的邻居数"量过成串性、得到"软暴不成串
（中位 0）"，据此削弱过微暴假说。**尺度选错了——10 分钟太粗，该看的是 ±30 s 到亚秒。**
GECAM 那边的 `neighbors_10min` 在 GECAM-C 上同样零分辨力，根因也是尺度不匹配。**成串
判据的时间尺度必须按仪器、按人群选，10 分钟不是通用尺度。**

具体到天格：v10 全部 3013 个候选的 `neighbors_10min` **中位 0、均值 0.36、最大 5**，
四颗星都一样，`is_train` **一个都没有**。`blink_wwlln` 新的按池缩放阈
`max(34, round(3.78 × 池中位))` 在天格四星池上算出来仍是 **34**（逐星与合池都是），
所以那次改动对天格的目录**零影响**。但反过来说，**这道判据在天格上什么也没测到**——
同一批候选在 ±30 s / 3 ms 的尺度上每分钟有中位 182 个同类尖峰。

**没验成**：没做 Kp/Dst 的定量关联，"按日聚集 ↔ 地磁扰动"目前只是形态上像
（GRID-07 的 17 个出在 15 个日期，2024-03-03 一天 3 个，3 月一个月占 7/17）。

**一条不依赖 CPD 的外部参照，但两边不是同一把尺子。** 已发表的 GECAM-B TGF 目录
（Zhao et al. 2023 GRL，arXiv 2306.10255，Zenodo 10.5281/zenodo.8028217，147 个）：
`Duration_us` 中位 179.5、5–95% 35.7–372、最大 670.9，100% 短于 1 ms；`NetCounts`
中位 66.9、5–95% 21.2–185、最小 8.7。

**必须先说清楚尺子不同**：他们的 `Duration_us` 是 **Bayesian Block** 的块长，不是 T90，
两个量在低计数下的偏差机制也不同（BB 会把整块并成一块，T90 被首末事例夹住）。所以：

- **可以用来否定 B 角**：B 角 T90 中位 2963 µs、净超出中位 47 个计数，亮度与他们的
  `NetCounts` 中位 66.9 同量级，而时长是他们最大值的 4.4 倍——两把尺子的差别撑不起
  4.4 倍。B 角在这 147 个真 TGF 里没有对应物。
- **不能用来肯定 A 角**。A 角 T90 中位 533 µs 已在他们的 95% 分位（372 µs）之上，而且
  **A 角的净超出中位只有 9.9 个计数，他们是 66.9**——差 7 倍，尺子和亮度两条都不可比。
  在做完"把他们的暴降采样到 10 个计数、再用我们自己的 T90 定义重量一遍"这个检验之前，
  **不要写"天格 A 角的时长与已发表 TGF 一致"**，也不要写"天格 A 角比已发表 TGF 长"。

**谱硬度在低计数下不能用作逐候选的判据。** 零假设（暴内谱与本底相同）下，从本底能谱
里抽同样多的事例算硬度，95% 分位随计数急剧变化：

| T90 内事例数 | 零假设硬度 95% 分位 | 观测硬度中位 |
|---|---|---|
| 7–9（17 个） | 2.74 | 7.78 |
| 10–14（15 个） | 2.25 | 3.98 |
| 15–25（7 个） | 1.93 | 2.59 |
| 26–60（30 个） | 1.43 | 1.04 |
| 61–200（11 个） | 1.23 | 0.92 |

零假设的中位数在各档都是 1.00，没有小样本偏置——**低计数处的问题是散布，不是偏移**。

**把富集定量归因到噪声**（`null_p_at_1p6` = 零假设下硬度越过 1.6 的概率，对全表求和）：

| T90 内事例数 | 实测硬度 ≥ 1.6 | 该档候选数 | 纯噪声期望个数 |
|---|---|---|---|
| 7–9 | 14 | 17 | 3.50 |
| 10–14 | 14 | 15 | 2.38 |
| 15–25 | 6 | 7 | 0.73 |
| 26–60 | 4 | 30 | 0.95 |
| 61–200 | 1 | 11 | 0.04 |
| **合计** | **39** | **80** | **7.6** |

**所以这一刀不是纯噪声**：实测 39 个超阈，纯噪声只解释得掉 7.6 个（19%）。但**在低计数档
里每四个"硬"候选就有一个是噪声**（7–9 计数档 14 个超阈、噪声期望 3.5），而且判据在两个
方向上都会出错。最直接的反证是**7 个闪电证实的真 TGF 里有 3 个的硬度在统计上不显著**
（单侧 p = 0.235 / 0.109 / 0.082）——判据连已知真值都认不出来，就不该拿它逐个切别人。

结论：**硬度可以在群一级用**（A 角合并 p = 4.7×10⁻⁴³），**逐候选用不行**；真要逐候选
用，必须带自举区间和零假设分位一起报。`t90_v15.csv` 里 `hard_e_lo/hi/hard_e_95`、
`null_p50/null_p95/null_p/null_p_at_1p6`、`ks_p` 就是为这件事留的。

（方法上照 SVOM 那边否决同戳判据的三条：(a) 拿真值扫阈值、(b) 把富集定量归因到噪声、
(c) 确认真值与其余候选在该量上重合。上面 (a)(b) 都做了；(c) 在天格不成立——真值确实
比其余候选硬，所以天格是"**群一级可用、逐候选不可用**"，不是 SVOM 那种"整个判死"。）

**A 角里还混着带电粒子，用 f₃ 挑得出来。** 现行的同戳判据只数"最长的一串"，拦不住
"几百微秒里重复出现 2–4 次三重同戳"这种形态（每一串都不到 0.35）。加一个 f₃ = 落在任意
≥3 重同戳簇里的计数占比（03B 的 38 个显著候选，从暴内事例流算）：

| | f₃ > 0 | f₃ ≥ 0.5 |
|---|---|---|
| A 角（28 个，全是 03B） | 8 | 4 |
| 中间带里的 03B（9 个） | 4 | 4 |
| **7 个闪电证实** | **0** | **0** |
| 本底对照（17500 次抽样） | 7.0% | — |

**7 个闪电证实的 f₃ 全是 0，一个例外都没有**——这个量不误伤真 TGF。去掉 f₃ > 0 之后
A 角剩 20 个干净的（T90 中位 612 µs、硬度中位 3.57、7 个闪电证实全在里面）。f₃ 只用作
**污染标记**不用作分群轴：另外三星是共帧读出，时戳本来就塌在帧上，f₃ 在那里含义不同，
要单独论证。

**f₃ 的富集不是偶然符合**（三步法的第 (b) 步，`diag/lead/` 逐候选算）。n 个事例散在
时长 T 上、量化步 q 时，二重同戳的偶然期望是 (n−1)·q/T，**三重是它的平方量级**——
`Σ_{k≥3} k·m·C(n,k)p^k(1−p)^{n−k} / n`，`m = T/q`、`p = 1/m`：

| | 偶然期望 | 实测 | 实测 / 偶然 |
|---|---|---|---|
| f₂（≥2 重） | 中位 0.0113 | 中位 0.427 | 24× |
| **f₃（≥3 重）** | **中位 5.6×10⁻⁵、最大 2.1×10⁻³** | 非零的 12/38，中位 0.33 | **最小 1.1×10⁴、中位 7.2×10⁴** |

**f₃ 的富集比偶然高四到五个数量级**，不可能是量化偶然造出来的。7 个闪电证实的 f₃ 偶然
期望在 1×10⁻⁴–2×10⁻³ 之间，实测全 0——完全符合偶然，即"它们确实没有三重同戳"。这就是
现行判据（"最长的一串"）为什么会漏掉那批粒子：**它们是几百微秒里重复 2–4 次三四路同戳，
不是一次大同戳，最长串除以总计数当然过不了 0.35**。"最长的一串"这个统计量对**重复发生**
的粒子事件天然失明，f₃ 不失明，而且三重的偶然基底被平方压掉了，不会像 f₂ 那样被偶然
符合淹掉。

**污染范围**：29 个短硬暴里有 6 个 f₃ > 0（2022-11-28、2023-03-30、2023-06-02、
2023-06-22、2023-07-04、2023-11-18），9 个"软暴"里有 4 个——**天格 TGF 样本的六分之一
带粒子签名**。7 个闪电认证不受影响。所以"29 个短硬暴"这个数要改口径，用到它的地方
（讲图、A 角计数、Fisher 检验）都按 f₃ 标记后的干净子集重报。

**谱硬度还受量程上限堆积污染（未决项 15），但 A/B 的谱差别经得起扣。** 把 1.2 MeV 以上
的事例从暴内和本底里同时扣掉再算（`t90_v15.csv` 的 `hardness_e_cut`）：

| | 硬度中位 | 扣后 | 暴内 > 1.2 MeV 占比中位 | 本底占比中位 |
|---|---|---|---|---|
| A 角 | 4.24 | **2.09** | 0.306 | 0.091 |
| B 角 | 1.00 | **1.08** | 0.000 | 0.031 |
| 中间带 | 7.78 | **1.88** | 0.438 | 0.118 |

A/B 两角的硬度差别仍然在（Mann–Whitney p 从 1.1×10⁻⁹ 变成 2.9×10⁻⁴），**但 A 角硬度的
量级有一半以上靠这些事例撑着**，逐候选的显著性也从 21/28 掉到 11/28。所以"A 角谱比 B 角
硬"这个**方向**稳，"硬度 4.24"这个**数值**不能当定论。注意扣掉的不全是堆积——真暴本来
就有 > 1 MeV 光子，本底里那一区也占 3%–9%——所以扣后的值是硬度的**下限**。

讲图（`scripts/plot_grid_talk.py`）已按新分组重画，三张图分别是：T90 × 磁纬的两个角、
T90 × 硬度、硬度的噪声底。

### 15. 逐探头量程上限：超量程沉积以硬光子身份进了搜索（2026-09-10）

三颗星上分别实测到同一个现象：**每个探头有自己的满量程位置，位置之下有一个堆积包、
之上基本为零**，而 `Event::keep` 只把最高道（`pi >= n_channels`）当溢出道，所以这些
超量程沉积全部以 1.2–1.9 MeV 硬光子的身份进了搜索、也进了谱硬度。

| 星 | 满量程位置（逐探头） | 峰道单道占该探头事例 |
|---|---|---|
| GRID-03B | det3 ch83–85、det0 ch84–87、det1 ch88–93、det2 ch88–94（1.24–1.81 MeV） | 0.01%–7.3% |
| GRID-07 | det0 ≈ ch89、det1 ≈ ch91、det2 ≈ ch95（约 1.5 MeV 起） | 最高 7.1%（det0 ch88） |
| GRID-04（2022 段） | det0 ch90、det1 ch89、det2 ch92、det3 ch90（1.55–1.70 MeV） | 0.31%–0.84% |
| GRID-04（2024 段） | **没有堆积包**，ch90–97 是平的（每道约 0.5%），一直平到 ch97 | — |

三条性质：**位置逐探头不同**、**逐次过境漂移**（03B 同一天两次过境，det1 的 ch86–92
占比从 8.2% 掉到 0.5%；GRID-07 同一探头峰从 ch88 漂到 ch92）、**上限之上陡降**。真伽马
线不会这样，判断是满量程堆积。GRID-04 的 2024 段看着像增益掉了、堆积被推到 ch97 以上
（进溢出道被 `keep` 丢掉），高道剩一条平尾——这也解释了 GRID-04 那 21 个候选的硬度
中位 1.08 不是堆积撑出来的（21 个里 18 个在 2024 段）。

**影响已量化**（见未决项 14 末尾）：扣掉 1.2 MeV 以上后 A 角硬度中位 4.24 → 2.09，逐候选
显著的从 21/28 掉到 11/28。**方向不变，量级要打折。**

**怎么改还没定。** 上限逐探头不同且随过境漂移，写不成常数。两个方案：(a) 按探头按过境
从数据里估陡降位置——正确但复杂，而且低计数过境估不准；(b) 取所有已知上限里最低的那个
当统一截止——简单、损失一点高能统计、但不会把假高能放进来。**倾向 (b) 作为第一版**：
判据宁可保守，别为了多留统计量引入不可控的偏差。改 `Event::keep` 要重跑全量，所以先只
在诊断脚本里做（`grid_t90.py` 的 `OVER_RANGE_KEV = 1200.0` 就是 (b)），搜索本身不动。

**"上限之上是不是零"这个形状判据在暴内给不出信息**（03B 实测，逐候选取所在过境定
"包峰 P"与"上限 R"）：

| | 包内 [P−2, R) | 上限之上 [R, 96] |
|---|---|---|
| 本底（整次过境，38 个的中位） | 7.93% | **0.047%** |
| 暴内（最佳格合计 408 个事例） | **35.0%** | **0.98%** |
| ├ 7 个闪电认证（85 个） | 27.1% | 1.18% |
| └ 9 个软暴（101 个） | 42.6% | 0.99% |

本底里上限之上只有 0.047%，说明上限是真的；**但暴内上限之上也只有 0.98%**——任何真的
> 1.8 MeV 光子本来也只会被记进包里、不会出现在上限之上，所以**没法用形状把"堆积"和
"真高能"分开**。

因此 `hardness_e_cut` 是硬度的**紧下限**，不是修正值：暴内包内占比 35.0% 对本底 7.9%
是 **4.4 倍富集**（7 个闪电认证 27.1%，3.4 倍），而一个 RREA 谱的 TGF 本来就该有四分
之一到三分之一的计数在 1.3 MeV 以上——**这个富集不能当污染证据**。所以结论要写成"硬度
下限"，不能写成"修正后的硬度"；那个扣后掉到 0.87 的闪电认证候选也不代表它变软了。
一个副产品：**包内占比与 f₃ 同向**（包内占比最高的 10 个里 0 个是闪电认证，全是判为
粒子的那批，0.50–0.78；7 个闪电认证中位 0.27），两个量可以互相印证，但都不单独够用。

**没验成**：堆积的成因没跟天格团队核实过（缺读出/标定文档）；`EVT_TYPE == 2` 与量程上限
不是同一件事（GRID-07 上 det2 量程最高但 type2 最高、det0 反之）。

**一条没对齐的分歧，记录在案。** 本底谱低能端（< 30 keV 占比）：grid03b 在 03B 24 个
过境上量到安静过境 0.07%、含辐射带过境 9.03%，谱峰在 ch13–14（61–68 keV）；grid02 报
03B 是 23.6%–44.1%、峰在 ch6–7；grid07 先前报 56%–76% 后自行撤回、复核后说安静过境下
07 与 03B 低能端相同、峰都在 ch12–15。差别应该在过境选取（低纬安静 vs 含辐射带），但
**没有对到一起**，而低能端占比直接关系到能阈判据，不能一直挂着。三方要按同一套过境
选取重算一次。目前可确定的是能阈本身不是差别来源：29 个短硬暴的核心事例只有 3.6% 低于
50 keV（7 个闪电认证 2.4%），把阈从 30 抬到 50 keV 只拿掉 2%–4% 的暴内计数。

**跨仪器**：GECAM 的溢出块按 EBOUNDS 里静态的 `ch >= 448` 切，而 GECAM-A 实测
`PI >= 448` 的占比逐日在 0.56%–19.38% 之间摆动且与标定新旧无关——同一个征象，那边已在查。

### 16. 时刻被截断两次、事例准入比搜索松（已修，2026-09-10）

对账口径：候选的最佳格是 `t0 = start + delay`、`t1 = t0 + bin_size_best`，窗内计数
应当等于 `count`（`Candidate::merge` 会把 `stop` 拉长，所以不能拿 `[start, stop]` 对账）。
`grid_features.py` 用的一直是最佳格，但对账只有 6/80 命中。逐步修：

| 版本 | 改动 | `n_core − count` 分布 | 一致 |
|---|---|---|---|
| v10 | 原样 | −4:7, −3:3, −2:5, −1:57, 0:6, +1:1, +2:1 | 6/80 |
| v11 | 修 `met()` 的纳秒→微秒截断 | 0:70, +1:7, +2:2, +3:1 | 70/80 |
| v12 | 事例准入对齐 `Event::keep` | 0:80 | **80/80** |

**缺陷一：`met()` 把小数秒截到微秒。** 写法是 `(frac + "000000")[:6]`。搜索产物的
时刻带纳秒，候选窗两端都是事例本身的时刻，窗口整体左移不到 1 µs 就把落在窗末端的
那个事例挤出去（同戳的几个一起丢，所以有 −2/−3/−4）。修法是整秒交给 `datetime`、
小数部分按浮点单独加。仓库里 `grid_features.py`、`grid_t90.py`、`grid_profile.py`、
`grid_lightcurve.py`、`grid_gaps.py`、`grid_conserve.py` 六个脚本都有，已全修。

**缺陷二：`grid_features.py` 写 CSV 时把时刻截到毫秒**（`c["start"][:23]`）。这一条
比缺陷一严重三个数量级，而且当时没人注意到：下游 `grid_t90.py` / `grid_profile.py` /
`grid_conserve.py` / `grid_lightcurve.py` / `grid_footpoints.py` 全都从这一列重新定位
窗口，等于 1 ms 的定位误差。实测后果——T90 有 65/80 变了，相对变化中位 0.8%、p90 23%、
最大 +258% 与 −92%；一个候选的谱硬度从 13.00 掉到 1.25。`start` 列现在写全精度；要和
旧表对齐取 `start[:23]` 即可。

**缺陷三：Python 侧只筛 `EVT_TYPE == 1`，比搜索松。** `Event::keep` 还要求道号在
`[1, n_channels)` 内（最高道是溢出道）且该道下限能量 ≥ 30 keV。松了之后窗内计数偏大
（v11 残留的 +1/+2/+3 就是这个），而且**谱硬度把 30 keV 以下的道和溢出道也算进了
中位数**。`grid_features.py`、`grid_t90.py`、`grid_lightcurve.py` 已对齐；
`grid_profile.py`、`grid_conserve.py` 只用计数不用谱，暂未改，用它们的结论时注意口径。

**受影响、需要重跑的下游**：未决项 10 里的足点检验数字、`grid_profile.py` 的宽窗轮廓
（"真实时长 3–4 ms"是从截到毫秒的时刻量的）、`grid_conserve.py` 的守恒检验。T90 与
特征表已经重跑（`t90_v15.csv` / `features_sig_v12.csv`）。

### 17. 只有 GRID-03B 能探测 TGF：读出差别，不是曝光差别（2026-09-10）

29 个短硬暴与 7 个闪电证实的 TGF 全部来自 GRID-03B，另外三颗一个都没有。**这条反常
已经解释掉了，根因是读出，不是曝光。** 原先的"GRID-04 期望 19.4、实测 0、P = 4×10⁻⁹"
里，19.4 那个期望隐含"两颗星探测能力相同"，而这个前提实测不成立。

**(1) 时戳栅格四星完全相同。** `TIME × 2²²` 的小数部分**严格为 0**（四颗星各取文件、
80 万–250 万事例、零例外；grid07 在 10 过境 × 50 份表上复核，最大残差 0.000e+00）。
tick = 2⁻²² s = 0.238419 µs。这不是 float64 的精度极限：该 MET 量级（0.9–2.0×10⁸ s）
的 ulp 是 2⁻²⁶ 与 2⁻²⁵，栅格比 ulp 粗 16 倍与 8 倍，所以是真的仪器/编码量化。
以前记的"时间戳分辨率 4.77 µs"是错的——那是 03B 的探头死时间（正好 20 个 tick）。

**(2) 探头死时间差 6 倍**：03B 4.7684 µs（20 tick），02/04/07 都是 28.6102 µs
（120 tick），见未决项 6。两个值都精确落在整数 tick 上、比值恰好 1:6，说明它是**以
tick 为单位写进寄存器的配置值**，不是电路固有常数。GRID-04 已扫的 132 天抽样里边沿
一直在 120 tick，没有任何一段被设成 20；03B 三年的半高 tick 集合都是 {20,21}。
逐过境的全量序列在跑，**如果发现某颗星中途改过，结论的性质会从"仪器固有差别"变成
"运行配置差别"**（后者在后续任务里可以改正），所以这条一定要跑完。

**(3) 更要紧的是读出结构不同，不只是死时间数值不同。** 判据是跨探头间隔的分布形态
（GRID-04 2022-04-14 与 GRID-03B 2022-10-04）：

| | GRID-04 | GRID-02 | GRID-03B |
|---|---|---|---|
| 跨探头间隔落在开区间 (0, τ) | 4.3×10⁻⁴（1034/1060 在 tick 119） | **4.8×10⁻⁶**（102/2.11×10⁷） | 34.4%（连续铺满） |
| 四路独立模型的预期 | 5.5×10⁻² —— **差 130 倍** | 1.9×10⁻² —— **差 3900 倍** | 符合 |
| 跨探头 dt == 0 | 3.9×10⁻² | 1.4×10⁻² | 1.4×10⁻² |

GRID-02 那一列还做了算子自检：把共帧算子施加在 **GRID-02 自己的真实本底流**上**近乎
恒等**（丢弃中位 0.00000、最大 0.00167，时戳被改 0.00000），施加在 03B 真实流上则丢
0.14%、改 1.1% 的时戳——**这个算子就是 GRID-02 数据本身的结构，不是拟合出来的。**

GRID-04 的跨探头间隔要么恰好 0、要么 ≥ 28.610 µs，中间是空的；而时戳相位对 τ 取模
是均匀的（不是全局时钟帧）。唯一解释：**任一路击中就开一个 28.610 µs 的读出帧，帧内
每路最多贡献 1 个事例，全帧共用触发那一击的时戳，帧结束才能开下一帧。** GRID-03B 则是
四路各自独立的 4.768 µs 死时间。用模型生成本底反过来量同样的统计，共帧模型对上 04
（输出率 1971 对实测 1959、dt==0 为 0.041 对 0.039），独立模型对上 03B。

这条的后果比计数上限更严重：**`MAX_SIMULTANEOUS_FRACTION = 0.35` 那道"同戳即粒子"的
门，在 03B 上量的是物理（穿星粒子），在 02/04/07 上量的是读出。** 帧内四路的时戳全一样，
8 个计数挤在 2 帧里就是 4/8 = 0.5 > 0.35 直接否决——**慢读出星的读出会把真 TGF 改造成
粒子的样子**。实测：03B 那个最像真 TGF 的 2022-10-04（79 µs 里 21 个、有闪电认证），
放到 GRID-04 的读出上 76% 的试验被同戳门否掉（详见 (5)）。

**(4) 本底率的差别没有原先以为的大。** 原先的"1300 对 292 c/s"混进了"02/04/07 的候选
全在高磁纬"这个选择效应。统一口径（|偶极磁纬| < 30°、`Event::keep` 三条准入、四路合计、
逐秒或 10 s 分格取中位）实测：

| 星 | 低磁纬本底中位 | 四分位 |
|---|---|---|
| GRID-03B | 239–339 c/s | 216–531 |
| GRID-07 | 528 c/s | 485–657 |
| GRID-02 | 678 c/s | 457–2682 |
| GRID-04 | 678 c/s | 536–943 |

只差 2 倍左右，不是 4.5 倍。另一条形态差别值得记：**03B 的本底不只低，还散**（四分位
宽是中位的 93%，07 只有 33%），下四分位低到 216 c/s——短暴正是在这些更安静的时段过阈。

**(5) 注入检验：四份独立实现，结论一致。** 做法是把 03B 的真实短硬暴事例流按目标星的
读出模型重采样、叠该星的真实低磁纬本底、跑全套判据（`count ≥ 8` / `fa ≤ 1e-5` /
同戳 ≤ 0.35 / 单路占比 / 速率门 / 空洞门），相位随机化 200–2000 次。因子设计把读出与
本底两个效应拆开（每格是"存活 ÷ 对照"）：

| 组合 | GRID-02（共帧模型） | GRID-04（共帧模型） | GRID-07（逐探头模型） | GRID-03B 自查（共帧模型） |
|---|---|---|---|---|
| 对照：03B 读出 + 03B 本底 | 0.579（绝对值） | 22.4/29 | 28.0/29 | 38/38 |
| 只换读出 | 0.189 | 0.26 | 0.483 | — |
| 只换本底 | 0.912 | 0.73 | 0.660 | — |
| **都换（折扣因子）** | **0.181** | **0.168** | **0.277** | **0.138**（r = 678） |
| 对照实验：同样帧长但假设四路独立 | 0.347 | — | — | — |

最后一行是 GRID-02 做的对照实验，直接量出"时戳塌成一个值"这件事本身的代价：**同样的
28.37 µs，四路独立时折扣 0.347，共帧再压掉一半到 0.181**。两种模型的失败途径也完全不同
——共帧下同戳否决占 48.4%、无候选 21.7%、空洞门 13.1%、fa 不够 6.3%；四路独立下同戳只
占 1.5%、无候选 33.3%、fa 不够 33.6%。**GRID-07 那一栏用的还是逐探头独立模型，所以
0.277 偏高**，换成共帧后会更低（07 的共帧判定还没做）。

对照那一格不是 1.0 是正常的：29 个里有一批 fa 就在 1e-5 边上（最弱 9.2×10⁻⁶），重抽
一次自然只回来一部分——所以该用的是**都换 ÷ 对照**，不是都换 ÷ 29。**四套实现都给出
读出是主因、本底是次因**，而且合起来比两者相乘更低（读出把计数压到 8 附近之后，高一点
的本底就足以让 fa 够不着）。GRID-02 的失败原因分解：无候选（计数压到 `min_number = 8`
以下）33.6%、fa 不够 33.2%、空洞门 11.0%、同戳门 1.7%。

**共帧星上还有第三个机制：同戳门会把真 TGF 当粒子否掉。** 把判据逐条拆开量
（GRID-04，每暴 2000 次相位，本底 677 c/s，四条判据同时成立才算存活）：

| 臂 | 只 count + fa | 加单路门 | 再加同戳门 | **同戳门单独因子** |
|---|---|---|---|---|
| A：03B 读出 + 03B 本底 | 22.56 | 22.56 | 22.56 | **1.000** |
| B：04 读出 + 03B 本底 | 9.47 | 9.47 | 5.95 | **0.628** |
| C：03B 读出 + 04 本底 | 16.49 | 16.49 | 16.49 | **1.000** |
| D：04 读出 + 04 本底 | 5.63 | 5.63 | 3.80 | **0.675** |

**同戳门在 03B 读出下的因子恰好是 1.000（A 和 C 都是），只在共帧读出下才咬人**——这道门
的惩罚 100% 由共帧读出造成，不是本底、也不是暴发本身的性质。单路门在四个臂上都是 1.000。

**一个反讽，值得写进讲稿。** 同戳门在 GRID-02 的**全量搜索**里几乎没开火（全任务只砍掉
5 个候选，03B 是 7797 个），却在**注入检验**里否掉 48.4% 的真暴发。原因是穿星粒子在共帧
读出下只留 4 个计数、压在几百 c/s 的本底上根本够不着候选门，轮不到这道门；而真暴发计数
多、够得着候选门，然后一头撞上去。**这道用来挡带电粒子的门，在共帧星上主要挡的是真
暴发。**
三因子分解：帧长削计数 B/A = 0.420、本底抬阈 C/A = 0.731、同戳门 0.675，乘积 0.207，
实测联合 D/A = 0.168（有交互）。

grid03b 用**单次确定性重放**量到同戳门因子 0.50（共帧后最佳窗同戳占比中位 0.400，
35 个里 27 个越过 0.35），grid04 用**每暴 2000 次随机相位**量到 0.675，差 1.35 倍。
**两个数口径不同，不取平均**：确定性重放里计数挤得紧、同戳占比偏高；随机相位版更贴近
"真实暴发不会正好按 03B 那次的相位落在 04 的帧栅格上"。

最典型的是 03B 那个最像真 TGF 的 2022-10-04
（78.7 µs、21 个计数、有闪电认证）搬到 GRID-04 的读出上，2000 次随机相位里：

| 败在 | 占比 |
|---|---|
| 计数不足（`count < 8`） | **0.000** |
| `fa` 不够 | 0.005 |
| **同戳门** | **0.757** |

**计数完全够（一次都没败在计数不足）、`fa` 中位 8×10⁻¹²，四分之三的试验纯粹是被同戳门
当带电粒子否掉的**，D 臂同戳占比中位 0.364 正压在 0.35 那道门上。另外两个也一样：
2023-03-20（66.5 µs、12 个计数）败在同戳门 0.422，2022-08-21（41.2 µs、13 个）0.234。
grid03b 用独立实现的单次确定性重放得到同一候选同戳占比 0.500 被否——两边对上了。

所以准确的说法不是"三颗星探测能力差"，而是**"三颗星的读出让 TGF 既记不全、又被自己的
判据否掉"**。

注入流程本身的健全性检查（A 臂逐暴存活率与各自实测 `fa` 严格单调）：`fa ≤ 10⁻²⁰` 的
5 个全是 1.00；10⁻²⁰–10⁻¹⁰ 的 9 个 0.83–1.00；10⁻¹⁰–10⁻⁶ 的 8 个 0.59–0.89；
10⁻⁶–10⁻⁵ 的 7 个 0.31–0.54。**A = 0.774 缺的那 0.226 全部来自门槛附近的统计涨落，
不是流程偏差。**

**(6) 曝光要按有效搜索时间算，还要再乘一个"可用曝光"因子。** 日历天数是错的分母：

| 星 | 有效搜索时间 | 可定位曝光 | \|磁纬\| < 25° 的可定位曝光 |
|---|---|---|---|
| GRID-03B | 130.05 有效搜索日 / GTI 3121.2 h | 2637.9 h（84.5%） | 1783.0 h |
| GRID-04 | 109.25 有效搜索日 / GTI 2621.9 h | 1876.2 h（71.6%） | 1112.5 h（占 GTI 42.4%） |
| GRID-07 | GTI 1305.1 h（6.69 h/天，比 03B 的 3.96 高 69%） | 820.8 h（62.9%） | 411.2 h |

GRID-07 有 89 天（454.9 h、34.9% 的曝光）位姿和拟合轨道都定不了位，候选直接进
`dropped_no_ephemeris`——这是硬损失，也是"曝光质量"因子的来源（07 可用曝光占比 31.5%
对 03B 的 57.1%）。

**(7) 最终期望数与残余显著性。**

| 星 | 朴素期望 | 折扣 | 折扣后期望 | 实测 | P(0) |
|---|---|---|---|---|---|
| GRID-02 | 2.94（低磁纬曝光比 0.1013） | 0.181 | **0.53** | 0 | **0.589** |
| GRID-04 | 12.5（严口径：可定位 ∧ \|磁纬\|<25° 的 1112.5 h × 03B 产率 0.01122/h） | 0.168 | **2.10** | 0 | **0.12** |
| GRID-07 | 6.69（可定位低磁纬曝光 411.2 h × 03B 产率） | 0.277 | **1.85** | 0 | **0.157** |
| 三星合计（未扣可定位曝光） | 39.8 | 0.138 | 5.49 | 0 | 0.004 |

**原先 4×10⁻⁹ 的反常不成立。** 残余在 0.004–0.35 之间，取决于哪颗星、以及是否把
"可定位曝光"因子算进去（GRID-04 那一栏还没有，见下）。三个理由说明这些期望都是
**上限**、真值更小：

- 注入源是"03B 已经探到的"那 29 个，按定义偏亮；固有族群里更暗的那部分在共帧星上
  损失更大。（**未验成**：没做族群亮度分布模型，收不了口。）
- **曝光要用"可定位 ∧ 低磁纬"的严口径**，不能用日历天数也不能用全 GTI：GRID-04 的
  GTI 2621.9 h 里只有 1876.2 h（71.6%）可定位、1112.5 h（42.4%）同时落在 |磁纬| < 25°；
  03B 是 3121.2 h / 2637.9 h（84.5%）/ 1783.0 h（57.1%）。GRID-07 更极端：89 天、
  454.9 h、34.9% 的曝光位姿与拟合轨道都定不了位，候选直接进 `dropped_no_ephemeris`。
  用宽口径（全 GTI）算 GRID-04 会得到朴素期望 24.4、折扣后 4.09、P(0) = 0.017——
  **偏严的那一版才对**，因为不可定位时段的候选是硬损失。
- 若基准换成只取 7 个有闪电认证的 TGF，GRID-04 的朴素期望 4.37、折扣后 0.73、
  P(0) = 0.48。

**GRID-02 的老实结论：排除不了同质，但也不需要。** 实测 0 的 95% 上限是 GRID-02 的单位
低磁纬曝光 TGF 率 ≤ 5.7 倍 03B——要推到 95% 排除需要现有低磁纬曝光的 5.7 倍（约再 400 个
观测日），而事例产品只到 2021-03-08，拿不到。**但不需要排除同质来解释"一个都没有"**：
那是共帧读出的直接后果，不吃统计量。7 个认证 TGF 那一档折扣 0.251、期望 0.18、
P(0) = 0.837。

**逐个候选的存活率在两种读出模型之间摆得很厉害**（GRID-02 上 2022-10-04 从四路独立的
0.550 掉到共帧的 0.005，2023-10-09 反过来从 0.000 升到 0.685），**只有群体量是稳的，
逐个的存活率不要单独引用。**

**(8) 最直观的一条事实，以及一条不能当独立证据的统计。** v10 **全部**候选（不限显著度）
里 `bin_size_best < 300 µs` 的个数：**GRID-02 = 0、GRID-03B = 303、GRID-04 = 1、
GRID-07 = 0**；各星最短窗 330 / **26** / 211 / 467 µs。**另外三颗星连低显著度的短窗
候选都产生不出来**——讲报告时这一句顶得上一整页论证。

严格说它不是独立证据：它仍然条件在"能成为候选"（`count ≥ 8` 落在一个窗里）上，而那
正是帧长限制的东西，所以**记作与帧长解释一致，不记作独立检验**。至于"**显著**候选的
最短窗长 03B 30 µs 对另外三星 859–920 µs"那条则是彻底循环的：`min_duration = 0`，
另外三星的最短窗之所以是 860–920 µs，只因为它们的显著候选全是顶在 1 ms 上限的软暴，
等于"有没有短暴"说两遍。**这一条已作废，不要再当线索用。**

**(9) 否决链不是嫌疑。** 全量累加，两颗星的构成几乎一样：`dropped_dead_gap` 吃掉
99.67%（03B）与 99.99%（04）的原始候选，其余各门加起来不到千分之三。按 `search.rs`
同款判据在随机 1 s 本底窗上量"平坦效率"：空洞门否决率 04 = 40.4% / 03B = 47.2%，
速率门 04 = 4.2% / 03B = 11.2%，**两门在 04 上反而更宽松**。`dropped_simultaneous`
03B 是 04 的三个数量级、是 07 的 64 倍，方向与死时间解释一致（03B 在同一次粒子穿越里
留下更多可分辨事例，更容易凑够 8 个进候选再被同戳门挡掉）。

**(10) 必须落实的推论：有效曝光只有 GRID-03B 的那一份。** 天格能用于 TGF 搜索的曝光是
03B 的 130.05 个有效搜索日（低磁纬可定位 1783 h），不是四星合计。所有按总曝光算的 TGF
率、上限、期望数都要按这个重算。**这一步还没做**，等死时间的逐过境全量序列确认没有
配置变更之后一次算清。

**(11) `MAX_SIMULTANEOUS_FRACTION = 0.35` 在共帧星上测的东西和它以为自己在测的东西
不是一回事。** 这道门建立在"同戳 = 穿星粒子"这个假设上。在 GRID-03B 上假设成立（同一
探头从不同戳，同戳必然是跨探头符合，是物理）；在 02/04/07 上**同戳是帧结构的必然产物**
——帧内四路共用触发那一击的时戳。所以对这三颗星它既挡不住真粒子（帧内符合把粒子和光子
一起塌到同一时戳），又会把真 TGF 当粒子否掉（实测 27/35）。这比"不改变目录"更根本。

裕度检验（`diag/lead/simul_margin.py`）：偶然同格占比的期望 ≈ (n−1)·q/T，q = 2⁻²² s。
GRID-03B 的 38 个显著候选，偶然期望中位 0.0114、最大 0.0694；实测同戳占比中位 0.236；
**实测/偶然中位 17×，阈到噪声的裕度（0.35 ÷ 偶然期望）中位 31×、最小 5×**。对照：
GECAM 500×（安全）、SVOM 0.5×（已判死）。**天格 03B 上这道门站得住。**

**当下的风险：这道门在 GRID-03B 上已经没有余量了。** 38 个显著候选的实测同戳占比
**最大 0.333**，门槛 0.35，**差 0.017**。任何一个比现有样本再亮一点、再短一点的真 TGF
都会被它直接否掉——而更亮更短正是我们想探的方向。把门槛定 0.30 会砍掉 12 个（闪电认证
0 个），定 0.25 会砍掉 22 个（含 2 个闪电认证）。**这是当前风险，不是历史记录。**

**它有一条已知边界，不是脚注。** n 大 T 小时偶然期望飙升，真信号和噪声挤到一起：
7 个闪电证实里最亮最短的两个（2022-08-21，41.2 µs / 13 计数；2022-10-04，78.7 µs /
21 计数）实测/偶然只有 2.2 和 1.6，同戳占比几乎就是偶然水平。对它们这是好消息（确实
不是粒子），但说明**这道门对"亮而短"的候选已经快没有分辨力**——天格再往更亮更短探就会
进入 SVOM 那个区。建议的做法是**逐候选标出偶然期望**，把"偶然期望 > 0.1"当作该候选不
适用此判据的标志，而不是全局用一个阈。

**现在不动 `search.rs`。** 改了要重跑全量，而这三颗星的显著候选已判定为磁层沉降电子
（未决项 14），改动不改变目录。可能的做法记在这里：对共帧星改用"帧数"而不是"事例数"
做同戳统计，或者对这三颗星不设这道门。

**B 角那 40 个的同戳裕度算不了，而这本身就是结论**，不是待办：它们来自共帧星，
(n−1)·q/T 这个模型在那里不成立，实测同戳占比反映的是读出机制，既不是物理也不是噪声。
